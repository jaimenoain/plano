# AI Status

## Current Phase
Programme Platform — Phase 4 complete, Phase 5 (Coverage Gap Prioritisation) next

## CURRENT_ARCHITECTURE_SNAPSHOT
- Monorepo: React 18 SPA (Vite) + Supabase backend
- Routing: React Router v6 with `createBrowserRouter`
- State: TanStack Query for server state
- Auth guard: AdminGuard checks `profile.role IN ('admin','app_admin')`
- **Programme platform** shipped (Phases 1–4):
  - Phase 4: `/admin/programme/broadcasts` — Broadcast composer and sent-message history with per-president read status. Tables `admin_broadcasts`, `admin_broadcast_reads`. Notifications type extended with `admin_broadcast`. RPCs: `send_admin_broadcast()` (rate-limited 3/day, resolves recipients by scope all/country/chapter), `toggle_broadcast_pin()`, `get_admin_broadcasts()`, `get_broadcast_read_status()`, `get_ambassador_broadcast_banners()`, `mark_broadcast_read()`. Embassy Leadership tab shows pinned + unread action_required banners. Presidents "Send message" button now links to broadcasts composer pre-addressed. Migration `20271125000000_admin_broadcasts.sql`.
- **Programme platform** shipped (Phases 1–3):
  - Phase 3: `/admin/programme/interventions` — Intervention Queue with 5 automated flag types (no_president, president_inactive, forming_stalled, at_capacity_open_apps, no_chapter_activity). Per-admin dismiss/snooze (7/14/30 days) via `admin_flag_dismissals` table. Sidebar badge shows active flag count. RPC `get_programme_intervention_flags()`, `dismiss_intervention_flag()` (migration `20271123000000_intervention_queue.sql`).
  - Phase 2: `/admin/programme/presidents` — president directory with slide-out panel. RPC `get_president_directory()` (migration `20271122000000_president_directory_rpc.sql`).
- **Programme platform** shipped (Phase 1):
  - Routes: `/admin/programme` (redirect) → `/admin/programme/health`
  - RPC `get_programme_health_summary` (migration `20271120000000_programme_health_rpc.sql`)
  - Sidebar: new "Programme" group with "Health Dashboard" entry
  - Pulse zone (4 stat cards), 30-day activity chart (edits + photos + 7-day rolling avg), flagged chapters list, top-5 chapters table
- **Plano Updates** feature shipped:
  - Table `plano_updates` (migration `20271026000000_plano_updates.sql`)
  - Storage bucket `plano-updates` (public read, admin write)
  - Public routes: `/updates` (listing), `/updates/:slug` (detail)
  - Admin routes: `/admin/updates`, `/admin/updates/new`, `/admin/updates/:updateId`
  - Footer link added to "Plano" section after "About"
  - Admin sidebar: "Updates" item added under Content group

## SCHEMA_DRIFT_LOG
- [2026-05-18] `ambassador_goals.current_value` defaults to `0` and has no writer (no trigger, no client update), so the counter and progress bar on `/embassy/goals` stayed at 0% no matter how many photos/edits/visits/firm-claims the ambassador made. Migration `20271030000000_ambassador_goals_progress_rpc.sql` adds RPC `get_my_ambassador_goals()` that derives `current_value` at read time from `review_images` (photos), `building_audit_logs` (edits), `user_buildings` (visits), and `company_stewards` (firms_claimed), scoped to events on or after `goals.created_at`. Frontend (`MyGoals.tsx`) switched from a direct select to this RPC. Status: **needs apply** in the Supabase SQL Editor.
- [2026-05-18] `find_nearby_buildings` had two overloads live in the DB: the original 3-arg `(lat, long, radius_meters)` from migration `20260421000001` and the canonical 4-arg `(lat, long, radius_meters, name_query)`. PostgREST returned `PGRST203` ("could not choose the best candidate function") for every RPC call, breaking the "Show nearby buildings" button on building detail pages. Migration `20271029000000_drop_orphan_find_nearby_buildings_overload.sql` drops the 3-arg variant. Status: **needs apply** in the Supabase SQL Editor.
- [2026-05-17] Generated types (`src/integrations/supabase/types.ts`) do not yet include `plano_updates`, `programme_campaigns`, `chapter_projects`, `outreach_log`, or several other tables added by recent migrations. API files for those features cast the client to `any` to bypass the type gap. Status: **open** — needs `npm run gen-types` after migrations are applied.
- [2026-05-17] `credit_role_enum` rename never reached the live DB: migration `20270896000000_rename_credit_roles_to_disciplines.sql` shared its timestamp with `20270896000000_fix_feed_silent_visits.sql` (both added in commit `6c3e4ce9`), so the Supabase migration tracker silently skipped the second one. App code and generated types use the new discipline-based labels (`design_architecture`, `architecture_of_record`, …), but the DB still holds the old person-based labels (`design_architect`, …), producing `invalid input value for enum credit_role_enum` errors on credit insert. Replacement migration `20271027000000_rename_credit_roles_to_disciplines.sql` is idempotent and supersedes the skipped file (which has been deleted). Status: **needs apply** in the Supabase SQL Editor.

## SCHEMA_DRIFT_FIXED
- [2026-05-20] `/embassy/goals` — 500 errors on `get_ambassador_my_audit_timeline` and `get_chapter_ambassador_activity`. Migration `20271132000000_fix_goals_page_rpc_500_errors.sql` unconditionally DROP+CREATE+GRANT both RPCs and their two internal helper functions, using the definitive latest bodies (photos counted from `review_images`, `_ambassador_can_access_chapter` auth guard, all activity columns including `outreach_count`). Status: **needs apply** in the Supabase SQL Editor. (Feedback id a6a4c786-422a-405d-ba2d-35b226c37ca1)
- [2026-05-20] Leaderboard on `/embassy/goals` only showed the top 10 chapter members. `MyGoals.tsx` applied a `leaderboard.slice(0, 10)` cap before rendering, then appended the current user with a "· · ·" separator only if they were outside the top 10. Members ranked 11+ were invisible to anyone but themselves — what the reporter saw as "not including all the chapter's members." The `get_chapter_ambassador_activity` RPC already returns one row per active member (active membership rows joined with profiles, including members with zero counts), so the cap was purely a UI choice. Removed the slice, the top-10 detection, and the "current user outside top 10" separator block — the leaderboard now renders every active member in score-desc / last-active / username order, exactly as the RPC returns them. No schema or API changes. (Feedback id 89499f18-501c-4ca5-a56d-2423cb7b3a2c — re-opened from the earlier "leaderboard empty" report on the same id)
- [2026-05-20] `/admin/ambassadors/campaigns` "Ambassador ideas inbox" hid drafts from chapters where the admin was not an active member. Two stacked issues: (1) RLS — the SELECT policy from `20271107000001` only allows visibility to active members of the same chapter (non-drafts) plus the author of the draft. The "Leadership can manage projects" FOR ALL policy from `20271017000000` scopes to a single chapter. No policy granted platform admins (`role IN ('admin','app_admin')`) cross-chapter access, so drafts and the Publish/Delete actions on out-of-chapter drafts were filtered out. Migration `20271131000000_admin_select_chapter_projects.sql` adds two admin policies on `chapter_projects` using `public.is_admin()`: `"Admins can view all chapter projects"` (SELECT) and `"Admins can manage all chapter projects"` (FOR ALL). (2) FK-hint — the query used `profiles!created_by(username)`, but `chapter_projects.created_by` references `auth.users(id)`, not `profiles(id)` (same class of bug as the `outreach_log` fix earlier today). Refactored `AmbassadorCampaigns.tsx` `admin-draft-ideas` query into a FK-agnostic three-step fetch: drafts → batched `ambassador_chapters` by id → batched `profiles` by id. `DraftIdea` shape flattened to `chapter_name`/`author_username`. Added an `isError` branch in the UI so future query failures surface as a visible message instead of an empty inbox. Status: **needs apply** in the Supabase SQL Editor. (Feedback id 44e9de96-3d71-416e-a0c9-6f937e6ce634)
- [2026-05-20] `fetchModerationCredits()` fetched ALL unmoderated credits globally without chapter scope. `ambassador_approve_credit()` validates chapter scope server-side and raises `out_of_scope` for any credit outside the ambassador's chapter, causing every approval in a bulk "Approve all" to fail with "20 approvals failed — please retry." Migration `20271118000000_get_ambassador_moderation_credits_rpc.sql` adds `get_ambassador_moderation_credits(p_chapter_id, p_limit)` RPC using the same `_building_in_ambassador_chapter_scope` guard as `get_ambassador_recent_buildings`. `fetchModerationCredits` updated to accept `chapterId` and call the RPC. `CreditsModerationTab` now receives `chapterId` from `CurationTool` and includes it in the query key. Code fix committed in `b5f376fa` (~18:20 UTC). Status: **needs apply** in the Supabase SQL Editor. (Feedback ids bcaa8607-5de5-4d4b-8e74-80a542610dd2, 8184ecff-5943-4be2-8f7f-c8bf64c87e4a — second report by @davolon at 17:48 UTC before fix landed)
- [2026-05-20] `fetchModerationVideos` in `taskFeed.ts` used `profiles(username)` without an FK hint when querying from `building_posts`. PostgREST detects two paths between `building_posts` and `profiles`: the direct FK (`building_posts.user_id → profiles.id`) and an M2M path through `review_images` (`building_posts.id ← review_images.review_id; review_images.user_id → profiles.id`). This caused a PGRST200 ambiguous-relationship error on every call to the Videos tab, producing "Failed to load videos." Fixed by changing the embed to `profiles!user_id(username)`. No migration required. (Feedback id a8eeba36-dafd-4ad1-8a90-3a8f04453019)
- [2026-05-20] `get_ambassador_recent_buildings` function body referenced `b.n` but the actual column on `buildings` is `b.community_preview_url`. Migrations `20271105000000` and `20271110000000` both had this typo; `20271105000000` was applied, causing every call to fail at runtime with "column b.n does not exist" → "Failed to load buildings." in the Moderation → Buildings tab. Migration `20271111000000_fix_recent_buildings_community_preview_column.sql` drops and recreates the function using `b.community_preview_url::text AS n`, incorporating the full schema from `20271110000000` (moderated_at, moderated_by_username, approved-buildings filter). Both source migrations also corrected. Status: **needs apply** in the Supabase SQL Editor. (Feedback id a1514d04-8527-412d-9b44-412c70657dac)
- [2026-05-20] `get_map_clusters_v3` was missing `exclude_construction_statuses` support — it was added in `20270906000000` but dropped when `20271019000000` (photography gap layer) and `20271031000000` (century filter) rewrote the function without it. The frontend always sends `exclude_construction_statuses: ['Demolished','Lost','Under Construction','Unbuilt']` as the default browse path; since the RPC ignored that key, `v_construction_statuses` stayed NULL and the status filter was bypassed entirely — causing unbuilt and lost/demolished buildings to appear on the Embassy photography gap map. Migration `20271103000000_fix_map_clusters_v3_exclude_statuses.sql` restores the variable, parser, and WHERE-clause guard. Status: **needs apply** in the Supabase SQL Editor. (Feedback id 58ffb924-1e2e-4b89-80cd-c235d334aa00)
- [2026-05-20] `get_ambassador_buildings_missing_metadata` (defined in `20270870200000_ambassador_task_feed_rpcs.sql`) still compared `building_credits.role` to the legacy enum value `design_architect`. Once migration `20271027000000` renamed `credit_role_enum` values to the discipline-based labels (`design_architect` → `design_architecture`), the RPC threw `invalid input value for enum credit_role_enum`, breaking the Embassy → Contribute → "Data & Research" tool with "Failed to load research tasks." Replacement migration `20271102000000_fix_ambassador_missing_metadata_credit_role_rename.sql` recreates the function using `design_architecture`. Status: **needs apply** in the Supabase SQL Editor.
- [2026-05-18] `get_discovery_feed` referenced `ub.video_url` (user_buildings) in its HAVING clause. Migration `20270872000000` dropped that column. Fixed in `20271028000000_fix_discovery_feed_video_url.sql` — replaced with an EXISTS subquery against `building_posts.video_url`.

- [2026-05-20] AI research `ambassador_apply_building_research` RPC field mismatch — live DB had the original `20271108000000` body which only handled `year_completed`, `access_level`, `access_logistics`, and referenced non-existent junction-table concepts (`materials`, `typology`, `context`). The frontend was already sending the corrected field set (`status`, `alt_name`, `access_cost`, `access_notes`, `size_sqm`, `height_m`, `storeys`) but those 7 fields were silently dropped on save. `Contribute.tsx` `FIELD_LABELS` was also stale (still mapped the old fields, missing all new ones). Frontend updated (labels + textarea condition cleaned up). Migration `20271133000000_fix_building_research_rpc_final.sql` DROP+CREATEs the RPC with the full correct field set. **Superseded by `20271134000000` — apply that migration instead.** (Feedback ids 9513bf1c, df6fd550, 1c365260-b0ba-404f-a252-3442115d4947)
- [2026-05-20] AI research missing taxonomy fields (category, typologies, style, materiality, context) — the system prompt and RPC only covered direct `buildings` columns; fields backed by junction tables (`building_functional_typologies`, `building_attributes`) and the FK `functional_category_id` were never requested from Claude nor applied on save. Updated `RESEARCH_SYSTEM_PROMPT` and user prompt in `building-research.route.ts`. Added `FIELD_LABELS` entries and `ARRAY_FIELDS` set in `Contribute.tsx`; `handleSave` splits comma-separated values for array fields. Migration `20271134000000_extend_building_research_taxonomy_fields.sql` DROP+CREATEs the RPC with taxonomy support (case-insensitive name resolution, additive junction-table inserts ON CONFLICT DO NOTHING). **Apply `20271134000000` in the Supabase SQL Editor** (supersedes `20271133000000`). (Feedback id aa8e2827-42df-406e-b7d6-537454948a41)

## SCHEMA_DRIFT_FIXED
- [2026-05-21] `get_chapter_metrics` 500 error on `/embassy/leadership` (feedback 033ee938, ee2b3fd3-9b44-4f13-97b0-a0e81e698cfa): `20271133000000_fix_chapter_metrics_500_error.sql` shared its timestamp with `20271133000000_fix_building_research_rpc_final.sql`, so Supabase's migration tracker silently skipped the metrics fix — `get_chapter_metrics` retained a lost GRANT EXECUTE from prior DROP+CREATE cycles. Renamed/superseded by `20271135000000_fix_chapter_metrics_500_error.sql` (idempotent DROP+CREATE+GRANT using the definitive body from `20271128000000`). `get_chapter_ambassador_activity` was already covered by `20271132000000`. **Apply `20271132000000` and `20271135000000` in the Supabase SQL Editor.**
- [2026-05-20] Video approval in Embassy → Contribute → Moderation → Videos did not persist. `VideosModerationTab` only mutated a client-side `dismissed` Set on "Approve" / "Approve all" — no RPC, no DB write — and `fetchModerationVideos` had no `moderated_at IS NULL` filter, so every refresh re-fetched the full `building_posts` set with `video_url IS NOT NULL` and the same videos reappeared. Photos and Credits had matching schema + RPC scaffolding (migrations `20271113000000`, `20271114000000`); Videos had none. Migration `20271129000000_video_moderation_approval.sql` adds `building_posts.moderated_at TIMESTAMPTZ` and `building_posts.moderated_by UUID → profiles`, and creates SECURITY DEFINER RPC `ambassador_approve_video(p_post_id)` (verifies active ambassador, ensures post has a `video_url`, stamps the columns idempotently, writes `ambassador_video_approval` audit log). `fetchModerationVideos` updated with `.is("moderated_at", null)`. `VideosModerationTab` now mirrors `PhotosModerationTab`: per-video approve button with spinner, real batch "Approve all" calling `approveVideo()` from `taskFeed.ts`, query invalidated on success. No exclusion-list fix needed (no architect-guard trigger on `building_posts`). Status: **needs apply** in the Supabase SQL Editor.
- [2026-05-20] `get_chapter_metrics.total_photos_added` and `get_chapter_ambassador_activity.photos_added` counted only `building_audit_logs` rows where `hero_image_url` changed from null → value. Photos uploaded by ambassadors through the building "save note" flow insert directly into `review_images`, which produces no audit-log row (the trigger only fires on `buildings UPDATE`); `hero_image_url` is only touched when a photo is later promoted to hero. Net effect: the Photography tool's primary output never showed on `/embassy/leadership`, and (separately) `total_score` in the activity RPC summed edits + visits + firms + moderation + outreach but omitted `photos_added` entirely, so even correct photo counts would not influence the leaderboard. Migration `20271128000000_count_real_photo_uploads_in_chapter_activity.sql` rebuilds both RPCs: photos are now counted from `review_images` joined to `building_posts → buildings` and gated by `_building_in_ambassador_chapter_scope`; `total_score` and the `ORDER BY` include `photos_added`; `last_active_at` also considers `review_images.created_at`. Status: **needs apply** in the Supabase SQL Editor.
- [2026-05-20] `protect_review_image_fields` trigger (`check_review_image_update`) and `protect_building_official_fields` trigger (`check_building_official_update`) compared `to_jsonb(NEW/OLD)` after excluding only `is_official`/`updated_at` (images) or `hero_image_id`/`architect_statement`/`updated_at` (buildings). `moderated_at` and `moderated_by` added by migrations `20271113000000` and `20271110000000` were not excluded. When `ambassador_approve_photo()` or `ambassador_approve_building()` stamped those columns, the trigger detected an "illegal" change for any ambassador who is also a verified architect for that building but didn't upload the photo/building. Result: RAISE EXCEPTION → 400 for one specific photo or building. Migration `20271121000000_fix_architect_guard_for_moderation_approvals.sql` extends both JSONB exclusion lists with `- 'moderated_at' - 'moderated_by'`. Status: **needs apply** in the Supabase SQL Editor. (Feedback id 7643d28d-3e05-40be-b01b-4b7944974af7)

## KNOWN_ISSUES

- [2026-05-20] — **Leaderboard on /embassy/goals still not showing ambassadors ranking** (feedback 1b73c182-e724-40b1-a849-8c78c2d3db37): Two causes. (1) `get_chapter_ambassador_activity` has a lost `GRANT EXECUTE` in the live DB — migration `20271132000000_fix_goals_page_rpc_500_errors.sql` has been written and is in the repo but **has not been applied to the live DB**. Apply it in the Supabase SQL Editor. (2) Frontend loading-state bug: while `ambassador_memberships` query was resolving, `chapterId` was `undefined` and the disabled leaderboard query showed `loadingLeaderboard = false`, so `!chapterId && !loadingLeaderboard` fired immediately and displayed "You're not in a chapter yet." before the membership fetch completed. Fixed in `MyGoals.tsx`: added `isLoading: loadingMembership` destructuring and reordered the render condition so loading skeletons show while either query is in flight, with the "not in a chapter" fallback only after both have resolved. **Pending DB fix**: apply `20271132000000` in Supabase SQL Editor.

- [2026-05-20] — **"Failed to submit idea" on /embassy/projects** (feedback c25dedc9): Root cause is migration `20271107000001_chapter_projects_draft_ideas.sql` not yet applied to the live DB. Two blockers: (1) `chapter_projects.status` CHECK constraint only allows `('active','completed','archived')` — `'draft'` causes a constraint violation; (2) the RLS INSERT policy `"Ambassadors can submit draft ideas"` does not yet exist, so non-leadership ambassadors get a policy denial. Both are fixed by applying the pending migration in the Supabase SQL Editor. Code is correct; `onError` now logs the raw DB error to console for future diagnostics.
- [2026-05-20] — PostGIS `public.spatial_ref_sys`: migrations must **not** use `ALTER TABLE … ENABLE ROW LEVEL SECURITY` / `CREATE POLICY` on hosted Supabase (table owner `supabase_admin`; SQL Editor runs as `postgres` → **42501**). Use **`REVOKE ALL`** on `anon`, `authenticated`, **`PUBLIC`** (see `20270807000000`, `20270818000000`, `20271101000000_enable_rls_on_public_tables.sql`). The security linter may still list RLS-disabled on this table until Supabase exposes an owner-transfer or linter exception — security posture matches “no API role direct access.”
- [2026-05-17] — Pre-existing: generated types stale; multiple tables from recent migrations missing. Non-blocking (api files use `any` cast). Resolution: run `npm run gen-types` after applying all pending migrations.
- [2026-05-17] — `UpdateDetail.tsx` renders body as plain `whitespace-pre-wrap` text; if Markdown rendering is needed in future, add a library (e.g. `react-markdown`).
- [2026-05-20] — Pre-existing TypeScript errors in two files (not related to credits fix): (1) `src/features/guides/useGuides.ts` — `queryFn` receives TanStack Query context object but is typed to accept `number | undefined`; causes cascade errors in `GuidesPage.tsx`. (2) `src/features/localities/pages/LocalityPage.tsx` — two unused variable declarations (`VolunteerTeamMemberCard`, `LocalityBuildingsGrid`) flagged by `noUnusedLocals`. Note: `DiscoveryCard.tsx` temporal dead zone bug (block-scoped `y` used before declaration) was fixed on 2026-05-20 — it was causing the explore page to crash with an AppErrorBoundary render error.

## Completed Tasks
- Leaderboard loading-state race condition on /embassy/goals (2026-05-21, feedback 1b73c182-e724-40b1-a849-8c78c2d3db37): While `ambassador_memberships` was fetching, `chapterId` was `undefined` and the leaderboard query (disabled) reported `loadingLeaderboard = false` — so `!chapterId && !loadingLeaderboard` fired immediately and showed "You're not in a chapter yet." before the user's chapter was known. Fixed in `MyGoals.tsx`: destructured `isLoading: loadingMembership` from the membership query and refactored the leaderboard render condition to check `loadingMembership || loadingLeaderboard` first (shows skeletons), then the error branch, then the `!chapterId` fallback. **DB fix still required**: the underlying `get_chapter_ambassador_activity` GRANT EXECUTE issue requires migration `20271132000000` to be applied in the Supabase SQL Editor.
- Draft ideas submitter visible on /embassy/projects (2026-05-21, feedback 91ced1af-a1d9-4912-9143-8f3c931e89a5): `DraftCard` in `ChapterProjects.tsx` only showed "Submitted X ago" with no author. `chapter_projects.created_by` references `auth.users(id)` not `profiles(id)`, so FK-hint joins don't work. Extended the `chapter-projects` query to batch-fetch `profiles` by UUID for all draft `created_by` values and attach `author_username` to each draft row. `DraftCard` now renders `· @username` after the timestamp. No schema or migration changes.

- "Submit Idea" button visible to leaders on /embassy/projects (2026-05-21, feedback 29fe788c-881a-400b-8e69-27e69551a4bc): The "Submit Idea" button in `ChapterProjects.tsx` was rendered unconditionally for all ambassador roles. Presidents and ExCo can directly create projects via "Pin Project", so showing "Submit Idea" to them was redundant and confusing. Fixed by wrapping the button in `{!isLeader && (...)}` — the same guard already used on "Pin Project" in reverse. No schema or API changes.
- Data & Research tool split into tabs (2026-05-20, feedback 12460cc4-c8a0-4a78-ab58-0fcc87dd82bc): `/embassy/contribute?tool=research` now uses `Tabs` ("Data completion" / "Duplicate Detection") inside `DataResearchTool` in `Contribute.tsx`. "Data completion" preserves the existing search + filter chips + missing-metadata building cards. "Duplicate Detection" is a new `DuplicateDetectionPanel` component that mirrors the section from `MergeEntities.tsx`: entity-type sub-tabs (building/person/company/locality), similarity-threshold slider, max-results select, "Run Query" button, and a results grid where each pair's "Analyze & Merge" button navigates to `/admin/merge/${entityType}/${id1}/${id2}`. Calls the same `get_potential_duplicates`/`get_potential_duplicate_people`/etc. RPCs, which are admin-gated server-side — non-admin users hit the existing "Duplicate detection is not yet available for {type} records." graceful empty state. No schema or API changes.
- Feedback form not capturing browser console info (2026-05-20, feedback 086a353b-5a3d-4f47-8d48-7d5554ee4ddd): `ConsoleErrorInterceptor.tsx` only monkey-patched `console.error()` — uncaught JS errors (`window.error` events) and unhandled promise rejections (`window.unhandledrejection` events) were handled in `root.tsx` for DB diagnostics but never written to `capturedErrors[]`. Result: the feedback widget always sent an empty `consoleErrors` array when errors came from runtime crashes or unhandled rejections rather than explicit `console.error()` calls. Fix: extended `ConsoleErrorInterceptor` to also patch `console.warn` and listen to both global error events, pushing prefixed entries (`[error]`, `[warn]`, `[uncaught]`, `[unhandledrejection]`) into `capturedErrors`. Buffer increased from 10 to 20. No schema or API changes.

- Edit button hidden from logged-in users on building detail page (2026-05-20, feedback 58adb214): `canEditOfficialData` in `useBuildingInteractions.ts` was gated to admins, verified architects, and creators-without-verified-architects — so the Edit button was invisible to regular logged-in users. DB RLS (`20260711000000_allow_all_users_edit_buildings.sql`) already allows any authenticated user to UPDATE buildings, so the restriction was frontend-only. Changed `canEditOfficialData` to `!!user` (any logged-in user). Separately, `EditBuilding.tsx` only fetched data when `id && user` were truthy but never redirected unauthenticated visitors — added auth guard following the `EditNote.tsx` pattern: wait for auth to resolve, then redirect to `/login` if no session. Removed the now-unused `isVerifiedArchitect` useMemo and prefixed unused destructured reads with `_`. No schema or migration changes.
- AI research taxonomy fields missing (2026-05-20, feedback aa8e2827-42df-406e-b7d6-537454948a41): Category, typologies, style, materiality, and context were absent from both the AI prompt and the save RPC. Updated `RESEARCH_SYSTEM_PROMPT` + user prompt in `building-research.route.ts` to request all five. Added `FIELD_LABELS`/`ARRAY_FIELDS` in `Contribute.tsx`; `handleSave` now sends array fields as string arrays. Migration `20271134000000_extend_building_research_taxonomy_fields.sql` extends `ambassador_apply_building_research` to resolve taxonomy names to UUIDs and write to `building_functional_typologies` and `building_attributes` (additive, ON CONFLICT DO NOTHING). Apply `20271134000000` in Supabase SQL Editor (supersedes `20271133000000`).
- AI research fields not saving correctly (2026-05-20, feedback 1c365260-b0ba-404f-a252-3442115d4947): Live DB was running the original `20271108000000` RPC which only mapped `year_completed`, `access_level`, `access_logistics` — silently discarding the other 7 fields the frontend was sending (`status`, `alt_name`, `access_cost`, `access_notes`, `size_sqm`, `height_m`, `storeys`). `FIELD_LABELS` in `Contribute.tsx` still had the old stale field names, so new fields showed raw snake_case keys in the review panel. Fixed: updated `FIELD_LABELS` to the full current field set, removed stale `architect_statement` reference in the textarea condition and old "comma-separated" hint. Migration `20271133000000_fix_building_research_rpc_final.sql` DROP+CREATEs `ambassador_apply_building_research` with all 10 fields and clean REVOKE/GRANT. Apply in Supabase SQL Editor.
- Research review long-text fields rendered as single-line inputs (2026-05-20, feedback 7ff5f217): The Embassy → Contribute → Data & Research review panel mis-mapped which fields got a `Textarea` vs an `Input`. `access_logistics` (a short enum: walk-in / booking_required / tour_only / exterior_only) was the only field getting a `Textarea`, while the genuinely long fields — `access_notes` (up to 300 chars of practical visitor notes) and `architect_statement` (a verbatim quote) — were stuck in a single-line `Input`, truncating most of the value off-screen. Fixed in `Contribute.tsx` `ResearchReviewPanel`: swapped the condition so `access_notes` and `architect_statement` render as `Textarea` (`rows={3}`, `min-h-[80px]`); everything else stays as `Input`. No schema or API changes.
- Leaderboard empty on /embassy/goals (2026-05-20, feedback f4f31b9f): Membership query in `MyGoals.tsx` used `.single()` without a `status = 'active'` filter. For users with multiple membership rows (e.g. a past inactive chapter + a current active chapter), `.single()` threw PGRST116 — TanStack Query caught it silently, `chapterId` resolved to `undefined`, and the leaderboard query was never enabled, rendering an empty list with no loading state and no error. Fixed by adding `.eq("status", "active")` and switching to `.maybeSingle()`. Separately, the DB-level guard on `get_chapter_ambassador_activity` also needs migration `20271118000000` applied (see KNOWN_ISSUES above).


- Embassy Tasks tab (2026-05-20, feedback bf6e6d1e): Added `/embassy/tasks` as a top-level tab visible to all active ambassadors. New `chapter_tasks` table (`chapter_id`, `title`, `description`, `due_date`, `visibility`, `status`, `created_by`, `assigned_to`, `project_id`, `company_id`). Visibility rules: `'chapter'` (default, all members), `'leadership'` (president/exco only — only leaders can set this in the UI), `'only_me'` (creator only). RLS enforces all three. RPC `get_chapter_tasks(p_chapter_id)` returns tasks with joined display names for assignee, project, and firm. `Tasks.tsx` page: grouped by status (To do / In progress / Done), one-click status cycling, create/edit dialog with assignee dropdown (from team members), project link dropdown, firm search, due date, and visibility picker. Migration `20271124000000_chapter_tasks.sql` needs apply in Supabase SQL Editor.
- Admin campaigns page shows ambassador draft ideas (2026-05-20, feedback 7ca48186): `/admin/ambassadors/campaigns` now has an "Ambassador ideas inbox" section above the programme campaigns table. Queries all `chapter_projects` with `status = 'draft'`, joined with `ambassador_chapters(name)` and `profiles!created_by(username)`. Each idea card shows chapter, submitter, age, with Publish (→ `status = 'active'`) and Delete actions. Note: ideas only appear once migration `20271107000001_chapter_projects_draft_ideas.sql` is applied.
- Photo approval 400 for specific photo (2026-05-20, feedback 7643d28d): `protect_review_image_fields` BEFORE UPDATE trigger on `review_images` calls `check_review_image_update()`. When `ambassador_approve_photo()` stamped `moderated_at`/`moderated_by`, the trigger's JSONB comparison (which only excluded `is_official` and `updated_at`) detected those new columns as a "non-official" change. If the approving ambassador happened to be a verified architect for that specific building and wasn't the photo uploader, the trigger raised `'Architects can only update the is_official flag on community images.'` → 400. Same structural flaw existed in `check_building_official_update` for `ambassador_approve_building`. Migration `20271121000000_fix_architect_guard_for_moderation_approvals.sql` extends both JSONB exclusion lists to also strip `moderated_at` and `moderated_by`. Apply in Supabase SQL Editor.
- Chapter President Directory (Phase 2, 2026-05-20): Added `/admin/programme/presidents` — searchable/filterable table of all active chapter presidents with avatar, chapter status, member count, last active, edits (30d), open applications. Clicking a row opens a Sheet slide-out with expanded metrics, ExCo members list, "Manage chapter" link, and stubbed "Send message" button (Phase 4). Migration `20271122000000_president_directory_rpc.sql` adds `get_president_directory()` RPC. Sidebar "Presidents" entry added. Migration needs apply in Supabase SQL Editor.
- Programme Health Dashboard (Phase 1, 2026-05-20): Added `/admin/programme/health` with pulse stat cards (active/forming/inactive chapters + pending applications), 30-day activity chart with 7-day rolling average, flagged chapters list (no president / president inactive / forming stalled), and top-5 chapters table. Migration `20271120000000_programme_health_rpc.sql` needs apply in Supabase SQL Editor.
- Chapter metrics and activity still failing on /embassy/leadership (2026-05-20, feedback 6a636d1d): Both `get_chapter_metrics` and `get_chapter_ambassador_activity` were erroring in the live DB. `get_chapter_metrics` had never been through a DROP+CREATE+GRANT cycle — all prior migrations used `CREATE OR REPLACE` which can silently lose EXECUTE grants in some Supabase environments. `get_chapter_ambassador_activity` was missing GRANT EXECUTE since migration `20271112000000` dropped and recreated it without one; migration `20271117000000` was written to fix it but may not have been applied to the live DB. Migration `20271118000000_fix_chapter_metrics_and_activity_grants.sql` covers both: (1) DROP+CREATE `get_chapter_metrics` with `REVOKE ALL / GRANT EXECUTE TO authenticated`, keeping the same leader-only auth guard; (2) re-applies the full `get_chapter_ambassador_activity` fix from `20271117000000` (full column set, `_ambassador_can_access_chapter()` guard, GRANT EXECUTE) — idempotent if `20271117000000` was already applied. Apply `20271118000000` in the Supabase SQL Editor.
- Interaction History in firm outreach drawer (2026-05-20, feedback c02fc4c1): `FirmOutreachDrawer` queried `outreach_log` using `profiles!ambassador_id(username, avatar_url)` — a PostgREST FK-hint join that requires `outreach_log.ambassador_id` to have a direct FK to `public.profiles`. The original migration (`20271015000000`) set that FK against `auth.users` instead; migration `20271115000000` (written in a prior session) re-points it but has not yet been applied, so every drawer open threw a PostgREST relationship error → "Failed to load interactions." Fixed by splitting the query into two steps: (1) fetch `outreach_log` rows without any join, (2) batch-fetch `profiles` by `id IN (ambassador_ids)`. This is FK-agnostic and works in both the current (unapplied-migration) state and after `20271115000000` is applied. Also replaced the dead-end "Please close and reopen." error message with a "Try again" button that calls `refetch`.

- Leadership page "Could not load" for metrics and activity (2026-05-20, feedback 0d31f8d5): Two root causes. (1) `20271112000000_fix_leaderboard_all_contributions.sql` used DROP+CREATE without a GRANT EXECUTE, revoking execute permission on `get_chapter_ambassador_activity` for ALL authenticated users — including active leaders — producing "permission denied for function" on every call → "Could not load activity." (2) The `Leadership.tsx` loader checked `role` but not `status`, so an inactive leader could reach the page while `is_chapter_leader()` (which requires `status = 'active'`) returned false → `get_chapter_metrics` returned 0 rows → `metrics = null` → the `|| !metrics` condition showed "Could not load chapter metrics." Two conflicting `20271116000000_*` migrations had been written as fixes but shared a timestamp (the Supabase tracker would only apply one) and the outreach-fix variant also reverted the auth guard to leaders-only. **Fix**: Migration `20271117000000_fix_ambassador_activity_access_and_columns.sql` supersedes both `20271116000000_*` files — drops and recreates the function with `_ambassador_can_access_chapter()` guard, full column set (edits, photos, visits, firms, moderation, outreach, total_score, last_active_at), and `GRANT EXECUTE TO authenticated`. `Leadership.tsx` loader now also checks `membership.status === 'active'` to prevent inactive leaders from reaching the page. `EmbassyLeadership.tsx` metrics section now distinguishes "error" from "empty data" — showing a neutral "No metrics data available yet." instead of the destructive-red error message when the RPC returns no rows. **Do not apply** `20271116000000_fix_leaderboard_access_for_all_ambassadors.sql` or `20271116000000_fix_ambassador_activity_moderation_outreach.sql` — apply `20271117000000` instead.
- Ambassador activity missing moderation and outreach (2026-05-20, feedback 54d13920): `get_chapter_ambassador_activity` RPC only counted `building_audit_logs` entries (edits + photos) and skipped two entire activity types: (1) moderation approvals written to `building_audit_logs` with `table_name IN ('ambassador_approval','ambassador_photo_approval','ambassador_credit_approval')` were lumped into `edits_count` without a separate visible column; (2) outreach interactions in `outreach_log` were not counted at all. The `EmbassyLeadership.tsx` activity table also only rendered Edits and Photos — `visits_count` and `firms_claimed_count` from the previous migration were fetched but never displayed. Migration `20271116000000_fix_ambassador_activity_moderation_outreach.sql` recreates the RPC to: separate `moderation_count` from `edits_count`; add `outreach_count` from `outreach_log.ambassador_id`; include both in `total_score`; extend `last_active_at` via GREATEST over audit-log and outreach timestamps. `ChapterAmbassadorActivityRow` type in `leadership.ts` extended with `moderation_count` and `outreach_count`. `EmbassyLeadership.tsx` activity table now shows Edits / Photos / Visits / Moderation / Outreach / Points columns; description text updated from "Based on building edit logs" to "All ambassador activity." Migration needs apply in Supabase SQL Editor.
- Leaderboard access for all ambassadors (2026-05-20, feedback 89499f18): `get_chapter_ambassador_activity` used `is_chapter_leader()` as its auth guard, so regular ambassadors (role = `ambassador`) always received an empty result set. Additionally, migration `20271112000000` used DROP+CREATE without a GRANT statement, silently stripping the `GRANT EXECUTE TO authenticated` set in `20270870300000`. Migration `20271116000000_fix_leaderboard_access_for_all_ambassadors.sql` recreates the function with `_ambassador_can_access_chapter()` (any active chapter ambassador) and restores `GRANT EXECUTE TO authenticated`. No frontend changes needed. Migration needs apply in Supabase SQL Editor.
- Outreach drawer not showing interactions (2026-05-20, feedback 1fc5fd47): `outreach_log.ambassador_id` referenced `auth.users(id)` instead of `public.profiles(id)`. PostgREST's FK-hint join `profiles!ambassador_id` requires a direct FK to `profiles`; without it the query returned a PGRST200 error, which TanStack Query caught — but because the drawer had no error branch, it fell into `!logs?.length` and showed "No interactions logged yet." Migration `20271115000000_fix_outreach_log_ambassador_fk_to_profiles.sql` re-points the FK to `public.profiles(id)`. Added an explicit error branch in `FirmOutreachDrawer` so future query failures surface as a visible message rather than a misleading empty state. Migration needs apply in Supabase SQL Editor.
- Video moderation tab fix (2026-05-20, feedback a8eeba36): `fetchModerationVideos` used `profiles(username)` without a FK hint when querying from `building_posts`. PostgREST found two paths to `profiles` (direct FK + M2M through `review_images`) and returned a PGRST200 ambiguous-relationship error, producing "Failed to load videos." Fixed by changing the embed to `profiles!user_id(username)` in `taskFeed.ts`. No schema or migration changes.
- "Failed to submit idea" root-cause investigation (2026-05-20, feedback c25dedc9): Traced error to migration `20271107000001_chapter_projects_draft_ideas.sql` not applied — live DB rejects `'draft'` status (CHECK constraint) and has no INSERT RLS policy for ambassador role. Code is correct; added `console.error` in `submitIdeaMutation.onError` to surface raw DB errors. Fix: apply the pending migration in Supabase SQL Editor.

- Credit approval persistence (2026-05-20, feedback c51f4adc): `Approve` and `Approve all` in the Embassy → Contribute → Moderation → Credits tab now persist to the database. Migration `20271114000000_credit_moderation_approval.sql` adds `building_credits.moderated_at TIMESTAMPTZ` and `building_credits.moderated_by UUID → profiles` columns, creates SECURITY DEFINER RPC `ambassador_approve_credit(p_credit_id)` (verifies active ambassador, checks credit's building is in chapter scope, stamps the columns, writes `ambassador_credit_approval` audit log via building_audit_logs). `fetchModerationCredits` updated with `.is('moderated_at', null)` filter so approved credits no longer reappear on refresh. `CreditsModerationTab` gains per-credit approve button and a real batch "Approve all" button, both calling `approveCredit()` in `taskFeed.ts`; on success, the query is invalidated. Migration needs apply in Supabase SQL Editor.
- Auto-advance rating overlay (2026-05-20, feedback b6ffc36d): `DiscoveryCard.tsx` now starts a 5-second `setTimeout` when the "Add points" rating overlay appears (`showRating = true`). If the user takes no action, `onSwipeSave` fires automatically and advances to the next building. The effect cleans up on unmount and when the user interacts early. No schema or API changes.
- Locality map crash + clustering + priority styling (2026-05-20, feedback 878b6e62): `BuildingPopupContent` called `useMapContext()` directly — throws "useMapContext must be used within a MapProvider" when a building pin popup opened on locality pages (no MapProvider in scope). Switched to `useOptionalMapContext` with `photographyGaps` defaulting to `false`. Added supercluster-based bubble clustering to `CollectionMapGL` (radius 60, maxZoom 16) replacing flat one-marker-per-building; clusters carry `max_tier` aggregated from constituent buildings for premium cluster styling. Priority tier-rank labels (Top 1% / Top 5% / Top 20% / Standard) computed from popularity-sorted order in `LocalityMap` and passed as `tier_rank` on each building before handoff to `CollectionMapGL`, matching search-page pin hierarchy. No schema changes.
- Photo approval persistence (2026-05-20, feedback 410311ad): `Approve` and `Approve all` in the Embassy → Contribute → Moderation → Photos tab now persist to the database. Migration `20271113000000_photo_moderation_approval.sql` adds `review_images.moderated_at TIMESTAMPTZ` and `review_images.moderated_by UUID → profiles` columns, creates SECURITY DEFINER RPC `ambassador_approve_photo(p_photo_id)` (verifies active ambassador, stamps the columns, writes `ambassador_photo_approval` audit log via building_audit_logs). `fetchModerationPhotos` updated with `.is('moderated_at', null)` filter so approved photos no longer reappear on refresh. `PhotosModerationTab` gains per-photo approve button (top-left hover overlay) and a real batch "Approve all" button, both calling `approvePhoto()` in `taskFeed.ts`; on success, the query is invalidated. Migration needs apply in Supabase SQL Editor.
- Leaderboard counts all contributions (2026-05-20, feedback f15215b4): `get_chapter_ambassador_activity` RPC recreated (migration `20271112000000_fix_leaderboard_all_contributions.sql`) to add `visits_count` (user_buildings in period, chapter-scoped), `firms_claimed_count` (company_stewards in period), and `total_score` = edits + visits + firms. Ordering changed from `last_active_at DESC` to `total_score DESC`. `ChapterAmbassadorActivityRow` in `leadership.ts` replaced with inline interface including new fields (supabase cast to `any` until gen-types runs). Leaderboard header updated from "Edits (30d)" to "Points (30d)"; score display switched from `edits_count` to `total_score`. Migration needs apply in Supabase SQL Editor.
- Flag reason picker on Moderation tool (2026-05-20, feedback ac67f0c2): `FlagButton` in `Contribute.tsx` refactored into a `Popover` with five reason options (Incorrect information, Low quality, Spam or off-topic, Copyright issue, Inappropriate content). Selecting a reason closes the popover, dismisses the item, and fires a toast with item label + chosen reason. `onFlag` signature extended to `(id, label, reason: FlagReason)` across all four moderation tabs. The inline photo-grid flag button unified with `FlagButton` via an `overlay` prop. No schema changes.
- Building approval persistence (2026-05-20, feedback 984713ff): `Approve` and `Approve all` in the Embassy → Contribute → Moderation → Buildings tab now persist to the database. Migration `20271110000000_building_moderation_approval.sql` adds `buildings.moderated_at TIMESTAMPTZ` and `buildings.moderated_by UUID → profiles` columns, creates SECURITY DEFINER RPC `ambassador_approve_building(p_building_id)` (verifies active ambassador + chapter scope, stamps the columns, writes `ambassador_approval` audit log), and rebuilds `get_ambassador_recent_buildings` to exclude already-approved buildings. Frontend `BuildingsModerationTab` now has a per-building "Approve" button and a batch "Approve all" button, both calling the RPC via `approveBuilding()` in `taskFeed.ts`; on success, the query is invalidated so approved buildings disappear from the list. `AmbassadorRecentBuilding` type extended with `moderated_at` and `moderated_by_username` (cast needed until next `gen-types` run). Migration needs apply in Supabase SQL Editor.
- Explore "Add points" overlay contrast fix (2026-05-20, feedback 214e580a): The rating overlay shown after a building is saved on the explore page had near-invisible text (`text-white/20` and `text-white/30`) on a `bg-black/85` backdrop. Raised unselected state opacities: header label `white/30 → white/60`, number/icon `white/30 → white/70`, point labels `white/20 → white/55`, "Next building" CTA `white/20 → white/50` (hover `white/80`). Selected state unchanged (brand-primary lime). No schema or API changes. (`DiscoveryCard.tsx`)
- Admin merge page design alignment (2026-05-20, feedback 1effb8a1): Brought `/admin/merge` and `/admin/merge/:entityType/:targetId/:sourceId` into alignment with design tokens. Replaced gradient text on h1 with plain `text-text-primary`. Replaced all raw palette colors (`green-500`, `red-500`, `amber-500`, `blue-500`) with semantic token aliases (`feedback-success`, `feedback-destructive`, `feedback-warning`). Reduced excessive border radius from `rounded-2xl/3xl/[2.5rem]` to `rounded-xl` (8px) at most, with most elements at `rounded-md` (4px) or `rounded` (2px). Removed `hover:scale-105 active:scale-95` scale transforms from all buttons. Replaced `shadow-2xl shadow-brand-primary/20` with flat `shadow-md`. Applied `rounded-none` to building photo aspect-ratio containers (removed `rounded-2xl` from image wrappers). No schema or API changes.
- Locality page editorial redesign (2026-05-20, feedback d9d2051a): Wired up three previously empty sections on locality pages (`/architecture/:cc/:city`). Migration `20271109000000_locality_editorial_rpcs.sql` adds three public SECURITY DEFINER RPCs: `get_locality_volunteer_team(locality_id)` (finds the local chapter, returns president → ExCo → ambassadors with public-safe fields), `get_locality_top_contributors(locality_id, limit)` (ranks users by buildings added × 3 + photos + visits), and `get_locality_collections(locality_id, limit)` (public collections with ≥ 2 buildings in locality, with preview images). Loader now fetches all three in parallel alongside `getLocalityBuildings`. Added new `LocalityVolunteerTeam` component rendered between City Guides and Local Experts. Migration needs apply in Supabase SQL Editor.
- Onboarding step-2 copy improvement (2026-05-20, feedback ae35227d): Replaced vague "Select the tools you'd like to help with. Your first pick will appear at the top of your Contribute page." with "Pick the areas where you'll make the most impact — tap them in priority order. Your Contribute page will be arranged to match." — new copy explains the ordering mechanic, not just the first pick. `Onboarding.tsx` line 193.
- Welcome page "Curation" → "Moderation" (2026-05-20, feedback 1a629357): `Onboarding.tsx` still had `key: "curation"` / `title: "Curation"` in the TOOLS array and the `ToolKey` union type. Renamed both to `"moderation"` / `"Moderation"` to match the earlier fix on Contribute.tsx (feedback 209e2994).
- Locality detail map broken (2026-05-20, feedback 178ba019): `MapMarkers` called `useMapContext()` unconditionally, throwing `"useMapContext must be used within a MapProvider"` whenever `CollectionMapGL` was rendered outside a `MapProvider` (all locality/country/collection pages). Added `useOptionalMapContext` to `MapContext.tsx` (returns `null` instead of throwing) and updated `MapMarkers` to use it with `photographyGaps` defaulting to `false` when no provider is present.
- Collapsed sidebar design fix (2026-05-20, feedback a21a46e0): `AdminSidebar.tsx` menu items had icon and label wrapped in a nested `<div>` inside the `<Link>`, with `justify-between` on the link. The shadcn Sidebar component's CSS selectors (`[&>svg]:size-4`, `group-data-[collapsible=icon]:!size-12`) require the SVG to be a direct child of the button — the nested div broke icon sizing and centering in collapsed mode. Fixed by removing the wrapper div and the `justify-between` class so icon and span are direct children of `<Link>`. Migrated the inline award badge `<span>` elements to `SidebarMenuBadge` (absolutely positioned, auto-hides in icon mode via `group-data-[collapsible=icon]:hidden`). Imported `SidebarMenuBadge` alongside the other sidebar primitives.
- Ambassador AI Research Tool (2026-05-20, feedback c3ab60b5): Added an AI-powered building research workflow to the Embassy → Contribute → "Data & Research" tool. Each building card now has a "Research with AI" button that POSTs to the new `/api/embassy/building-research` resource route. The route verifies ambassador membership, fetches building name/location, then calls Claude `claude-sonnet-4-6` with the `web_search_20250305` tool to extract year_completed, materials, typology, access_level, access_logistics, context, and architect_statement from live web sources. Results are returned as `ResearchDataPoint[]` (field, label, value, source_url, snippet). The UI transitions into a `ResearchReviewPanel` where each data point shows an editable value, source URL link, and verbatim snippet; users can accept/reject each item individually and click "Save N accepted" to commit. The save action calls the same route with `action: "apply"`, which invokes the new SECURITY DEFINER RPC `ambassador_apply_building_research` (migration `20271108000000_ambassador_building_research_rpc.sql`) that verifies chapter scope, applies the field updates, and writes an `ai_research_apply` audit log row. Migration needs apply in Supabase SQL Editor.
- Ambassador idea submission for chapter projects (2026-05-20, feedback 7b541b5c): Any active ambassador can now submit a project idea from `/embassy/projects` via a "Submit Idea" button. Ideas are stored in `chapter_projects` with `status = 'draft'`. Leadership (exco/president) sees a dedicated "Ideas inbox" section with Publish and Delete actions per draft. Regular ambassadors cannot see other members' drafts (RLS updated). Migration `20271107000001_chapter_projects_draft_ideas.sql` extends the status check constraint and adds/tightens the relevant RLS policies. Needs apply in Supabase SQL Editor.
- Video moderation inline player (2026-05-20, feedback f49b0308): `VideosModerationTab` in `Contribute.tsx` replaced the external "Watch" link (which 404'd when `video_url` was a relative Supabase storage path) with an inline `VideoPlayer` component. `getStorageAssetUrl` resolves the URL before passing it to the player, handling both full URLs and storage paths. The video URL text line was removed; building name, date, flag button, and "View Building" remain. Duplicate `react-router` import merged as a side-fix.
- Auto-advance to "In review" on copy prompt (2026-05-20, feedback cd5cb03c): `CopyFeedbackClaudePromptButton` gained an optional `onAfterCopy` callback. `FeedbackDetailContent` passes it as `() => onStatusChange(row, 'in_review')` when `!readOnly && row.status === 'open'`, so copying the Claude Code prompt on an Open ticket silently moves it to In review. No schema or service changes required.
- Team tab on Contribute (2026-05-20, feedback 4a54dac7): Added "Team" tool card to `/embassy/contribute`. New migration `20271106000000_add_get_chapter_team_rpc.sql` creates `get_chapter_team(p_chapter_id uuid)` SECURITY DEFINER RPC accessible to all active ambassadors (via `_ambassador_can_access_chapter` guard). Returns `user_id`, `username`, `avatar_url`, `role`, `exco_responsibility`, `joined_at` — no email. `fetchChapterTeam` added to `leadership.ts` with inline type. `TeamTool` renders members grouped by role (President → ExCo → Ambassadors) with avatar, username, role badge, ExCo responsibility label, and join month. Migration needs apply in Supabase SQL Editor. Superseded by feedback 69b00a07 — see below.
- Team promoted to top-level tab (2026-05-20, feedback 69b00a07): Removed "Team" from the tool cards on `/embassy/contribute`. Created `src/features/embassy/pages/Team.tsx` as a standalone page at `/embassy/team`. Added "Team" nav tab to `EmbassyLayout` (visible to all ambassadors, between "Chapter Projects" and "Leadership"). Route registered in `app/routes.ts`.
- Leaderboard shows current user when outside top 10 (2026-05-20, feedback b508dfc8): `MyGoals.tsx` leaderboard now checks whether the current user appears in the top-10 slice. If they don't, a "· · ·" separator and their row at their actual rank are appended. No schema or API changes — `get_chapter_ambassador_activity` already returns all chapter members.
- Building moderation thumbnail fix (2026-05-20, feedback 5f0c424a): `get_ambassador_recent_buildings` RPC extended (migration `20271105000000_add_community_preview_to_ambassador_recent_buildings.sql`) to return `buildings.n` (community preview image path). `BuildingsModerationTab` now resolves thumbnails via `getBuildingImageUrl(b.n ?? b.hero_image_url)` instead of using `b.hero_image_url` directly as an img src (which was a raw storage path, not a full URL). Migration needs apply in Supabase SQL Editor.
- Photo moderation gallery view (2026-05-20, feedback e1f4ac37): `PhotosModerationTab` in `Contribute.tsx` switched from a horizontal list (small 144px thumbnail per row) to a responsive gallery grid (`grid-cols-2 sm:grid-cols-3 lg:grid-cols-4`). Each cell is a square aspect-ratio tile with full-cover photo, gradient overlay showing building name/caption, a hover-reveal "View Building →" link, and a hover-reveal flag button in the top-right corner. No schema or API changes required.
- Moderation tool on Embassy/Contribute (2026-05-20, feedback 209e2994): Renamed "Curation" card to "Moderation". Replaced the single-list `CurationTool` with a tabbed `ModerationTool` (Buildings / Photos / Videos / Credits). Each tab fetches recent contributions (buildings via existing RPC; photos/videos/credits via direct Supabase queries in `taskFeed.ts`). Items show as a batch with a flag icon on hover (dismisses + toasts "Flagged for admin review") and an "Approve all" button at the end of the batch (dismisses the full visible set from local state). No schema changes required.
- Swipe-left hide confirmation (2026-05-20, feedback 3fa6247b): `DiscoveryCard.tsx` now animates the card off to the left (220ms ease-in) before calling `onSwipeHide`, giving the exit a visible fly-off. `Explore.tsx` `handleSwipeHide` adds `toast("Building hidden")` to match the existing `toast.success("Saved to your list")` pattern on swipe-right.
- Search page load waterfall (2026-05-20, feedback 3e89b545): `MapContext` now initialises `bounds` from URL lat/lng/zoom via `approximateBoundsFromCenter` instead of `null`. Both `get_map_clusters_v3` (map pins) and `get_buildings_list` (SERP sidebar) now fire in parallel with map tile loading, eliminating a ~0.5–2 s blank window. `useMapData` gained an `enabled: !isDegenerateBounds` guard to prevent a wasted zero-bounds RPC on any path that still passes the all-zero fallback. `BuildingSidebar.isLoading` fixed to show the spinner during the initial fetch regardless of bounds nullability.
- Explore swipe-save freeze (2026-05-20, feedback 1563ea71): After a right swipe committed (save), `x` was never reset — the card stayed displaced mid-swipe under the rating overlay, appearing frozen. Added `animate(x, 0, spring)` in `finishHorizontalSwipe` immediately after `setShowRating(true)` so the card snaps back to centre before the overlay is interacted with.
- Curation card enrichment (2026-05-20, feedback b7fc46ec): `get_ambassador_recent_buildings` RPC extended (migration `20271104000000_enhance_ambassador_recent_buildings.sql`) to return `address`, `lat`, `lng`, and `added_by_username` (via LEFT JOIN on profiles). `CurationTool` cards now show a hero image thumbnail, full address line (street + city + country), a "Map" link to Google Maps (lat/lng), and the correct contributor username. Migration needs apply in Supabase SQL Editor.
- Merge duplicate 404 fix (2026-05-20, feedback 748b12f8): `EditBuilding.tsx` navigated to `/admin/merge/${buildingId}/${d.id}` (2 segments), but the route is registered as `/admin/merge/:entityType/:targetId/:sourceId` (3 segments). Fixed by inserting the missing `building` entity-type segment: `/admin/merge/building/${buildingId}/${d.id}`. Also corrected stale re-exports in `src/features/admin/index.ts` that referenced the old filenames `MergeBuildings` and `MergeComparison` (now `MergeEntities` and `MergeComparisonEntities`).
- Photography map blank on first load (2026-05-20, feedback 2b9a9286): `PlanoMap.onLoad` now calls `map.resize()` before reading bounds — fixes blank map in freshly-mounted `MapProvider` (flex-container layout resolves after map init). `PhotographyTool`'s `useEffect` refactored to depend only on `view` (via ref pattern), preventing it from re-running on every map pan/zoom and interfering with map initialisation.
- Architect Outreach CRM drawer (2026-05-20, feedback 746458f7): Replaced the Dialog + single-row upsert on the Outreach page with a Sheet drawer that shows firm info (name, building count, country, link to firm page), a "Log Interaction" form (INSERT, not upsert — supports multiple entries per firm), and a full interaction timeline scoped to the firm across all ambassadors. Status badge in the list still shows the current user's most-recent status per firm.
- Century global filter (2026-05-19): Added Century accordion to search page Global filters (`DiscoveryFiltersPanel`); wired URL state, map/list/search RPCs via migration `20271031000000_add_century_filter_to_search_rpcs.sql` (needs apply in Supabase SQL Editor). Also extended `useURLMapState` to parse award/size filter params into `MapContext` so map browse respects drawer filters.
- Century filter UI (2026-05-19): Search drawer lists 21st→1st century plus B.C. checkbox; URL/RPC use `0` in `centuries` for B.C. (`century < 1`). Same migration updated if not yet applied.
- Plano Updates (2026-05-17): Created `plano_updates` table, storage bucket, full admin CRUD (list + create/edit form with hero image upload, tags, geo scope, publish toggle), public listing and detail pages, footer link, admin sidebar entry.
