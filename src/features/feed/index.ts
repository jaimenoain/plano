/**
 * Public barrel for the feed feature. Cross-feature consumers should import
 * from here (`@/features/feed`) rather than reaching into internal modules.
 * (Data access lives in `./api/*`, which is importable directly by rule.)
 */
