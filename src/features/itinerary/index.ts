/**
 * Public barrel for the itinerary feature. Cross-feature consumers should import
 * from here (`@/features/itinerary`) rather than reaching into internal modules.
 */
export { useItineraryStore } from "./stores/useItineraryStore";
