## Current Phase
Phase 2 — Feature-based architecture (Tasks 2.1–2.5, 2.97). Task 2.98 UAT is manual.

## CURRENT_ARCHITECTURE_SNAPSHOT
Domain code lives under `src/features/`: `buildings`, `feed`, `auth`, `explore`, `architect`, `collections`, `profile`, `admin`, `connect`, `notifications`, plus existing `maps`, `search`, `itinerary`. Each feature has `index.ts` barrels where specified. Shared UI stays in `src/components/` (`ui/`, `common/`, `layout/`, `pwa/`) plus root `NavLink.tsx`, `GoogleAnalytics.tsx`. Shared pages: `src/pages/NotFound.tsx`, `Terms.tsx`, `Post.tsx`. Shared hooks: `src/hooks/` (e.g. `useDebounce`, `useTaxonomy`, `usePwaInstall`, `useGooglePlacePhotos`, `useUserLocation`, `useIntersectionObserver`, `use-mobile`, `use-toast`). Cross-feature types remain in `src/types/` (`database.ts`, `classification.ts`, `feed.ts`, `plano-map.ts`). Admin API client code: `src/features/admin/api/`. `useAuth` / `AuthProvider` live in `src/features/auth/hooks/useAuth.tsx` (consumers use `@/features/auth/hooks/useAuth`). `VideoCompressionTest.tsx` removed (unrouted dead page).

## KNOWN_ISSUES
- `npm run typecheck` still fails with many pre-existing errors (stale generated `Database`, RPC names, etc.); Phase 2 did not change that. `npm run build` and `npm run test` pass after the move.
- `npm run lint` still reports hundreds of repo-wide issues (pre-existing); Phase 2 did not run a global lint cleanup.

## Completed Tasks
- Task 1.1–1.97: (see prior phase) data layer hardening, Zod, hook typing.
- Task 2.1: `src/features/buildings/` with components, hooks, pages, barrel; imports updated.
- Task 2.2: `src/features/feed/`, `auth/`, `explore/`; feed landing under `feed/components/landing/`; App + consumers rewired.
- Task 2.3: `src/features/architect/`, `collections/` (incl. `CollectionMapPage`); `types/architect` → feature `types.ts`; `types/collection` → feature `types.ts`; empty `components/architect`, `components/collections` removed.
- Task 2.4: `src/features/profile/`, `admin/`, `connect/`, `notifications/`; admin `api/` + `types/`; profile hooks + `user_buildings` type path.
- Task 2.5: App.tsx `@/components` aliases; removed `useAuth` shim file; empty `src/api/` removed; `VideoCompressionTest` deleted.
- Task 2.97: `npm run build` OK; `npm run test` OK; full-repo `npm run lint` still fails (pre-existing).
