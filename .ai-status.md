## Current Phase
Launch readiness — Track A (code) delivered (Tasks A.1–A.5, A.97). **Phase 3 (layout & navigation):** desktop shell uses sidebar `defaultOpen={true}` + offcanvas collapse; `SidebarInset` uses `bg-surface-default`; `AppLayout` no longer applies inner `max-w-5xl` (pages own max-width in Phase 4). Task A.98 UAT and Tracks B/C are manual.

## CURRENT_ARCHITECTURE_SNAPSHOT
Package `plano@0.1.0`. **Layout shell:** [`MainLayout.tsx`](src/components/layout/MainLayout.tsx) wraps authenticated routes with `SidebarProvider`, [`AppSidebar`](src/components/layout/AppSidebar.tsx) (`collapsible="offcanvas"`, link-based nav + left-border active state, plain-button user menu), and `SidebarInset` with `bg-surface-default`. [`AppLayout.tsx`](src/components/layout/AppLayout.tsx) still gates mobile `Header` / `BottomNav`; `fullWidth` prop retained with `void fullWidth` until Phase 4 wiring. Mobile chrome: [`Header.tsx`](src/components/layout/Header.tsx) and [`BottomNav.tsx`](src/components/layout/BottomNav.tsx) use Plano semantic tokens (no raw grays/neon hex on nav). **Public surface:** [`index.html`](index.html) and [`public/robots.txt`](public/robots.txt) use `plano.app` branding; GA removed from `index.html` and loaded only after analytics consent ([`src/lib/consent.ts`](src/lib/consent.ts), [`src/components/common/CookieConsent.tsx`](src/components/common/CookieConsent.tsx), [`src/components/GoogleAnalytics.tsx`](src/components/GoogleAnalytics.tsx)). GA measurement ID is centralized in [`src/lib/analytics.ts`](src/lib/analytics.ts) with optional `VITE_GA_MEASUREMENT_ID`. **Edge functions:** [`supabase/functions/og-tags`](supabase/functions/og-tags) (OG HTML + redirect for sharers) and [`supabase/functions/sitemap`](supabase/functions/sitemap) (dynamic XML); both use `verify_jwt = false` in [`supabase/config.toml`](supabase/config.toml). **Hosting:** [`docs/LAUNCH_HOSTING.md`](docs/LAUNCH_HOSTING.md) documents crawler routing (social bots only — not Googlebot for OG), sitemap proxy, and optional `STORAGE_PUBLIC_URL`. **Observability:** [`@sentry/react`](package.json) with [`src/lib/sentry.ts`](src/lib/sentry.ts), init in [`src/main.tsx`](src/main.tsx), error-boundary reporting and [`setSentryUser`](src/App.tsx) on auth; `VITE_SENTRY_DSN` in CI build env ([`.github/workflows/ci.yml`](.github/workflows/ci.yml)). **Contract:** [`docs/DATA_CONTRACT.md`](docs/DATA_CONTRACT.md) Edge Function registry + env vars updated for `og-tags`, `sitemap`, Sentry, optional storage URL for OG images.

## KNOWN_ISSUES
- **Button destructive text token:** `button.tsx` uses `text-feedback-destructive-foreground`; `tailwind.config.ts` has no `feedback-destructive-foreground` color key (CSS var exists in `index.css`) — destructive label colour may not compile in Tailwind until the key is added — Queue Item 2.
- **`react-hooks/exhaustive-deps` disabled:** Re-enable selectively when refactoring effects is safe per screen.
- **Supabase types vs client:** `createClient` is not generic-typed with `Database`; regenerating [`src/integrations/supabase/types.ts`](src/integrations/supabase/types.ts) via `npm run gen-types` must be validated against the live project schema.
- **Stale coverage HTML:** `coverage/` may still mention old Terms email until `npm run test:coverage` is run again.

## Completed Tasks
- Task 5.1–5.2: `strictNullChecks` enabled; shared + all `src/features/*` modules typecheck clean.
- Task 5.3: `no-explicit-any` → error; production `src` violations resolved (tests keep override).
- Task 5.4: `ci.yml` added; `CONTRIBUTING.md` branch protection; README script/CI pointer updated.
- Task 5.97: `typecheck`, `lint` (0 errors, 0 warnings), `test`, `build` (with placeholders) verified locally.
- Launch A.1: Legacy domain/branding cleanup; PWA manifest; MetaHead default; Terms contact → `support@plano.app`; Playwright test email domain updated.
- Launch A.2: `og-tags` edge function + hosting doc + `STORAGE_PUBLIC_URL` / image absolutization.
- Launch A.3: Cookie consent + consent-gated GA; early `loadAnalytics` in `main.tsx` for returning users.
- Launch A.4: Sentry SDK, boundaries, user context, `config.sentry`, CI `VITE_SENTRY_DSN`.
- Launch A.5: Dynamic sitemap edge function (`created_at` for buildings/architects; `profiles.updated_at` for lastmod when set).
- Launch A.97: `npm run typecheck`, `lint`, `test`, `build` all pass.
- Queue Item 1: `tailwind.config.ts` — removed `hsl()` wrappers from CSS variable color refs; sidebar colors mapped to Phase 1 semantic tokens.
- Queue Item 2: `src/components/ui/button.tsx` — rewritten with Plano semantic tokens (CVA variants + sizes).
- Queue Item 3: `src/components/ui/card.tsx` — rewritten with Plano semantic tokens.
- Queue Item 4: `src/components/ui/badge.tsx` — semantic tokens; `rounded-sm` + `uppercase tracking-widest`; brand/success/warning/destructive variants.
- Queue Item 5: `src/components/ui/input.tsx` — rewritten with Plano semantic tokens.
- Queue Item 6: `label.tsx`, `separator.tsx`, `skeleton.tsx` — Plano semantic tokens.
- Queue Item 7: `src/components/ui/select.tsx` — trigger/content/item/separator semantic tokens.
- Queue Item 8: `src/components/ui/dialog.tsx` — overlay, content, close, header/footer layout, title/description tokens.
- Queue Item 9: `sheet.tsx`, `drawer.tsx` — overlay/surface tokens, close ring, drawer content/handle/title/description.
- Queue Item 10: `src/components/ui/table.tsx` — wrapper border/radius/surface; header/footer/caption tokens; row hover/selected; head/cell typography.
- Queue Item 11: `src/components/ui/tabs.tsx` — TabsList/TabsTrigger/TabsContent semantic tokens; focus `ring-brand-primary`.
- Queue Item 12: `tooltip.tsx`, `scroll-area.tsx` — tokenized tooltip content + scroll thumb (`bg-border-default`).
- Queue Item 13: `sonner.tsx` — Plano semantic tokens + left-border variants (success/warning/error/info).
- Queue Item 14: `dropdown-menu.tsx`, `context-menu.tsx` — overlay panels, submenus, items, separators (semantic tokens).
- Queue Item 15: `popover.tsx`, `hover-card.tsx` — `surface-overlay`, border tokens, `shadow-lg`, `z-[1150]`.
- Queue Item 16: `alert-dialog.tsx` — overlay `bg-black/50`, content `surface-overlay` + `rounded-lg`, header/footer/title/description tokens.
- Queue Item 17: `command.tsx`, `navigation-menu.tsx`, `carousel.tsx` — semantic tokens; carousel controls `ghost` + `icon-md`.
- Queue Item 18: `sidebar.tsx` — `bg-background` → `bg-surface-muted` (SidebarInset, SidebarInput, menu button outline variant).
- Phase 3 — Layout & Navigation: `MainLayout.tsx` (sidebar open by default on desktop, `SidebarInset` surface token); `AppSidebar.tsx` (offcanvas, COMPONENT_SPEC §9-style assembly, no icon/hover collapse); `AppLayout.tsx` (removed inner max-width; `void fullWidth` for TS until Phase 4); `Header.tsx` + `BottomNav.tsx` semantic tokens.
