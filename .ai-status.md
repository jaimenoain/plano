## Current Phase
**Phase 5 — Design system polish (complete):** Bridge aliases removed from [`src/index.css`](src/index.css); [`tailwind.config.ts`](tailwind.config.ts) legacy shadcn color keys now point at semantic CSS variables; global token sweep across `src/features`, `src/pages`, `src/components` (including `ui/` completion, `NavLink`, root components); scrollbar + `.glass` use semantic `color-mix` / `var(--border-strong)`; quality gates (shadow discipline, `BuildingForm` `max-w-2xl`, admin Buildings row progressive disclosure with `opacity-100 md:opacity-0 md:group-hover:opacity-100`). **Verification:** `npm run build`, `typecheck`, `lint`, `npm run test` (321 tests) pass. **E2E:** `npx playwright test` → 2 passed / 38 failed when the app at `baseURL` reports missing required `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_GOOGLE_MAPS_API_KEY` (see browser console during tests). Re-run with a dev server on `http://localhost:8080` and a valid `.env` for UAT. **Build queue (re-verify):** Rating circles use `fill-brand-primary` (not invalid `fill-text-brand-primary`); `BuildingForm` “Add Aliases/Year/Architects” outline buttons use `rounded-sm`. **Queue 6 WCAG spot-check:** Pairs 1–4 align with AA intent; `#F59E0B` warning on white is borderline for normal text — keep warning copy at ≥14px bold or rely on icon/border emphasis.

## CURRENT_ARCHITECTURE_SNAPSHOT
Package `plano@0.1.0`. **Tokens:** Single source in `:root` — brand, surfaces, borders, text, feedback (+ foreground variants where defined), `--radius`. No `--background` / `--primary` / `--muted` bridge variables. **Tailwind:** `border`, `background`, `primary`, `muted`, `accent`, `popover`, `card`, `destructive`, `ring`, `input` theme keys resolve to `var(--surface-*)`, `var(--brand-*)`, `var(--text-*)`, `var(--feedback-*)`, `var(--border-default)` so legacy utility names remain valid at build time while app code uses explicit semantic class names. **UI kit:** Remaining primitives (`alert`, `checkbox`, `toggle`, `segmented-control`, `toast`, `menubar`, `calendar`, `chart`, `switch`, `slider`, etc.) aligned with semantic tokens. **Layout shell:** Unchanged — [`MainLayout.tsx`](src/components/layout/MainLayout.tsx), [`AppSidebar`](src/components/layout/AppSidebar.tsx), [`AppLayout.tsx`](src/components/layout/AppLayout.tsx). **Contract:** [`docs/DATA_CONTRACT.md`](docs/DATA_CONTRACT.md) (unchanged by this phase).

## KNOWN_ISSUES
- **Playwright / E2E:** Full `tests/**/*.spec.ts` run fails if the app at `baseURL` does not load (missing `VITE_*` env → blank/error shell). Not a design-token regression; fix by running Vite with committed env template filled in.
- **`react-hooks/exhaustive-deps` disabled:** Re-enable selectively when refactoring effects is safe per screen.
- **Supabase types vs client:** `createClient` is not generic-typed with `Database`; regenerating [`src/integrations/supabase/types.ts`](src/integrations/supabase/types.ts) via `npm run gen-types` must be validated against the live project schema.
- **Stale coverage HTML:** `coverage/` may still mention old Terms email until `npm run test:coverage` is run again.
- **Typecheck failures blocking RR7 Phase 1.2:** `npm run typecheck` currently reports extensive Supabase type mismatches (e.g. `user_buildings`, `architects`, `architectural_styles`, admin RPC names, and `AdminLayoutWithGuard` duplicate identifiers). RR7 migration Phase 1 checkpoint 1.2 (`typecheck` clean) is not yet satisfied.
- **RR7 Phase 2 dev/test checkpoint partially deferred:** RR7 framework mode is now structurally wired and `npm run dev` serves the app via SSR without `react-helmet-async` or `localStorage` boot errors (route config via `react-router.config.ts` + `app/routes.ts`, `src/routes.ts` shim, `src/entry.client.tsx`/`src/entry.server.tsx`, `src/pages/AdminSystemPlaceholder.tsx`, root loader in `src/root.tsx`, and a server-side `localStorage` shim). Several RR7-adjacent tests have been updated to match the new semantic styles and layout (`KanbanColumn` is-over/logic tests, `PopularityBadge`, `AllCaughtUpDivider`, `AppSidebar` trigger). However, `npm run test` still reports numerous failures in unrelated feature tests (e.g. `StatusBadge`, `FeedClusterCard`, `WriteReview` specs that assert old Tailwind classes like `bg-slate-100` or brittle DOM structures). Cleaning these up requires a dedicated “test alignment with semantic tokens/layout” slice; for now, the RR7 Phase 2 roadmap item “tests pass with zero failures” remains unchecked even though SSR dev is healthy.
- **RR7 Phase 4.4 (dev):** Googlebot-style `curl` checks documented in `docs/rr7-migration-roadmap.md` were run against `http://localhost:8080` with a running `npm run dev` and valid `.env.local`. Re-run against production/preview URLs when deployed.

## Completed Tasks
- **RR7 Phase 4.4 — SSR SEO checkpoint (dev):** Vite `~` → `src` alias; root `Layout` charset/viewport/theme-color + `Links` before `Meta`; `BuildingLocationMap` client-only map mount; dynamic Lucide icons via `createElement` / `NavIcon` where needed; `BuildingDetails` prerender shell (`interactiveUiReady`) so SSR emits title/meta without crashing on remaining client-only subtrees; `ArchitectDetails.loader` drops non-existent `nationality` column and surfaces Supabase errors. Roadmap 4.4 boxes marked complete with localhost curl evidence.
- **Sidebar (Mar 2026):** [`use-mobile.tsx`](src/hooks/use-mobile.tsx) `md` 768px; `SidebarRail` + triggers on [`AppSidebar`](src/components/layout/AppSidebar.tsx) / [`Header`](src/components/layout/Header.tsx) / [`Explore`](src/features/explore/pages/Explore.tsx). **[Shell]** [`MainLayout`](src/components/layout/MainLayout.tsx) **always** mounts `AppSidebar` (removed logged-out `/` branch that omitted it); desktop `SidebarProvider` **`open={true}`** + noop `onOpenChange` so off-canvas collapse cannot hide nav; guest [`Landing`](src/features/feed/pages/Index.tsx) header includes `SidebarTrigger`.
- Task 5.1–5.2: `strictNullChecks` enabled; shared + all `src/features/*` modules typecheck clean.
- Task 5.3: `no-explicit-any` → error; production `src` violations resolved (tests keep override).
- Task 5.4: `ci.yml` added; `CONTRIBUTING.md` branch protection; README script/CI pointer updated.
- Task 5.97: `typecheck`, `lint` (0 errors, 0 warnings), `test`, `build` (with placeholders) verified locally.
- Launch A.1–A.5, A.97: prior launch track (branding, OG, consent/GA, Sentry, sitemap).
- Phase 2–4 UI / layout queues: prior entries (button, card, badge, shell, feature pages).
- **Phase 5 — Queue 1:** [`src/index.css`](src/index.css) `@layer base`: `border-border-default`, `bg-surface-default text-text-primary`, `::selection` with `var(--brand-primary)` / `var(--brand-primary-foreground)`; scrollbar thumb `var(--border-strong)`; `.glass` via `color-mix` + semantic vars.
- **Phase 5 — Queue 2:** Fifteen targeted `ui/` files + remainder (`context-menu`, `sidebar`, selects, `textarea`, `calendar`, `chart`, `avatar`, `radio-group`, `switch`, `slider`, `LocationInput`, etc.) migrated to semantic tokens; `Toggle` uses `cn(toggleVariants({ variant, size }), className)`.
- **Phase 5 — Queue 3:** Portable `perl -pi` sweep over features, pages, common, layout, pwa, `NavLink`, maxdepth-1 components; follow-up fixes for `bg-card`, `border-background`, `border-border`, `text-primary` (safe lookbehind), `border-primary`/`ring-primary`.
- **Phase 5 — Queue 4:** Bridge block removed from `:root`; [`tailwind.config.ts`](tailwind.config.ts) legacy colors repointed to semantic variables.
- **Phase 5 — Queue 5–6:** Zero `hsl(var(--*` in `src/`; legacy class grep clean on `src/**/*.tsx|ts|css`; shadow/hover-shadow violations reduced; search overlays use `shadow-lg`; [`BuildingForm`](src/features/buildings/components/BuildingForm.tsx) wrapped `max-w-2xl mx-auto`; admin [`Buildings.tsx`](src/features/admin/pages/Buildings.tsx) row action progressive disclosure.
- **Phase 5 — Queue 7:** Vitest 321/321; [`DiscoveryBuildingCard.overflow.test.tsx`](src/features/search/components/DiscoveryBuildingCard.overflow.test.tsx) updated for `rounded-sm` badges; [`pinStyling` tests](src/features/maps/utils/pinStyling.test.ts) for `bg-surface-muted/80`.
- **Phase 5 — build queue (execution pass):** Queues 1–4 confirmed already applied in tree; gate greps clean (`hsl(var(--*` absent; legacy shadcn class strings absent in `src/**/*.{tsx,ts,css}`); `fill-brand-primary` + [`BuildingForm`](src/features/buildings/components/BuildingForm.tsx) `rounded-sm` follow-ups; Playwright outcome documented above.
- **Sidebar/Desktop shell (Mar 2026):** [`SidebarInset`](src/components/ui/sidebar.tsx) margin-left override removed to rely on the flex spacer only; [`MainLayout`](src/components/layout/MainLayout.tsx) `SidebarProvider` now uses `defaultOpen` only so desktop toggling and cookie persistence work; [`AppLayout`](src/components/layout/AppLayout.tsx) now renders [`Header`](src/components/layout/Header.tsx) at all breakpoints with consistent `pt-16` content offset and the header is fixed with `md:left-[--sidebar-width]`/`z-40` so it aligns with and sits beneath the sidebar; [`Index.tsx`](src/features/feed/pages/Index.tsx) guest Landing view now uses `AppLayout` instead of its own fixed header; active nav styling in [`AppSidebar`](src/components/layout/AppSidebar.tsx) simplified to a 2px brand-primary left border; unit test added in [`AppSidebar.test.tsx`](src/components/layout/AppSidebar.test.tsx) to verify `SidebarTrigger` toggles `data-state`, and new Playwright spec [`tests/e2e/sidebar-header.spec.ts`](tests/e2e/sidebar-header.spec.ts) covers sidebar/header alignment and toggling on desktop including the Feed page. Vitest currently reports unrelated failures in `StatusBadge` and `KanbanColumn` tests whose expectations still reference pre-token styles; these pre-existing issues were not changed in this slice.
- **RR7 Phase 2 — Framework mode bootstrap:** `react-router.config.ts` confirms `ssr: true` with `appDirectory: "src"` and explicitly sets `routes: "app/routes.ts"`; the config-based route tree lives in `app/routes.ts` mirroring the existing `App.tsx` router, with explicit `id` values for routes that share a module (profiles, profile photos, building detail/edit/review variants, and grouped/not-found fallbacks) to satisfy RR7’s unique route ID requirement. A `src/routes.ts` shim re-exports the config for the plugin’s expected `src/routes.ts` location, `src/pages/AdminSystemPlaceholder.tsx` has been created for the admin system placeholder route, and `src/entry.client.tsx` / `src/entry.server.tsx` mirror the `app/` entry files so the framework can resolve client and server entries under the `src` app directory. `src/root.tsx` hosts the root loader that bootstraps the Supabase session for the client, and SSR now succeeds with `react-helmet-async` imported via default exports plus a minimal `localStorage` shim in `src/entry.server.tsx` to guard legacy browser-only code paths until Phase 3 replaces them with cookie-based auth. RR7-adjacent tests (`KanbanColumn` drag-over styles, `PopularityBadge` rank badges, `AllCaughtUpDivider`, `AppSidebar` sidebar toggle) have been brought in line with the new semantic styling. Full `npm run test` still fails due to older feature tests that assert pre-token Tailwind classes or brittle DOM shapes (see KNOWN_ISSUES); resolving those is explicitly deferred to a later “test alignment” task, and the Phase 2 roadmap checkpoint box for “tests pass with zero failures” remains intentionally unchecked.
- **RR7 Phase 1 (1.3–1.4) prep:** Verified `MetaHead` already uses a `SITE_URL` constant instead of `window.location.origin`; confirmed `vite.config.ts` VitePWA plugin is configured with `strategies: "generateSW"` and `devOptions: { enabled: false }`; ensured `.env.example` exists at the repo root with all required `VITE_*` keys and that `.env` is ignored in `.gitignore`. Roadmap checkboxes for these steps are marked complete; the remaining task to populate real values in Vercel’s environment dashboard is a manual hosting step and remains unchecked.
