## Current Phase
Phase 1 — (next) — Phase 0 integrity verified (Task 0.97).

## CURRENT_ARCHITECTURE_SNAPSHOT
Vite + React 18 SPA at repo root. Junk/debug files removed from root and loose SQL removed from `supabase/` (non-migrations). QA and verification markdown moved to `docs/archive/`. Legacy `_legacy_v1` map folder removed; collection map page lives under `components/collections/`, admin merge map under `components/admin/`.

## KNOWN_ISSUES
- `npm run typecheck` (`tsconfig.app.json`) fails with many TS errors (Supabase `Database` types vs app usage) — pre-existing.
- `npm run lint` reports hundreds of issues — pre-existing; Task 0.97 did not attempt a repo-wide lint cleanup.
- `npm run gen-types` will re-add `get_cine_sync_stats` to `src/integrations/supabase/types.ts` until that RPC is removed from the remote Supabase schema; Task 0.97 removed that entry so Phase 0 grep checks stay clean.

## Completed Tasks
- Task 0.1: Deleted verified junk files from repo root and `supabase/` root; moved 14 QA/historical markdown files to `docs/archive/`. `npm run build` succeeds.
- Task 0.2: Removed CineSync-era dead code: deleted `src/lib/constants.ts` (unused GENRE_MAP), `src/VerificationWrapper.tsx`, `src/types/cine-sync.ts`; moved `MutualAffinityUser` to `useProfileComparison.ts`; dropped `subscribed_platforms` from profile select/`UserProfile`. `npx tsc --noEmit` passes.
- Task 0.3: Removed `src/components/_legacy_v1/`: moved `CollectionMap.tsx` → `src/components/collections/CollectionMapPage.tsx` (App import updated); moved `BuildingMap.tsx` → `src/components/admin/BuildingMap.tsx` (MergeComparison import updated); deleted eight unused legacy components. `npx tsc --noEmit` passes.
- Task 0.4: Realigned `.cursor/rules/00-architecture.mdc`, `02-api.mdc`, `03-frontend.mdc`, and `05-vertical-slice.mdc` with the Vite + React Router + TanStack Query + Supabase SPA (removed Next.js/Turborepo/Vercel-runtime guidance; TanStack Query caching + `invalidateQueries`; build commands `npm run build` / `typecheck` / `lint`).
- Task 0.5: Removed `console.log` / `console.error` / `console.warn` from `src/hooks/` and `src/features/*/hooks/`; `useLoginTracker` failures use `logDiagnosticError`; PWA SW callbacks are no-ops. `npx tsc --noEmit` (root) passes.
- Task 0.97: `npx tsc --noEmit`, `npm run build`, `npm run test` (`vitest run`), and Phase 0 grep suite pass. Fixed `Profile.regression.test.tsx` (stable `useAuth` user reference). Stripped `console.*` from remaining `src/features/**` production files in the Step 5 grep scope; removed unused `get_cine_sync_stats` from `types.ts`. Added `"test": "vitest run"` to `package.json`.
