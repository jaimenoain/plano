## Current Phase

### Feed Rebuild — In Progress
**Phase 2 shipped (2026-05-13).** Merge three feed sources into one ranked stream; cold-start page fork retired behind the flag. New migration `supabase/migrations/20270911000001_add_ring_to_feed_sources.sql` — adds `ring text` column to `get_collections_feed` (`'direct'`) and `get_suggested_posts` (`'open'`). New `src/features/feed/api/getCollectionsFeedAsItems.ts` — calls `get_collections_feed` RPC and maps rows to `FeedItem[]` with `ring='direct'`, freshness-scored (72h half-life). New `src/features/feed/api/getSuggestedPostsAsItems.ts` — calls `get_suggested_posts` and maps rows to `FeedItem[]` with `ring='open'`; applies the same freshness × engagement-velocity × media-quality formula as `get_feed_ranked` SQL. Added `FeedItemPrompt` variant to `src/types/feedItem.ts` (union now `FeedItemPost | FeedItemCollection | FeedItemPrompt`). Updated `src/features/feed/utils/scoreFeedItem.ts` — ring multiplier applied before diversity/seen penalties (`direct: 3.0`, `extended: 1.5`, `open: 1.0`, `editorial: 2.0`). New `src/features/feed/utils/mergeFeedSources.ts` — pure function combining all three sources via `scoreFeedItem`. 5 new Vitest tests in `mergeFeedSources.test.ts` (ring distribution, no-dedup, empty social, seen penalty, empty-all) all green. `Index.tsx` gains three `useQuery` hooks for the v2 path (`v2SocialQuery`, `v2CollectionsQuery`, `v2DiscoveryQuery`) that run in parallel when `isFeedV2RankerEnabled()`; merged items rendered by `buildV2FeedNodes` which handles `post`, `collection`, and `prompt` cards; `injectPromptCards` injects `PeopleYouMayKnow` slots every 7 cards for users with < 5 follows. Flag-off path (`ColdStartFeed` fork) is untouched. **Phase 3 (mosaic surface) is next.** **Action required: run migration `20270911000001_add_ring_to_feed_sources.sql` on Supabase before testing the v2 path.**

**Phase 1 shipped (2026-05-13).** The ranker. New migration `supabase/migrations/20270911000000_get_feed_ranked.sql` — `get_feed_ranked(p_limit, p_offset)` RPC: same source set and visibility rules as `get_feed`, scores posts with freshness-decay × engagement-velocity × media-quality formula, returns extra columns `score`, `ring` (always `'direct'` in Phase 1), `freshness_hours`. Candidates capped at 500 most-recent before scoring to keep the pass tractable; offset pagination documented over keyset for simplicity. New `src/features/feed/api/getFeedRanked.ts` — TS wrapper that calls the RPC and maps rows to `FeedItem[]` with `ring='direct'` and attribution `${author} · ${relativeTime}`. New `src/features/feed/utils/scoreFeedItem.ts` — pure function applying author diversity penalty (0.6^N per subsequent item by the same username), building diversity penalty (0.6^N per subsequent item for the same building), and seen penalty (×0.3) via the `hasSeen` callback from `useSeenItems`; re-sorts the array by adjusted score. 7 Vitest unit tests (diversity, seen, mutation-safety, architectural regression) all green. New `src/features/feed/flags.ts` — `isFeedV2RankerEnabled()` reads `localStorage.getItem('feed_v2_ranker') === 'true'`; toggle in browser console, no env var needed. `useFeed` updated to call `getFeedRanked` + `scoreFeedItem` when the flag is on, returning `FeedReview[]` (same shape as before) so all existing consumers are unchanged; existing `get_feed` path is untouched when the flag is off. **Phase 2 is next** (merge the three sources + retire cold-start fork). **Action required: run migration `20270911000000_get_feed_ranked.sql` on Supabase before testing the flag-on path.**

**Phase 0 shipped (2026-05-13).** Foundations & inventory — no user-visible change. New `docs/archive/feed_inventory.md` lists every feed RPC, hook, component, type, and util with per-phase touch labels and explicit dead-code entries (`BucketListWidget`, `DetailCard`, `DetailSectionHeader`, `ExploreByStyle`, `FeedHero` — flagged for post-rebuild cleanup, not deleted). New `<CardAttribution>` primitive at `src/features/feed/components/card-parts/CardAttribution.tsx` (kinds: `direct | extended | open | editorial`); existing `CardAuthor` carries `data-attribution-kind="direct"` so every ring-1 card now renders the contract without a second line (visual diff: none). New umbrella type `FeedItem` at `src/types/feedItem.ts` — discriminated union over the two source variants in the current feed (`post` wrapping `FeedReview`, `collection` wrapping `FeedCollection`); each variant carries `ring`, `score` (default 0 until Phase 1), and `attribution`. Existing `src/types/feed.ts` is untouched and re-imported as the payload type. `Index.tsx` imports `FeedItem` as a passthrough. New `useSeenItems` hook (`src/features/feed/hooks/useSeenItems.ts`) wraps `track_note_views` via the existing `noteViewTracker` batcher plus a session `Set<string>` exposing `markSeen` / `hasSeen` / `flush`; `useTrackNoteView` keeps its IntersectionObserver dwell role. 4 new Vitest cases (`useSeenItems.test.tsx`) green; 8 feed-tracker tests still green; 0 regressions introduced. Source guidance: [docs/FEED_REDESIGN_BRIEF.md](docs/FEED_REDESIGN_BRIEF.md), [docs/ROADMAP.md](docs/ROADMAP.md).

**Search Rebuild Phase 4 shipped (2026-05-09).** Smart filter chips. New `src/features/search/utils/smartFilterSuggestions.ts` (pure ranking utility — taxonomy-name match against query with stem-similarity fallback for "brutalism" ↔ "Brutalist"; people / companies pulled from the search results' top hit gated by name match + creditCount > 0; up to 3 chips, sorted by score) and `src/features/search/components/SmartFilterSuggestions.tsx`. Decision: **client-side** (Option B) — `useTaxonomy` already caches categories / typologies / attributes for 1h; no new RPC, no migration. Counts are shown only when known cheaply (people / companies via `creditCount`); taxonomy chips render label-only by design — no fake numbers. Chips render above building results in `BuildingSidebar` (Find mode only, query ≥ 3 chars). Click → applies filter and clears the search input via `onApplied` callback. **Architectural note:** `useBuildingSearch` (mounted in `FilterDrawer`) is the canonical owner of filter URL state. Writing URL params from `MapContext.setMapState` directly loses to `useBuildingSearch`'s sync effect, which clears any param it doesn't know about from its local state when its `setSearchParams` ref churns. Solution: chip dispatches a `plano:apply-smart-filter` window CustomEvent; `useBuildingSearch` listens and routes the change through its own setters. 10 new Phase 4 tests green (`smartFilterSuggestions.test.ts`); `BuildingSidebar.test.tsx` + `useBuildingSearch.test.tsx` still green; 0 regressions. **Roadmap complete** — no further phases. **Action required:** none — no migration, ready to deploy.

**Prior — Search Rebuild Phase 3 shipped (2026-05-09).** Migration `20270905000000_get_map_clusters_v3.sql`: new Browse-only `get_map_clusters_v3` RPC. **No silent exclusions** — drops the v2 hardcoded `Demolished | Lost | Under Construction | Unbuilt` default exclusion and the `popularity_score >= -50` floor; construction-status filtering is now caller-driven. **No `query` text-search code path** — Find mode owns text search via `search_buildings_v2`. Same return shape as v2 so MapMarkers / pin styles need no changes. v2/v1 left in place for safe rollback. `useMapData` rewired to v3: stable scalar `filterKey` (replaces 30+-dep object queryKey that queued requests during fast pans), AbortController via `.abortSignal()` cancels in-flight RPC calls on viewport / filter change. New `MapFilters.showDemolished` + URL param (`showDemolished=true`); when on, `Demolished` and `Lost` are appended to the default `['Built','Temporary']` status set; explicit `constructionStatuses` picks override the toggle. **Show demolished** toggle added to the Building status accordion in `DiscoveryFiltersPanel` (URL-persisted, default off — preserves today's "non-built buildings stay hidden by default" behaviour). FilterDrawer status checklist now includes the full enum (Built, Under Construction, Temporary, Unbuilt, Demolished, Lost). 8 new Phase 3 tests green (`useMapData_v3.test.tsx`, `useURLMapState.phase3.test.tsx`); existing useMapData tests migrated to v3. Net test-suite delta: −6 failures, +3 passing, 0 regressions. **Phase 4 is next** (smart filter chips). **Action required:** run `20270905000000_get_map_clusters_v3.sql` on Supabase before deploying Phase 3.

**Prior — Search Rebuild Phase 2 shipped (2026-05-09).** Migration `20270904000000_search_people_companies_v2.sql`: adds `search_vector tsvector` + GIN index to `people` and `companies` (triggers `update_person_search_vector` / `update_company_search_vector`), GIN trigram indexes on `people.name` and `companies.name` (previously missing — cause of sequential-scan slowness). Creates `search_people_v2` and `search_companies_v2` RPCs (same hybrid-scoring pattern as `search_buildings_v2`). TS wrappers at `src/features/search/api/searchPeopleV2.ts` and `searchCompaniesV2.ts`. `searchPeople` / `searchCompanies` in `people.ts` / `companies.ts` replaced with thin wrappers — 3-round-trip sequential-scan pattern eliminated. `useUnifiedSearch` hook (`src/features/search/hooks/useUnifiedSearch.ts`) fires all three RPCs in parallel with AbortController. `MapContext` extended with `findModeBuildings` state and `setFindModeBuildings` method. `PlanoMap` uses `findModeBuildings` as clusters in Find mode (no `useMapData` RPC call). `BuildingSidebar` accepts `findModeBuildings` prop; when set, bypasses `get_buildings_list` and renders find-mode hits. `SearchPage` introduces Find/Browse dispatch: query ≥ 2 chars → Find mode via `useUnifiedSearch` + auto-fit map to results; no query → Browse mode unchanged. 16 tests green; 0 regressions. **Phase 3 is next** (slim Browse mode, get_map_clusters_v3, Show demolished toggle, map freeze fix). **Action required:** run `20270904000000_search_people_companies_v2.sql` on Supabase before starting Phase 3.

**Prior — Search Rebuild Phase 1 shipped (2026-05-09).** Migration `20270903000000_search_buildings_v2_foundation.sql`: adds `search_vector tsvector` column to `buildings` with trigger (`update_building_search_vector`), GIN index, backfill, and GIN trigram index on `alt_name`. Creates `search_buildings_v2(p_query, p_limit, p_offset, p_filters)` RPC — viewport-independent, `'simple'`-dictionary tsvector + trigram similarity scoring, no silent status exclusions, `building_credits`-based credit names, `SECURITY DEFINER`. TS wrapper at `src/features/search/api/searchBuildingsV2.ts`. 21 tests green.

**Prior — Phase Q11 — Cleanup & regression QA.** **QA 11.6** — Documentation audit: **`docs/ROADMAP.md`** **Status: Shipped** + QA 11.6 **[x]**; **`docs/PRD.md`** search/discovery §9 + map filter table + RPC appendix aligned to **`people`** / **`companies`** / **`building_credits`** (no stale **`architects`** table product requirements); QA 11.5 settings copy updated to **`DisconnectLegacyClaimDialog`**. **Prior — QA 11.5** — **`AppSidebar.test.tsx`**: **My portfolio** links only when **`useClaimedPersonForNav`** returns data; **`tests/unit/profile-nav-qa115.test.ts`**: **`Settings`** → **`DisconnectLegacyClaimDialog`**; **`Profile.tsx`** **Professional profile** copy. **Manual UAT:** claimed vs unclaimed user pages, portfolio tab + sidebar, multi-account nav cache, settings legacy-claim UI. **QA 11.4** — **`tests/unit/building-detail-pages-qa114.test.ts`**: **`BuildingDetails`** / **`BuildingForm`** / **`BuildingHeader`** / **`BuildingAttributes`** sources — no legacy architect table/query patterns or **`ArchitectSelect`**; **`BuildingDetails`** wires **`BuildingCredits`**; **`BuildingForm`** uses **`CreditedEntitiesSelect`**. Vitest: **`BuildingDetails.test`**, **`BuildingHeader.test`**, **`BuildingAttributes.test`**, **`BuildingForm.test`** green. **Manual UAT:** live building with migrated credits, empty credits CTA, add three credit shapes, logged-out flag + admin hide, no console errors. **QA 11.3** — **`tests/unit/seo-sitemap-qa113.test.ts`**: sitemap Edge source **`/person/`** + **`/company/`** locs, no **`/architect/`**; **`public/robots.txt`** does not disallow person/company; **`docs/LAUNCH_HOSTING.md`** legacy architect + **301**; **`vercel.json`** → Supabase **`sitemap`**; **`personPageStructuredData`** / **`companyPageStructuredData`**; **`PersonDetails`** / **`CompanyDetails`** **`meta()`** description + canonical + JSON-LD. **Manual UAT:** live **`/sitemap.xml`**, Google Rich Results on deployed person/company URLs, production **`<head>`** spot-check. **QA 11.2** — **`tests/unit/deprecated-artifacts-qa112.test.ts`**: zero legacy table / deprecated component / **`ArchitectDetails`** / stray **`/architect/`** (lines without **`redirect`** / **`LAUNCH_HOSTING`**) / **`@deprecated`** under **`src/`** + **`app/`**; **`OmniSearchBar`** header de-deprecated; notifications + loader tests + **`BuildingHeader`** assertions avoid literal legacy path/table substrings in source. **Manual UAT:** migration log (dropped **`architects`** / **`building_architects`**), **`tsc --noEmit`**, pages smoke per roadmap. **QA 11.1** — **`tests/unit/routes-canonical-qa111.test.ts`**: required routes live in **`app/routes.ts`**; legacy **`/architect/:id`** → **`ArchitectIdRedirect`**; **`src/routes.ts`** is re-export only (no **`route(`**). **Manual UAT:** browser smoke on token + person/company paths + architect UUID redirect. **Phase Q10 — Search & discovery QA.** **QA 10.2** — **`useURLMapState.qa102.test.tsx`**: **`creditCompany`** / **`creditRoles`** URL hydration + empty default; **`useMapData.test.tsx`**: RPC omits credit keys when unset (existing combined company+roles case retained); **`FilterDrawer.test.tsx`** QA block: **Credits** + **Company** / **Role** + **`CompanyMapFilterSelect`** stub + role labels. **Manual UAT:** live map/list + **`building_matches_credit_filters`** migration on Supabase. **QA 10.1** — **`no-architect-select-files-qa101.test.ts`**: zero **`ArchitectSelect*`** under **`src/`** / **`app/`**; no **`FeaturedArchitect`** module. **`BuildingSidebar.qa101.test.tsx`**: **People** / **Companies** tabs show name + credits + nationality or country. **`DiscoverySearchInput.qa101.test.tsx`**: mixed mode surfaces **Buildings** / **People** / **Companies** (`search_buildings`, **`searchPeople`**, **`searchCompanies`**); source scan excludes **`architects`** / **`building_architects`**. **`ArchitectSearchNudge.qa101.test.tsx`**: render + source scan. **Manual UAT:** live `/search` + Network (no **`architects`** reads). **Phase Q9 — Portfolio dashboards QA.** **QA 9.2** — **`CompanyDashboard.qa92.test.tsx`**: credits grouped by **Credits by role**; **Approve** → **`approveCompanyStewardRequestById`** + **`notifyStewardRequestApprovedWithClient`**; pending queue **owner-only** (non-owner → no **`listPendingStewardRequestsForCompany`**); role **Select** refetches **`getCompanyPortfolio(…, role)`**; multi-company **Select** switches **`companyId`**. **`AppSidebar.qa92.test.tsx`**: one steward → company name → **`/company-portfolio`**; many → **My companies** + **`?company=`** links. **Manual UAT:** live approve/decline RPC, non-steward redirect. **QA 9.1** — **`PersonDashboard.qa91.test.tsx`**: stat cards vs distinct **`building_id`** / roles / year span; **Year** vs **Role** sort within **Primary credits**; **`PersonCreditCard`** role badge + company **`href`**. **`AppSidebar.qa91.test.tsx`**: **My portfolio** → **`/portfolio`** (nav + menu). **`Profile.qa91.test.tsx`**: **`?section=portfolio`** — **Open portfolio dashboard** only (no **`Primary credits`** heading); no **Portfolio** tab when **`getClaimedPersonSummaryForProfile`** is null despite **`profiles.verified_architect_id`**. **Manual UAT:** live claimed user sidebar + DB **`building_credits`** count vs dashboard stats. **Phase Q8 — Admin panel QA.** **QA 8.3** — **`entity-audit-log-insert-qa83.test.ts`**: **`insertEntityAuditLog`** payloads **`credit_added`** / **`credit_status_changed`** (`old_value` / `new_value`); client Zod rejects **`person_claimed`**; enum lists RPC type. **`admin-audit-logs-schema-qa83.test.ts`**: generated **`admin_audit_logs` Insert** keys unchanged (credit context in **`details`** only). **`BuildingAudit.qa83.test.tsx`**: **`?building=`** merges **`building_audit_logs`** with filtered **`credit_added`** / **`credit_status_changed`** rows; wrong-**`building_id`** admin rows excluded. **Manual UAT:** Supabase rows after real add credit / flag / **`claim_person`**. **QA 8.2** — **`AdminPeople.test.tsx`**: directory **`searchAdminPeople`**, claim **Verified** → **`updateAdminPersonClaimStatus`**, **Confirm merge** → **`adminMergePeople(sourceId, targetId)`**. **`AdminCompanies.test.tsx`**: directory **`searchAdminCompanies`**, **View (n)** expands → **`getCompanyStewardsWithProfiles`**, steward usernames; claim **Verified** → **`updateAdminCompanyClaimStatus`**. **`EntityClaims.test.tsx`**: **Legacy architect claims** + **Company disputes** tabs; **Resolved** → **`resolveCompanyClaimDispute`**. **`admin-no-architect-claims-route.test.ts`** + **`AdminSidebar.qa82.test.tsx`**: **`/admin/claims`** → **`EntityClaims`**, no **`ArchitectClaims`**. **Manual UAT:** live **`admin_merge_*`** RPCs, **`claim_status`** on public person/company pages, dispute row **`resolved`** in DB. **QA 8.1** — **`FlaggedCredits.test.tsx`**: queue row shows reason, notes, flagged time, building / person / company links, **@** added-by or raw UUID; **Dismiss** → **`updateCreditStatus(…, active)`** without **`notifyCreditOutcome`**; **Verify** / **Hide** → status + **`notifyCreditOutcome`** when **`addedByUserId`**; verify with no submitter skips notify; verified-origin flag → **Previously verified** + **No auto-hide**; auto-hide column uses policy **title** + countdown copy (exact window in **`flagged-credit-moderation.test.ts`**); empty / error / notify-failure toasts. **Manual UAT:** live admin RLS, building page visibility after dismiss/verify/hide, Resend **`notify-credit-outcome`**. **Phase Q7 — Claiming flows QA (complete).** **QA 7.4** — **`CompanyDetails.qa74.test.tsx`**: claimed non-steward → **Dispute this claim** + credits/name unchanged; open dispute id → **Dispute under review** (no dispute link); other user mock → no review notice; logged-out → **Log in to dispute** (no review). **`CompanyClaimDispute.loader.test.ts`**: empty slug / missing company → **404**; unclaimed or steward → **302** to **`/company/:slug`**; claimed non-steward (signed in or out) → loader data. **`CompanyClaimDispute.test.tsx`**: log-in CTA; submit → **`submitCompanyClaimDispute("co1", { reason, evidenceUrl })`** + toast + **`navigate(…?disputeSubmitted=1)`**. **Manual UAT:** DB **`company_claim_disputes`**, **`notify-admin-dispute`** logs. **QA 7.3** — **`CompanyDetails.qa73.test.tsx`**: claimed company, non-steward → **Request access to manage this company** (no **Claim this company**); dialog → **`submitCompanyStewardRequest("co1", message)`** + **Request sent** toast; pending **`getMyPendingCompanyStewardRequestId`** → **Request pending** copy (no CTA). **`ApproveStewardRequest.loader.test.ts`**: bad token → **`invalid_format`**; no session → **`needs_auth`**; approve ok → **302** **`?stewardApproved=1`** + **`notifyStewardRequestApprovedWithClient`**; **`alreadyProcessed`** same redirect + notify; **`expired`** → error, no notify. **Manual UAT:** owner emails, DB row + idempotent **`company_stewards`** count, **`notify-steward-request`** / **`notify-steward-request-approved`** logs. **QA 7.2** — **`CompanyDetails.qa72.test.tsx`**: **Claim this company** dialog → **`requestCompanyClaimVerification("co1", email)`** + **Check your email** toast; **`action: "dispute"`** → **`navigate("/company/…/dispute")`**. **`VerifyCompanyClaim.loader.test.ts`**: bad token → **`invalid_format`** (no Supabase); no session → **`needs_auth`** + **`returnPath`**; redeem ok → thrown **302** **`/company/:slug?claimVerified=1`**; **`expired`** → error payload. **`VerifyCompanyClaim.test.tsx`**: invalid / needs_auth / expired UI. **Manual UAT:** **`verify-company-claim`** logs, DB **`company_stewards`** / **`claim_status`** after real redeem. **QA 7.1** — **`PersonDetails.qa71.test.tsx`**: logged-in **Claim this profile** → dialog **Confirm claim** with **`self`** → **`claimPerson`** args; banner cleared + **Edit**; logged-out **Log in to claim** `redirect` query (no dialog); claimed-by-other → no claim / no edit. **`tests/unit/no-claim-profile-dialog.test.ts`**: walk **`src/`** + **`app/`** — zero **`ClaimProfileDialog`**. **Manual UAT:** Supabase **`notify-entity-claimed`** logs. **Phase Q6 — Credit submission form QA** (prior): **QA 6.4** — **`RemoveCredit.loader.test.ts`** (invalid token → **`invalid_format`**, no RPC; success → **`buildingHref`** with/without slug; **`already_used`** / **`expired`** / **`unknown_token`**); **`RemoveCredit.test.tsx`** (success / invalid / error copy + **View building** link). **`ScrollRestoration`** mocked (needs data router in unit tests). **Manual UAT still:** DB **`building_credits.status`**, **`credit_removal_tokens.used_at`**, building page credit hidden, logged-out browser smoke. **QA 6.3** / **6.2** / **6.1** prior. **Phase Q5 — Building credits QA** (prior): **QA 5.1** — **`building-credit-display-qa.test.ts`**, **`search-buildings-rpc-qa.test.ts`**, **`BuildingHeader.test.tsx`**, **`ArchitectStatement.test.tsx`**. **QA 5.2** — **`BuildingCredits`** **`isAdmin`** + tests. **QA 5.3** — **`CreditFlagTrigger`**: no default reason — submit without selection → destructive toast, no **`flagCredit`**; **`BuildingCredits.test.tsx`** (popover / mobile sheet, **`wrong_role`** + null notes, verified → **`flagged`** mock, session clear restores report); **`credits-api-qa.test.ts`** — **`flaggedByUserId`** on post-RPC row (signed-in behaviour). **Phase Q4 — Company pages QA** (prior): **QA 4.1–4.2**. **Phase Q3 — People pages QA** (prior): **QA 3.1–3.3**. **Phase Q2 API QA** (prior): **`people-api-qa`**, **`companies-api-qa`**, **`credits-api-qa`**.

**Phase 11 — Legacy architect removal.** Tasks **11.1**, **11.2**, and **11.3** checked in **`docs/ROADMAP.md`**. **11.3** shipped: **`docs/DATA_CONTRACT.md`** — building domain credits pointer, no references to removed catalog tables **`architects`** / **`building_architects`**; §9a–§9d migration prose neutralized; §9 retitled; RPC/registry comments cleaned. **`docs/PRD.md`** — FR-4.1.8, FR-4.2.2, FR-4.2.6, FR-4.3.1–4.3.2, FR-4.3.5 aligned to **`building_credits`** / **`CreditEntityPicker`**. **`docs/SECURITY.md`** — opaque token pattern (credit removal, company claim verification, related flows). **`docs/ROADMAP.md`** — **Building Credits (v2)** marked shipped; Task 11.3 **[x]**. **`npm run gen-types`** was run against hosted **`lnqxtomyucnnrgeapnzt`**; app code aligned to live schema: **`buildings.hero_image_url`** (not **`main_image_url`**), **`profiles`** without **`first_name`/`last_name`**, **`review_images`** without **`building_id`**, **`merge_buildings(source_id, target_id)`**, stricter RPC optional args (**`get_discovery_feed`**, **`get_map_pins`**, **`flag_building_credit.p_notes`**, **`check_slug_availability.exclude_id`**). **Prior — Phase 10 — Search integration in progress.** Task 10.2 shipped: **Credits** filter in **`FilterDrawer`** — **`CompanyMapFilterSelect`** (`searchCompanies`) + role multi-select from **`CREDIT_ROLES`**; URL **`creditCompany`** / **`creditRoles`**; **`useBuildingSearch`** + **`useURLMapState`** / **`MapFilters`**; **`useMapData`** + **`BuildingSidebar`** pass **`credit_company_id`** / **`credit_roles`** into **`get_map_clusters_v2`** / **`get_buildings_list`**; migration **`20270836000000_map_credit_filters.sql`** adds **`building_matches_credit_filters`** and extends **`get_map_clusters`** / **`get_map_clusters_v2`** / **`get_buildings_list`**; library-mode map pins intersect via **`building_credits`** query; **`DATA_CONTRACT`** map RPC note; Roadmap 10.2 checked. **Prior — Task 10.1 shipped:** global map search uses **`useGlobalEntitySearch`** (`searchPeople` / `searchCompanies`); **`BuildingSidebar`** tabs **Buildings | People (n) | Companies (n)** with avatars/logos via **`getStorageAssetUrl`**; **`SearchPage`** mobile **`DiscoverySearchInput`** mixed autocomplete (Places + buildings + people + companies with icons); **`PersonFilterSelect`** replaces **`ArchitectSelect`** in **`FilterDrawer`** (URL `architects` filter unchanged — person UUIDs for individuals); **`ArchitectSearchNudge`** uses **`people`** + `/person/:slug`; removed **`useArchitectSearch`**, **`ArchitectSelect.tsx`**, **`ArchitectResultsList.tsx`**; **`OmniSearchBar`** parity; **`DATA_CONTRACT`** `searchPeople` row + **`ROADMAP`** 10.1 checked. **Prior — Phase 9 complete.** Task 9.2 shipped: **`CompanyDashboard.tsx`** at **`/company-portfolio`** (`?company=` slug when steward of multiple); credits **grouped by role** with **role filter** (`getCompanyPortfolio`); **owners** see **Pending access requests** with **Approve** / **Decline** → RPCs **`approve_company_steward_request_by_id`** / **`reject_company_steward_request_by_id`** (migration **`20270835000000_steward_request_owner_dashboard_rpcs.sql`**) + **`notify-steward-request-approved`** on new approval; **Manage company page** → **`/company/:slug?edit=1`** opens edit sheet on **`CompanyDetails`**; **`useStewardCompaniesForNav`** + sidebar **company name** (single) or **My companies** dropdown + user menu **Company portfolio**; **`App.tsx`**, **`app/routes.ts`**, **`companies.ts`** helpers + **`DATA_CONTRACT`** §9b; Roadmap 9.2 checked. **Prior — Task 9.1 shipped:** **`PersonDashboard.tsx`** at **`/portfolio`** (stats: buildings credited, distinct roles, year span; tier-grouped **`PersonCreditCard`** grid; sort by year or role); **`useClaimedPersonForNav`** + sidebar **My portfolio** + user menu link (only when `people.claimed_by_user_id` matches user); **`ArchitectDashboard.tsx`** redirects to **`/portfolio`**; **`Profile.tsx`** portfolio tab driven by **`getClaimedPersonSummaryForProfile`** (any profile with a linked claimed person); own profile links to **`/portfolio`**, others to **`/person/:slug`**; **`App.tsx`** + **`app/routes.ts`**; Roadmap 9.1 checked. **Prior — Phase 8 — Admin extensions in progress.** Task 8.3 shipped: **`admin_audit_logs`** entity/credit events — migration **`20270834000000_entity_audit_logs.sql`** (RLS `entity_audit_logs_actor_insert`; RPC audit in **`claim_person`**, **`redeem_company_claim_token`**, **`approve_company_steward_request`**, **`redeem_company_steward_invite`**); **`insertEntityAuditLog`** + **`addBuildingCredit`** / **`updateCreditStatus`** / **`removeCompanySteward`**; **`BuildingAudit`** `?building=` UUID filter merges **`building_audit_logs`** + credit rows from **`admin_audit_logs`**; **`DATA_CONTRACT`** §11; Roadmap 8.3 checked. **Prior — Task 8.2 shipped:** **`AdminPeople.tsx`** / **`AdminCompanies.tsx`** (`/admin/credits/people`, `/admin/credits/companies`) — search (≥2 chars), inline **`claim_status`**, side-by-side merge (patterned on **`MergeBuildings`**); RPCs **`admin_merge_people`** / **`admin_merge_companies`** (migration **`20270833000000_admin_merge_people_companies.sql`**); **`entity-management.ts`**; **`EntityClaims.tsx`** replaces **`ArchitectClaims`** at **`/admin/claims`** — tabs: legacy **`architect_claims`** + open **`company_claim_disputes`** with **Resolved**; Credits sidebar: Flagged, Entity claims, People, Companies (removed duplicate **Pending Claims** from Management). **`DATA_CONTRACT`** §11 Component 5 + admin RPC table; **`plano-tables.types`**; **`App.tsx`** + **`app/routes.ts`**. Roadmap Task 8.2 checked. **Prior — Task 8.1 shipped:** **Credits** sidebar + **`/admin/credits/flagged`**; migration **`20270832000000_building_credits_flagged_from_status.sql`**; **`notify-credit-outcome`**; Roadmap 8.1 checked.

**Phase 7 — Claiming flows (complete).** Task 7.4 shipped: **`company_claim_disputes`** (migration **`20270831000000_company_claim_disputes.sql`**); Edge **`notify-admin-dispute`** (`PLANO_ADMIN_NOTIFY_EMAIL` / `COMPANY_CLAIM_DISPUTE_NOTIFY_EMAIL` optional, else `hello@plano.app`); **`submitCompanyClaimDispute`** / **`getMyOpenCompanyClaimDisputeId`** / **`SubmitCompanyClaimDisputeSchema`** in **`companies.ts`**; **`CompanyClaimDispute`** form + loader redirects (unclaimed / stewards); **`CompanyDetails`** **Dispute this claim** + **Dispute under review** (submitter-only via RLS-scoped query); Vitest **`company-claim-dispute-input.test.ts`**; **`DATA_CONTRACT`** §9b Component 3d + API rows; Roadmap 7.4 checked. **Prior — Task 7.3 shipped:** **`company_steward_requests`** + **`company_steward_request_approval_tokens`** (migration **`20270830000000_company_steward_requests.sql`**); RPC **`approve_company_steward_request`**; Edge **`notify-steward-request`** (per-owner approval links) + **`notify-steward-request-approved`** (requester email); **`/approve-steward-request/:token`** loader + **`ApproveStewardRequest`**; **`CompanyDetails`** banner **Request access to manage this company** / **Log in to request access** (claimed, non-steward); **`RequestStewardAccessDialog`**; **`companies.ts`** helpers + Vitest **`approve-company-steward-request.test.ts`**; **`DATA_CONTRACT`** §9b Component 3c + API rows. **Prior — Task 7.2 shipped:** **`ClaimCompanyDialog`** on unclaimed **`CompanyDetails`** (work email → **`verify-company-claim`** Edge Function, 7-day token in **`company_claim_verification_tokens`**); **`/verify-company-claim/:token`** loader + **`redeem_company_claim_token`** RPC (migration **`20270829000000_company_claim_verification_tokens.sql`**); success **`redirect`** to **`/company/:slug?claimVerified=1`** + toast + **Edit** sheet; domain mismatch on already-claimed companies → **`/company/:slug/dispute`** (**`CompanyClaimDispute`**, Task 7.4); **`requestCompanyClaimVerification`** / **`redeemCompanyClaimTokenWithClient`** / **`parseRedeemCompanyClaimRpcPayload`** in **`companies.ts`**; Vitest **`company-claim-redeem.test.ts`**. **`DATA_CONTRACT`** §9b Component 3b + API rows. **Prior — Task 7.1 shipped:** **`ClaimPersonDialog`** on **`PersonDetails`** (unclaimed banner → **Claim this profile** / **Log in to claim** with `redirect`); **`claimPerson`** + **`notifyEntityClaimed`** in **`people.ts`**; RPC **`claim_person`** migration **`20270828000000_claim_person_rpc.sql`**; Edge Function **`notify-entity-claimed`** + **`EntityClaimedEmail`**; post-claim opens **Edit** sheet. **`DATA_CONTRACT`** §9a Components 3–5. **Prior — Building Credits v2 Phase 6:** Task 6.4 shipped: standalone **`/remove-credit/:token`** (`RemoveCredit.tsx` + loader, no `MainLayout`), SSR **`removeCreditByTokenWithClient`**; migration **`20270827000000_redeem_credit_removal_token_building_payload.sql`** extends RPC success JSON with **`building_id` / `building_name` / `building_slug`** for thank-you CTA (hidden credits are not anon-readable). **`app/routes.ts`** + **`App.tsx`** (loader import). **`DATA_CONTRACT`** §9d/§9e. **Prior — Task 6.3 shipped:** post-submit **notify** step in **`AddCreditForm`** — paste emails (comma/newline), dedupe + cap **15**, pills with remove, **Send notifications** → **`notifyCreditedEntities`** (`supabase.functions.invoke('notify-credited-entities')`); **`parseCreditNotifyEmails`** (`src/lib/parse-credit-notify-emails.ts`); Edge Function **`notify-credited-entities`** + **`CreditNotificationEmail`** template; per-credit **`generate_credit_removal_token`**, Resend, **`credit_notification_log`** hashes only; removal links **`/remove-credit/:token`** (Task 6.4 route). Vitest **`parse-credit-notify-emails.test.ts`** + **`AddCreditForm`** notify test. **`DATA_CONTRACT`** §9d + §9e Component 6. **Prior — Task 6.2:** **`AddCreditForm`** multi-row sheet; **`CreditEntityPicker`**; sequential **`addBuildingCredit`**; **Continue** → notify step; Zod notes **≤500**, **`other`** requires **`roleCustom`**. **`DATA_CONTRACT`** §9d `addBuildingCredit` row. **Prior — Task 6.1:** **`CreditEntityPicker`** (`src/features/credits/components/CreditEntityPicker.tsx`) — debounced parallel **`searchPeople`** / **`searchCompanies`**, merged list with Person/Company badges + disambiguation (affiliations / known building; country + **`creditCount`**); **Create new person/company** with name-only mini-form; pre-create fresh search + **trigram-style similarity** (`src/lib/trigram-similarity.ts`, threshold **0.4**) → **Did you mean** + dismiss before create; Vitest **`CreditEntityPicker.test.tsx`** + **`tests/unit/trigram-similarity.test.ts`**; **`searchCompanies`** now fills **`CompanySummary.creditCount`** (parallel head counts). **`DATA_CONTRACT`** §9b `searchCompanies` row updated. **Prior — Phase 5:** Task 5.3 shipped: per-credit **Report** control (`Flag` icon) in `BuildingCredits`; **Popover** (desktop) / **bottom Sheet** (mobile) with reason radios + optional notes; **`flagCredit`** calls RPC **`flag_building_credit`** (anon OK, `flagged_by_user_id` null when signed out); toast **Credit reported — we'll review it**; **`plano-reported-credit-ids`** in **sessionStorage** hides the control after submit until the tab session ends; **`BuildingCredits.test.tsx`** + `cleanup()`. Migration **`20270826000000_flag_building_credit_rpc.sql`**. **Prior — Task 5.2:** **`BuildingCredits`** on `BuildingDetails` (below About): tiers **primary** / **contributor** / **ancillary** with role sub-groups, **Lead** label, entity links + optional **@** company, role / years / notes / `project_url` icon, **verified** tooltip; ancillary behind **Show all credits**; authenticated **Add a credit** opens a **Sheet** stub for Phase 6 Task 6.2; `BuildingCredits.test.tsx`. **Prior — Task 5.1:** `building_credits` in header + loader/query + search RPC + `PrimaryCreditsLinks` / `buildingCreditDisplay` / structured data. **Prior — Phase 4:** Task 4.2 shipped: stewards see **Edit** + `EditCompanyForm` (bio, country, years, logo via `avatars`, website); **Stewards** section (stewards only) with profile avatars; owners **Invite** (`invite-company-steward` Edge Function + `company_steward_invites`) and **Remove** on steward rows; `redeem_company_steward_invite` RPC; `/accept-company-steward` + `Auth` optional `redirect` query; verified `BadgeCheck` when `claim_status = verified`; stricter `company_stewards` DELETE RLS (owner removes stewards / self-leave). **Manual:** apply `20270825000000_company_steward_invites.sql`; deploy `invite-company-steward` (set `SITE_URL` / `RESEND_API_KEY` on the function as needed). **Prior — Phase 3 complete:** Task 3.3 shipped: `/architect/:id` → `ArchitectIdRedirect` loader (`replace` + **301** to `/person/:slug` or `/company/:slug` by UUID; invalid UUID / no row → 404); `ArchitectDetails.loader` profile redirect removed; `LAUNCH_HOSTING.md` legacy `/architect` note; `App.tsx` parity for non-SSR router. Task 3.2 shipped: claimed-owner **Edit** on `/person/:slug` (`EditPersonForm` sheet — bio, nationality, years, location, website, avatar via `avatars` bucket + `resizeImage`); TanStack `personQueryKey` + `useQuery` sync with loader; `updatePerson` + invalidate + `revalidator`; verified `BadgeCheck` + tooltip; **own** `Profile` shows **Professional profile** card (`getClaimedPersonSummaryForProfile` → name link + credit count). Task 3.1 baseline unchanged. Roadmap 3.2 checked; `DATA_CONTRACT` §9a table extended.

**Prior:** Task 2.4 shipped: `src/features/credits/api/credits.ts` — `getBuildingCredits`, `addBuildingCredit`, `flagCredit` (now RPC `flag_building_credit`; signed-in sets `flagged_by_user_id`), `updateCreditStatus`, `removeCreditByToken` → RPC `redeem_credit_removal_token`; Zod inputs; `tests/unit/credits-removal-token.test.ts`. Migration `20270824000000_redeem_credit_removal_token.sql` adds redemption RPC (`anon`/`authenticated`). `docs/DATA_CONTRACT.md` §9d Component 3 (app API) + §9e Component 4 (RPC); `plano-tables.types.ts` RPC entry. **Manual:** run new migration in Supabase SQL Editor after 1.5. **Manual (Phase 1 DB):** apply migrations 1.1–1.5 if not yet run.

**Phase 7 — Feed Redesign: complete.** P7-99 closed: `DATA_CONTRACT`, `PRD`, `Roadmap` Phase 7 Summary, `.ai-status`; `DESIGN_TOKENS` + `tailwind.config.ts` extended with `text-2xs`, `text-2xs-plus`, `collection-mosaic`, `mosaic-gap`; C7 feed components (`FeedActivityCard`, `FeedCollectionCard`, `SectionDivider`, `ColdStartFeed`) use token-scale classes only (no hex / pixel arbitrary utilities in class strings).

**Phase 6 — RR7 SSR migration (dev entry):** `index.html` is a minimal shell; `npm run dev` uses `react-router dev`. SSR deep links verified.

**Phase 5 — Design system polish (complete):** Semantic tokens via `tailwind.config.ts` and `src/index.css`; quality gates documented in prior snapshots.

## CURRENT_ARCHITECTURE_SNAPSHOT
Package `plano@0.1.0`. **Ambassador program (Phase 6 location review + polish, 2026-04-29):** **`Settings.tsx`** calls **`sync_ambassador_membership_after_profile_geography`** after **`country`** / **`location`** save when values changed; RPC sets **`pending_review`** + **`ambassador_membership_review`** notifications. **`has_embassy_portal_access`** gates **`/embassy`** (active or pending review). **`Embassy.tsx`** / **`BecomeAmbassador.tsx`** handle **`pending_review`**; **`Notifications.tsx`** renders the new type; admin chapter detail flags **Location review**. Migration **`20270870500000_ambassador_phase6_location_review.sql`** (see KNOWN_ISSUES). **Prior — Phase 5 national + admin coverage:** National chapter **presidents** — **National overview** tab on **`/embassy`** (`?tab=national`, **`get_national_chapter_overview`** + read-only local chapter drill-down via existing leadership RPCs). Admins — **`/admin/ambassadors/coverage`** (**`get_admin_ambassador_locality_coverage`**, **`get_admin_ambassador_program_stats`**). Migration **`20270870400000_ambassador_phase5_national_overview_admin_coverage.sql`** (see KNOWN_ISSUES). **Prior — Phase 4 leadership:** **`/embassy`** Leadership tab — metrics, activity, member directory; president invite/update RPCs; **`20270870300000_ambassador_leadership_rpcs.sql`**. **Prior — Phase 3 task feed:** RPCs in **`taskFeed.ts`** + **`Embassy.tsx`** task sections; **`20270870200000_ambassador_task_feed_rpcs.sql`**. **Prior — Phase 2 applications:** Public **`/become-ambassador`**, application review RPCs, admin **`/admin/ambassadors/applications`**; **`20270870100000_ambassador_applications.sql`**. **Prior — Phase 1 foundation:** Admin **`/admin/ambassadors`** list + create dialog, **`/admin/ambassadors/:chapterId`** member management; profile badge via **`get_ambassador_badge_for_profile`** (foundation migration — see KNOWN_ISSUES). **Prior — Phase 1 events — integrity (Task 1.97, 2026-04-14):** typecheck + lint + production build green; events route chunks present in client build output. **Community events (Phase 1 core):** **`/events`** (infinite upcoming list), **`/events/new`** + **`/events/:slug/edit`** (**`SubmitEvent`** — edit fetches **`getEventBySlug`**, non-submitters redirected; **`useUpdateEvent`**; **Where** uses **`LocationInput`** with **`searchTypes={["(addresses)"]}`** (Places New primary types via **`useAutocompleteSuggestions`**); **cover** via **`resizeImage`** + **`uploadFile`** → **`getStorageAssetUrl`** into **`cover_image_url`**), **`/events/:slug`** (**`EventDetail`**). **AppSidebar** / **`BottomNav`**: **Events** (**`CalendarDays`**) between Feed and Explore. **`app/routes.ts`**: distinct route ids **`events-submit-new`** / **`events-submit-edit`** for duplicate **`SubmitEvent`** module. **Events Phase 4 Task 4.2:** **`useOrganisingEvents`** / **`useAttendingEvents`** / **`useInterestedEvents`** + **`useProfileEventsCountBadges`** in **`src/features/events/hooks/useProfileEvents.ts`**; **`profileEventsApi.ts`** (organising **`events`** **`.or()`**; attendance via untyped client for **`event_attendances`** until **`gen-types`** exposes the table); **`eventKeys.profile`**; **`hydrateEventRowsToCards`** + **`EVENT_CARD_LIST_COLUMNS`** shared in **`eventsApi.ts`**. **Task 4.3:** **`EventProfileCard`**, **`ProfileEventsSection`** on **`Profile`**, past attendance hooks + **`fetchProfilePastAttendanceCount`**. **Task 4.4:** event **`RecommendDialog`** + **`EventDetail`** Recommend CTA. **Task 4.5:** **`Notifications.tsx`** — when **`metadata.event_slug`** is set, copy uses **`@{actor}`** + **`Link`** to **`/events/:slug`** on the event title; row click navigates to the event; building recommendation copy unchanged; **`NotificationSettingsDialog`** toggle label **Recommendations & invites**. **`FeedResolvedEntry`** carries a **`TODO`** for future feed rows that surface event-targeted recommendations (no **`get_feed`** branch yet). **Map credit filters (Phase 10.2):** Filter drawer **Credits** — company combobox (`searchCompanies`) + multi-select roles (`CREDIT_ROLES`); URL `creditCompany` / `creditRoles`; `get_map_clusters_v2` + `get_buildings_list` + legacy `get_map_clusters` filter JSON keys `credit_company_id` / `credit_roles` (migration `20270836000000_map_credit_filters.sql`). **Buildings image column:** Queries and embeds use **`hero_image_url`**; client layers that still expose **`main_image_url`** (feed, discovery, search DTOs) map from hero or RPC **`main_image_url`** where the function still returns that key.

**Building detail:** Main column content uses **`w-full min-w-0`** + **`px-4 sm:px-6 lg:px-8`** (no **`max-w-4xl`**); **`EditBuilding`** matches (**`max-w-2xl`** removed). Full-bleed hero band in **`BuildingDetails`** renders only when **`heroImageUrl`** is set — no fixed-height empty **`bg-surface-muted`** strip when there is no image (title block starts under the nav). **`BuildingHero`** (where used elsewhere) returns nothing if the image errors. Above-fold **Info** (styles, typology, access badge, notes) stacks above **Credits** preview on the left — header row includes **View info →** (`#building-info-extended` when shown) and **Edit info →** to **`/edit`** when the user may edit official data; the right column is the map (**`h-48`** when collapsed) plus address line (no section heading). Below **Resources**, **`#building-info-extended`** renders an **Info** definition list (functional category, materials, site context, intervention, aliases, address, access level/logistics/cost) when catalogue fields exist; editors see a short placeholder + **Edit catalogue →** when none. Photos & reviews stream uses **`space-y-10`**. Every **entry-backed** row renders **`resolveCardType`** (non-**`activity`**) → **`DetailCard`** → **`resolveDetailVariant`** → **`DetailCardWithMedia`** or **`DetailCardNoMedia`**. **With media:** 50/50 row on **`md+`** (stacked full-width hero + **`px-0 py-6`** text below on narrow viewports), or full-width **`h-[400px]`** when **`mediaCount === 1`** and **`textTreatment === 'none'`**; §4.3 byline (**`truncate`** @username on small screens), §4.4 **`DetailCardTextTreatmentBlock`**, **`DetailCardFootnote`**. Below-fold **`partitionDetailOverflowImages`** on **`entry.images.slice(1)`** — **`gap-1`** (4px), **`h-[196px]`** cells, odd remainder → final **3-col** row; single overflow image spans2 columns. **`formatDetailMediaMetadataLine`** in **`resolveCardType.ts`**. Section label **`DetailSectionHeader`** + **Upload →** unchanged. **orphan** photos keep legacy featured / image-only tiles; **`video-${reviewId}`** is tied to its review (not a duplicate orphan). **Also visited** remains **`ActivityStreamGroup`** for activity-only entries. Architect-of-building reviews get a client **score boost** so they sort higher (below official signal). **`BuildingDetails`** no longer swaps from a **`BuildingHeader`‑based preview to the editorial layout after mount; **`useBuildingInteractions`** only uses the full-page loading spinner on the first successful load per **`building.id`** (or after navigation to another building). Background refetches (e.g. **`buildingCreditsFingerprint`** / **`userId`** changes) refresh stream and links data without **`setLoading(true)`**. Users who may edit official data see a pencil **`Link`** to **`/building/:id/:slug/edit`** (no inline official-field editing on the detail page). **Visited** / **Save** / **Hide**: when unset, three separate controls; when set, a single **`DropdownMenu`** trigger (current state + chevron) lists all three (same **`handleStatusChange`** semantics, including re-pick current → remove confirm). Rating dots: unfilled slots use **`opacity-0`** until the rating strip is hovered or the user is previewing a hover level; committed filled dots stay visible. Signed-in **Visited** / **Save** (`pending`): **Add note** / **Add to collection** / **Plan a visit** share the top action row with status + rating, **`justify-end`** on the right (replacing **Write review →** in that slot); on **`md+`** that cluster fades in on **`group/actions`** hover, **`focus-within`**, or when note / collections / visit UI is open; **`max-md`** keeps links visible. Expandable panels (note, collections, visit) stay below the row inside the same **`group/actions`** block. When the user has not marked **Visited** or **Save**, **Write review →** remains on the right. The note textarea opens automatically only after a **`user_buildings`** update in-session (**`handleStatusChange`** / **`handleRate`**), otherwise the user expands it via **Add note** (toggle); **`noteEditorOpen`** resets on building or user change; collection selector is no longer auto-expanded when the building is already in collections; **`useBuildingInteractions`** exposes **`setNote`**; **`accessSynthesis`** typed **`| null`**. Community line uses **`totalRatingPoints`** (sum of visitor **`rating`** 1–3) instead of average when any ratings exist.

**Places suggestions:** **`useAutocompleteSuggestions`** calls **`google.maps.places.AutocompleteSuggestion.fetchAutocompleteSuggestions`** (new Places Data API); geocoding via **`@/lib/googleMapsGeocoding`**. **Search `/search` SERP (`BuildingSidebar` buildings tab):** building rows show credit names + **`city`** only (no country/year in the meta line); row **`pr-3`** insets the thumbnail from the column edge. **Global map search (Phase 10.1):** `/search` sidebar tabs for buildings vs people vs companies; `searchPeople` returns nationality, avatar, credit count for list UI; filter drawer **People** multi-select uses `PersonFilterSelect` (no `architects` table reads in that path). **Company steward portfolio (Phase 9.2):** **`/company-portfolio`** — stats + editorial CTAs + toolbar layout (**`CompanyDashboard`**); credits list / reorder / add flows (**`CompanyPortfolioManageSection`**); role filter + owner pending-request queue; sidebar / account menu links; **`?edit=1`** on **`/company/:slug`** opens steward edit sheet when allowed. **Person portfolio dashboard (Phase 9.1):** Authenticated **`/portfolio`** (`PersonDashboard`) for users who have claimed a **`people`** row; **`getPersonPortfolio`** + tier sections + year/role sort; **`/architect/dashboard`** redirects to **`/portfolio`**. Sidebar + account menu **My portfolio** when applicable. **Profile** portfolio tab uses claimed-person detection (not **`verified_architect_id`**). **Entity audit (Phase 8.3):** **`admin_audit_logs`** records **`credit_added`**, **`credit_status_changed`**, **`steward_removed`** (client, actor RLS), **`person_claimed`**, **`company_claimed`**, **`steward_added`** (RPCs). **`BuildingAudit`** (`/admin/audit`) optional **`?building=<uuid>`** merges **`building_audit_logs`** with credit audit rows for that building. **Admin people/companies + entity claims (Phase 8.2):** **`/admin/credits/people`** and **`/admin/credits/companies`** for search, **`claim_status`** edits, merge tool calling **`admin_merge_people`** / **`admin_merge_companies`**; **`/admin/claims`** **`EntityClaims`** (architect claims tab + company dispute queue with resolve). **Admin flagged credits (Phase 8.1):** **`/admin/credits/flagged`** lists **`status = flagged`** credits with building/entity/added-by links; informational **30-day auto-hide** countdown when all linked entities remain **`unclaimed`** and the flag did not originate from **`verified`**; warning when **`flagged_from_status = verified`**. **Company claim disputes (Phase 7.4):** Claimed **`/company/:slug`** — non-stewards see **Dispute this claim** (or log-in link) under the steward-request banner; **`/company/:slug/dispute`** collects reason + optional evidence URL; insert + **`notify-admin-dispute`**; submitter sees **Dispute under review** while status is **`open`**. **Steward access requests (Phase 7.3):** Claimed **`/company/:slug`** for non-stewards — **Request access** (optional message) → DB row + owner emails with **`/approve-steward-request/:token`**; owner signs in → RPC adds **steward** + **`notify-steward-request-approved`** → redirect **`?stewardApproved=1`** + toast + Edit sheet. **Company first claim (Phase 7.2):** Unclaimed **`/company/:slug`** — **Claim this company** / **Log in to claim** → work email → email link **`/verify-company-claim/:token`** redeems RPC (owner steward + **`claimed`** + **`verified_domain`**); already-claimed + domain mismatch → **`/company/:slug/dispute`**. **Person claim (Phase 7.1):** Unclaimed **`/person/:slug`** shows claim CTA; signed-in users open **`ClaimPersonDialog`** (This is me / I represent); **`claim_person`** RPC sets owner + **`claimed`**; **`notify-entity-claimed`** emails distinct **`added_by_user_id`** on visible credits (excludes the claimant). **Building credits:** Phase 1 DB + Phase 2 APIs; **`BuildingCredits`** on building detail (tier/role groups, lead labels, verified tooltip, ancillary collapsible, **report credit** popover/sheet + session hide, authenticated **Add a credit** opens **`AddCreditForm`** sheet (multi-row submission + optional email notify via **`notify-credited-entities`**); email **Remove this credit** opens **`/remove-credit/:token`** (loader redeems RPC, success/error UI, building link when RPC returns building fields). **`CreditEntityPicker`** (Phase 6.1) for merged person/company search + create with similarity gate; **`searchCompanies`** includes per-company **`creditCount`**. **`flag_building_credit`** RPC (apply migration `20270826…`) for public flagging without steward UPDATE RLS. **Phase 4:** public company profile at `/company/:slug` (unclaimed banner, credits by tier with sub-headings per role, client-side role filter, ancillary collapsible; Schema.org `Organization` JSON-LD). Stewards: edit sheet, steward list + owner invite/remove, accept-invite route; **`getCompanyStewardsWithProfiles`**, **`inviteCompanySteward`**, **`redeemCompanyStewardInvite`**, **`companyStewardsQueryKey`**. **`getCompanyWithClient(client, slug)`** for SSR loaders; **`companyQueryKey`**. **Phase 3:** public person profile at `/person/:slug` (unclaimed banner, credits by tier with collapsible ancillary, company links to `/company/:slug` when present); claim owner sees Edit sheet + verified badge when `claim_status = verified`. Legacy **`/architect/:uuid`** redirects to `/person/:slug` or `/company/:slug` (same UUID as migrated `architects`); `ArchitectDetails` UI no longer mounted on that path (Phase 11 removal). **`getPersonWithClient(client, slug)`** for SSR loaders; **`getPerson`** delegates to it with browser client; **`personQueryKey`**, **`getClaimedPersonSummaryForProfile`** in `people.ts`. Own **`/profile/:username`** surfaces linked `/person/:slug` + credit count when `people.claimed_by_user_id` matches. **Superadmin card playground (Card System 3.4):** **`/superadmin/cards`** — background presets ( **`surface-default`** feed/profile, **`bg-black`** dark canvas, **`surface-muted`** building panel), viewport 375 / 768 / 1280 preview strip, prominence diff + **Copy fixture JSON**; **`ReviewCardFeed`** supports optional **`prominenceOverride`** for forced **`CardSpec.prominence`**.

**Feed (Phase 7 + Card System):** Home **`Index.tsx`** — **`hero`** and **`compact`** social rows render **`FeedCardA`**/**`B`**/**`C`** via **`resolveCardType`** (with optional **`item.cardType`** from **`aggregateFeed`**); suggested rows use chromeless **`SuggestedContentBlock`**; **`ColdStartFeed`** / **`EmptyFeed`** / playground use **`FeedResolvedEntry`**; PYMK (**`PeopleYouMayKnow`** → **`UserRow`**): inline placements use default horizontal row; feed right rail uses **`userRowLayout="stacked"`** — full-width identity (avatar + **`line-clamp-2`** username + mutuals), dismiss + **Follow** on a second row for the narrow column. **`mergeAggregatedFeedWithEventAttendance`** interleaves **`FeedEventAttendance`** rows (followed users **`going`**, last 30 days, clustered by event) with aggregated review items by recency; **`useFeed`** exposes **`eventAttendance`** + **`isEventAttendancePending`** (`useQuery` **`staleTime: 60s`**). Full-page loader waits only for **`get_feed`** when the social stream has items; when empty, still waits for event-attendance resolution so cold-start vs attendance-only does not flash wrong. **`useCollectionsFeed`** / **`useSuggestedFeed`** enable after primary **`get_feed`** resolves (less parallel contention on first paint). **`docs/COMPONENT_SPEC.md`** §14 still references legacy **`CardSpec`** / **`ReviewCardFeed`** prose — sync deferred (see KNOWN_ISSUES). **`aggregateFeed`** attaches **`cardType`** where applicable. Interleaves collections (`useCollectionsFeed` → `get_collections_feed` RPC) and discovery (`useSuggestedFeed`). **Components:** `FeedActivityCard`, `FeedCollectionCard`, `FeedClusterCard`, `FeedEventAttendanceRow`, `SectionDivider`, `ColdStartFeed` / `EmptyFeed` as before. **Types:** `FeedCollection`, `FeedEventAttendance`, `FeedHomeEntry`, `RawCollectionFeedRow`, `CollectionPreviewBuilding` in `src/types/feed.ts`; `ReviewBuilding` includes `community_preview_url`; activity Rule 0 uses main or community path. **Theme:** Tailwind `extend.fontSize` (`2xs`, `2xs-plus`) and `extend.spacing` (`collection-mosaic`, `mosaic-gap`) documented in `docs/DESIGN_TOKENS.md`. **`font-display`** (Barlow700/900) + Inter + Space Mono (with italics) from Google Fonts in **`src/root.tsx`**; feed card building titles on **`ReviewCardFeed`** / **`FeedHeroCard`** use **`font-sans`** (Inter); Barlow remains available for other display use. **`CollectionCard`** / **`DiscoveryBuildingCard`** — chromeless cards, monochromatic hovers (opacity), favourite collection bottom accent **`border-text-primary`**; discovery building title **`font-black text-xl`**. **Contract:** `docs/DATA_CONTRACT.md` includes `get_collections_feed` and collection-feed DTOs; `BuildingSummaryDTO.mainImageUrl` optional for feed joins. **Brand icons:** `public/favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png`, `android-chrome-192x192.png`, `android-chrome-512x512.png` (white “P” on black); document head in `src/root.tsx` and `index.html`; PWA manifest in `vite.config.ts` uses `theme_color` / `background_color` `#000000` and precaches the icon set via `includeAssets`. **PWA Workbox:** `navigateFallback: null` so the service worker does not register a NavigationRoute for `index.html` (not in the SSR client precache — avoids workbox `non-precached-url`). **`vite-plugin-pwa`:** `registerType: "prompt"` (no `autoUpdate` tab-switch reload); `PwaProvider` applies waiting SW via `updateServiceWorker()` only after a full browser reload or a client-side route pathname change (no Sonner “new version” toast). Build injects `__APP_VERSION__` from `package.json`; `useVersionNotification` in `AppShell` shows a Sonner toast on major/minor bumps or if 14+ days since last toast (`localStorage` keys `plano_last_notified_*`). **Feed RPCs:** `get_feed` / `get_suggested_posts` `building_data` uses `public.main_image_url(b)` (migration `20270839000000` + corrected `20270837000000`); **`company_stewards`** SELECT uses `plano_auth_is_company_steward` to avoid RLS recursion. **`ColdStartFeed`** featured building: `creditedEntities` credit line + `getBuildingUrl(id, slug, short_id)`.

## KNOWN_ISSUES
- **Credit notes SQL (2026-05-09):** Apply **`20270902000000_building_credit_notes.sql`** in the Supabase SQL Editor. Creates `building_credit_notes` table (UNIQUE on `credit_id`), RLS (owner read/write; public read on active/verified credits), `updated_at` trigger. Until applied, the note join in `getBuildingCredits` will silently return no note data and `upsertCreditNote` / `deleteCreditNote` calls will fail at runtime.
- **Award edition events SQL (2026-05-08):** Apply **`20270901000000_award_edition_events.sql`** in the Supabase SQL Editor. Creates `award_edition_event_type` enum + `award_edition_events` table with RLS. Depends on `20270876000000_awards_foundation.sql` and `20270900000000_award_admins.sql` (for `plano_auth_is_award_admin_for_recipient`). Until applied, the Events section in admin edition pages and the "What's Next" section on award pages will silently return empty data (no runtime crash — queries will fail with a Supabase error caught by TanStack Query).
- **Ambassador Phase 6 SQL (2026-04-29):** Apply **`20270870500000_ambassador_phase6_location_review.sql`** in the Supabase SQL Editor **after** **`20270870400000`** (and the full ambassador chain through **704**). Adds **`ambassador_membership_review`** notification type, **`has_embassy_portal_access`**, **`sync_ambassador_membership_after_profile_geography`**, internal **`_ambassador_profile_matches_chapter`**, and updates **`submit_ambassador_application`** to reject **`pending_review`** memberships. Until applied, **`has_embassy_portal_access`** and geography sync RPC calls will fail at runtime.
- **Ambassador Phase 5 SQL (2026-04-29):** Apply **`20270870400000_ambassador_phase5_national_overview_admin_coverage.sql`** in the Supabase SQL Editor **after** leadership migration **`20270870300000`**. Adds **`get_national_chapter_overview`**, **`get_admin_ambassador_locality_coverage`**, **`get_admin_ambassador_program_stats`**, **`is_national_president_of_local_chapter_parent`**, and extends **`get_chapter_metrics`** / **`get_chapter_ambassador_activity`** / **`get_chapter_members_with_contact`** so national presidents can read child local chapters. Until applied, **National overview** in **`/embassy`** and **`/admin/ambassadors/coverage`** will error at runtime.
- **Ambassador leadership SQL (2026-04-29):** Apply **`20270870300000_ambassador_leadership_rpcs.sql`** in the Supabase SQL Editor **after** foundation + applications + task-feed migrations. Adds **`updated_by`** on **`ambassador_memberships`**, drops chapter-leader direct **`UPDATE`** policy (presidents use RPCs; admins keep table **`UPDATE`**), and creates leadership RPCs. Until applied, **`/embassy`** Leadership tab and president actions will error at runtime.
- **Ambassador task feed SQL (2026-04-29):** Apply **`20270870200000_ambassador_task_feed_rpcs.sql`** in the Supabase SQL Editor **after** foundation + applications migrations so Embassy chapter-scoped task RPCs and **`get_ambassador_my_audit_timeline`** exist. Until then, **`/embassy`** task sections will error at runtime. After apply, run **`npm run gen-types`** if hosted schema should fully match committed **`types.ts`**.
- **Ambassador applications SQL (2026-04-29):** Apply **`20270870100000_ambassador_applications.sql`** in the Supabase SQL Editor **after** the foundation migration so **`ambassador_applications`**, extended **`notifications.type`** check, RLS, and RPCs **`submit_ambassador_application`** / **`review_ambassador_application`** exist. Until then, **`/become-ambassador`**, **`/embassy`** application review, and admin applications will error at runtime. After apply, run **`npm run gen-types`** if hosted schema should fully match committed **`types.ts`**.
- **Ambassador foundation SQL (2026-04-29):** Apply **`20270870000000_ambassador_foundation.sql`** in the Supabase SQL Editor so **`ambassador_chapters`**, **`ambassador_memberships`**, RLS, helpers, and **`get_ambassador_badge_for_profile`** exist. Until then, **`/admin/ambassadors`** and the profile ambassador badge RPC will error at runtime. After apply, run **`npm run gen-types`** if hosted schema should fully match committed **`types.ts`**.
- **Admin dashboard SQL (2026-04-29):** Apply **`20270869000000_admin_dashboard_metrics_overhaul.sql`** in the Supabase SQL Editor so **`get_admin_pulse`** returns full JSON (including **`new_users_24h`** and content counts), **`is_admin()`** treats **`app_admin`** like **`admin`**, retention/trends/content/notifications match the client, and admins can **`SELECT`** all **`user_buildings`** for moderation. Until applied, **`/admin`** may show an error from **`fetchAdminDashboardStats`** (RPC shape / access denied).

- **Explore `get_discovery_feed` location (2026-04-28):** Apply **`20270867000000_discovery_feed_tiered_location.sql`** in the Supabase SQL Editor (drops **`get_discovery_feed`** …**`text`** 14-arg form, adds **`p_locality_id`** + viewport floats + **`resolve_locality_for_explore`**). If **670** not applied yet, **660**/**620**/**630** notes in prior commits still apply. **Client:** **`Explore`** → **`resolve_locality_for_explore`** + viewport bounds + **`useDiscoveryFeed`**.

- **Card System — detail §4.4 “tertiary” (2026-04-13):** Roadmap footnote / metadata call for **`text-text-tertiary`**; repo has no such Tailwind token — footnote and **`none`** metadata use **`text-text-secondary`**. **`DetailCardA`**/**`B`**/**`C`** remain for **`CardPlayground`** until Task 4.1.
- **Card System docs vs code (2026-04-12):** **`docs/COMPONENT_SPEC.md`** §14 still says **`CardSpec`** lives in **`src/types/cards.ts`**; Task 1 moved **`CardSpec`** / related enums to **`src/features/feed/utils/resolveCardSpec.ts`** ( **`src/types/cards.ts`** now exports only **`CardType`** ). Sync in Roadmap Task 5.99 / COMPONENT_SPEC pass.
- **Card system feed RPC (2026-04-11):** Run **`20270843000000_feed_user_data_followers_count.sql`** in the Supabase SQL Editor so **`get_feed`** / **`get_suggested_posts`** return **`followers_count`** (and suggested feed **`is_verified_architect`** / **`is_architect_of_building`**) in **`user_data`**; until then the app maps **`followers_count: null`** and suggested cards omit verification flags from RPC.
- **Review upload presigned URL still shows `x-amz-checksum-crc32` / `x-id=PutObject` (2026-04-10):** That shape is **only** from the old **AWS SDK** presigner. Repo **`generate-upload-url`** uses **`aws4fetch`** and returns **`presignEngine: "aws4fetch-v1"`** + header **`x-plano-presign-engine`**. If those are missing in the **`generate-upload-url`** response on **www.plano.app**, the Edge bundle on the **Supabase project behind `VITE_SUPABASE_URL`** was not updated — often **`supabase functions deploy`** without **`--project-ref`** updates **`supabase/config.toml`** **`project_id`** (currently **`gyxspsuctbrxhwiyfvlj`**) while production types use **`lnqxtomyucnnrgeapnzt`**. Deploy with **`--project-ref`** matching the live app URL. Separately, S3 bucket **CORS** must allow **`https://www.plano.app`** for browser PUT or OPTIONS will still fail.
- **Building detail `get_building_reviews` (2026-04-09):** Run **`20270841000000_get_building_reviews_restore.sql`** in the Supabase SQL Editor so **`rpc/get_building_reviews`** exists, uses **`building_credits`** / **`people`** / **`company_stewards`** (not removed **`building_architects`**), and has **`GRANT EXECUTE`** for **`anon`** / **`authenticated`** (PostgREST **404** without exposure).
- **Google Places Autocomplete (New) (2026-04-09):** Programmatic suggestions use **`AutocompleteSuggestion.fetchAutocompleteSuggestions`** (not legacy **`AutocompleteService`**). In Google Cloud Console, ensure **Places API (New)** is enabled for the Maps key project if autocomplete returns errors or empty lists after deploy.
- **Feed RPC `get_feed` (2026-04-09):** Run **`20270840000000_get_feed_ambiguous_id_plpgsql.sql`** in the Supabase SQL Editor (fixes **42702** ambiguous **`id`** inside **`get_feed`** when **`20270839000000`** / **`20270837000000`** definitions are live). Also run **`20270839000000_feed_main_image_and_steward_rls.sql`** if not yet applied (**`main_image_url(buildings)`** + **`company_stewards`** RLS helper).
- **Follows / contacts visibility (2026-04-09):** Run **`20270838000000_user_buildings_contacts_followers_select.sql`** in the Supabase SQL Editor so **`user_buildings`** **`visibility = 'contacts'`** uses **one-way follow** (viewer follows author); until then RLS may still require **mutual** follow via **`is_mutual_contact`**, hiding contact-only reviews/photos from followers.
- **Phase 11 Task 11.1 (2026-04-08):** After editing **`supabase/functions/sitemap`** / **`og-tags`**, **redeploy** those functions on Supabase so production sitemap and legacy **`/architect/{uuid}`** OG shares match the repo.
- **Phase 11 follow-up (low):** Grep may still find the word “architect” in UI copy, SEO history in this file, or **`p_architect_ids`** RPC param names (values are person/company UUIDs). **`useDiscoveryFeed`** still passes **`architectIds`** into **`get_discovery_feed`** — param name is legacy; behavior is people/company IDs if the RPC matches map/search.
- **Task 10.2 (2026-04-08):** Run **`20270836000000_map_credit_filters.sql`** in the Supabase SQL Editor so map clusters, sidebar list, and legacy **`get_map_clusters`** honor company/role credit filters; until then, the UI writes URL params but RPCs ignore them.
- **Task 9.2 (2026-04-08):** Run **`20270835000000_steward_request_owner_dashboard_rpcs.sql`** in the Supabase SQL Editor so in-app **Approve** / **Decline** on the company portfolio dashboard work; until then those RPC calls fail at runtime.
- **Profile Vitest (2026-04-08):** `Profile.regression`, `Profile.view-persistence`, `Profile.test`, `Profile.likes` aligned with Profile v2: `MemoryRouter` + `Route`, `getClaimedPersonSummaryForProfile` mock, `EditorialBuildingCard` `data-testid` `review-card-${id}`, toggle queries use `getByRole('radio', …)`, partial **`framer-motion`** mock (`AnimatePresence` → fragment) so grid/list/kanban swap under test; **`useSidebar` → isMobile: false** where list columns matter; kanban search case asserts mock **`kanban-view`** text (Empire / no Chrysler).
- **Task 8.3 (2026-04-08):** Run **`20270834000000_entity_audit_logs.sql`** in the Supabase SQL Editor so credit/claim/steward audit rows and actor INSERT RLS apply; until then **`insertEntityAuditLog`** fails for non-admins and RPCs omit new audit lines.
- **Task 8.2 (2026-04-08):** Run **`20270833000000_admin_merge_people_companies.sql`** in the Supabase SQL Editor so **Merge people** / **Merge companies** work; without it, RPC calls fail at runtime.
- **Task 8.1 (2026-04-08):** Run **`20270832000000_building_credits_flagged_from_status.sql`** in the Supabase SQL Editor (adds **`flagged_from_status`**, updates **`flag_building_credit`**). Deploy Edge Function **`notify-credit-outcome`** with **`RESEND_API_KEY`** and **`SITE_URL`** (same as other Resend functions) so Verify/Hide emails send; without migration, **`select *`** on **`building_credits`** may error until the column exists.
- **Task 7.4 (2026-04-08):** Run **`20270831000000_company_claim_disputes.sql`** in the Supabase SQL Editor before disputes work. Deploy Edge Function **`notify-admin-dispute`** with **`RESEND_API_KEY`** and **`SITE_URL`** (same as other Resend functions). Optionally set **`PLANO_ADMIN_NOTIFY_EMAIL`** or **`COMPANY_CLAIM_DISPUTE_NOTIFY_EMAIL`** on the function so dispute mail does not go to the default inbox.
- **Task 7.3 (2026-04-08):** Migration + Edge deploy done on hosted project **`lnqxtomyucnnrgeapnzt`** (`notify-steward-request`, `notify-steward-request-approved`). Confirm **`RESEND_API_KEY`** / **`SITE_URL`** secrets on that project if any email fails.
- **Task 7.2 (2026-04-08):** Run **`20270829000000_company_claim_verification_tokens.sql`** in the Supabase SQL Editor before company claim redemption works. Deploy Edge Function **`verify-company-claim`** with **`RESEND_API_KEY`** and **`SITE_URL`** (same as other Resend functions) so verification emails send; until then token rows are still created but users may not receive the link.
- **Task 7.1 (2026-04-08):** Run **`20270828000000_claim_person_rpc.sql`** in the Supabase SQL Editor before claiming works. Deploy Edge Function **`notify-entity-claimed`** with **`RESEND_API_KEY`** and **`SITE_URL`** (same as other Resend functions) so credit submitters get email; until then claim still succeeds but notifications fail at the function.
- **Task 6.4 migration (2026-04-08):** Until **`20270827000000_redeem_credit_removal_token_building_payload.sql`** is applied in the Supabase SQL Editor, removal still works but the thank-you page may omit the **View building** link (RPC returns only `credit_id` until replaced).
- **Task 6.3 Edge Function (2026-04-08):** Until **`notify-credited-entities`** is deployed to Supabase with **`RESEND_API_KEY`**, **`SITE_URL`**, and optional **`PUBLIC_STORAGE_URL`** (match **`VITE_PUBLIC_STORAGE_URL`** for email images), **Send notifications** will fail at runtime. Requires DB migrations through **1.5** (`generate_credit_removal_token`, `credit_notification_log`).
- **Task 5.3 migration (2026-04-08):** Until `20270826000000_flag_building_credit_rpc.sql` is run in the Supabase SQL Editor, **Report credit** will fail at runtime (RPC missing).
- **Task 4.2 DB + function (2026-04-08):** Until `20270825000000_company_steward_invites.sql` is applied and `invite-company-steward` is deployed, invites and redemption will fail at runtime.
- **Supabase security linter 0013 (`spatial_ref_sys`):** On hosted Supabase, RLS on `spatial_ref_sys` often fails (owner `supabase_admin`). Migrations `20270807000000_enable_rls_spatial_ref_sys.sql` and `20270818000000_ensure_spatial_ref_sys_rls.sql` use **REVOKE** from `anon` / `authenticated` / `PUBLIC` per [supabase#29122](https://github.com/supabase/supabase/issues/29122). Run in SQL Editor with role **postgres**. If REVOKE returns 42501, contact Supabase support.
- **Apply migration for collection feed previews:** `20270817000000_collections_feed_preview_community_url.sql` adds `community_preview_url` to `get_collections_feed` JSON. Until applied in Supabase, the client still maps `communityPreviewUrl: null` and relies on `getBuildingImageUrl(main_image_url)` for thumbnails.
- **Profile header `firm` / `website` (2026-04-09):** Run **`20270842000000_profiles_firm_website.sql`** in the Supabase SQL Editor so `profiles.firm` / `profiles.website` exist; until then header save may error or silently drop those fields at PostgREST.
- **`seo-check.sh` vs live production `/terms` (Roadmap 6.7, 2026-04-06):** `BASE_URL=https://plano.app` smoke fails on `/terms` — missing canonical in deployed HTML; repo `Terms.tsx` already fixed — **deploy** clears it. Vercel PR preview (`VERCEL_PREVIEW_URL`) should pass once preview build includes that commit.
- **`og-tags` Edge Function not on live Supabase (Roadmap 6.5, 2026-04-06):** `https://lnqxtomyucnnrgeapnzt.supabase.co/functions/v1/og-tags` returns **404** NOT_FOUND; `sitemap` on the same host returns 200. Deploy `og-tags` before social/curl QA. Repo adds homepage canonical for unknown `short_id` in `og-tags/index.ts`.
- **Production vs `main` (Roadmap 6.4, 2026-04-06):** Live `www.plano.app` returned soft **200** for unknown paths and omitted `robots` on `/admin` SSR until the next deploy after `NotFound` `loader` (404) + existing admin meta ship.
- **SSR meta QA — public folder sample (Roadmap 6.3):** At 2026-04-06, production `sitemap.xml` had no `/:username/folders/:slug` entries; curl SSR spot-check for folder meta deferred until a stable public folder URL exists.
- **HydrateFallback visibility (P1-6 UAT):** Slow 3G hard navigation to loader routes may show a blank window during chunk load — follow up if product wants visible loading chrome.
- **`npm run preview`:** Serves client only; use `npx vercel dev` or deploy for SSR-like checks.
- **`react-hooks/exhaustive-deps` disabled:** Re-enable selectively when refactoring effects is safe per screen.
- **Supabase types vs client:** After **`npm run gen-types`**, run **`npm run typecheck`** (and fix selects/casts) — the generated file tracks **hosted** schema (e.g. **`hero_image_url`** on **`buildings`**).
- **Events Phase 4 — `event_attendances` typing (2026-04-14):** Hosted DB includes **`event_attendances`** (per Events roadmap); generated **`types.ts`** in repo may omit the table until **`npm run gen-types`** succeeds from a permitted environment. **`profileEventsApi`** uses a cast **`SupabaseClient`** for attendance reads; remove the cast once the table appears in **`Database`**.
- **Stale coverage HTML:** Re-run `npm run test:coverage` to refresh.
- **RR7 Phase 2 checkbox:** Reconcile `docs/rr7-migration-roadmap.md` “zero failures” box on next doc pass.
- **`npm run test` (full Vitest, 2026-04-09):** Suite **green** (**597** tests). QA 11.5 extends **`AppSidebar.test`** + **`profile-nav-qa115`**. QA 11.4 adds **`building-detail-pages-qa114`**. QA 11.3 adds **`seo-sitemap-qa113`**. QA 11.2 adds **`deprecated-artifacts-qa112`**. QA 11.1 adds **`routes-canonical-qa111`**. QA 10.2 adds **`useURLMapState.qa102`**, **`useMapData`** credit-omit case, **`FilterDrawer`** QA credits block. QA 10.1 adds **`no-architect-select-files-qa101`**, **`BuildingSidebar.qa101`**, **`DiscoverySearchInput.qa101`**, **`ArchitectSearchNudge.qa101`**. QA 9.2 adds **`CompanyDashboard.qa92`**, **`AppSidebar.qa92`**. QA 9.1 adds **`PersonDashboard.qa91`**, **`AppSidebar.qa91`**, **`Profile.qa91`**. QA 8.3 adds **`entity-audit-log-insert-qa83`**, **`admin-audit-logs-schema-qa83`**, **`BuildingAudit.qa83`**. Prior fixes: stable **`useAuth` user** reference + **`useParams`** + head-count **`select` chain** + **`maybeSingle`** for profiles in **`Profile.test`** / **`Profile.likes`**; **`AppSidebar`** credit-nav hook mocks; feed card / hero tests aligned with editorial layout; **`BuildingSidebar`** rating label + **`data.pages`** infinite-query mock.

## Completed Tasks
- **Company portfolio UI refinement (2026-04-29):** **`CompanyDashboard.tsx`** — company name eyebrow, editorial uppercase CTAs (**View public company →** / **Edit company profile →**), stat cards aligned with **`PersonDashboard`**, bordered portfolio role toolbar, unified pending-requests list in one card, **`rounded-none`** on relevant controls; **`CompanyPortfolioManageSection.tsx`** — bordered tool strip, list card chrome, improved empty state, tier rail + drag handle geometry. **`CompanyDashboard.qa92`** green; **`npm run build`**, **`typecheck`**, **`lint`** green.

- **Ambassador Phase 6 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870500000_ambassador_phase6_location_review.sql`**; **`Settings.tsx`** geography sync RPC + toast; **`AmbassadorGuard.tsx`** **`has_embassy_portal_access`**; **`Embassy.tsx`** pending-review UX; **`BecomeAmbassador.tsx`** pending-review + profile prompt copy; **`Notifications.tsx`** **`ambassador_membership_review`**; **`AmbassadorChapterDetail.tsx`** location-review badge; **`docs/DATA_CONTRACT.md`** + **`docs/ROADMAP.md`** Phase 6 **[x]**; **`types.ts`** RPC entries. Full **`npm run build`** + **`typecheck`** + **`lint`** green (2026-04-29).

- **Ambassador Phase 5 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870400000_ambassador_phase5_national_overview_admin_coverage.sql`** (national overview RPC, admin locality + program stats RPCs, **`is_national_president_of_local_chapter_parent`**, extended chapter read RPCs); **`fetchNationalChapterOverview`** in **`leadership.ts`**; **`EmbassyNationalOverview.tsx`** + **`Embassy.tsx`** **`tab=national`**; **`ambassadorCoverage.ts`**, **`AmbassadorCoverage.tsx`**, **`app/routes.ts`** **`/admin/ambassadors/coverage`** (before **`/:chapterId`**), **`AdminSidebar`** + active-state fix, **`AmbassadorChapters`** Coverage link; **`docs/DATA_CONTRACT.md`**, **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Phase 5 **[x]**; **`types.ts`**. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Ambassador Phase 4 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870300000_ambassador_leadership_rpcs.sql`**; **`src/features/embassy/api/leadership.ts`**, **`EmbassyLeadership.tsx`**; **`Embassy.tsx`** URL-driven tabs (**tasks** / **leadership** / **applications** + pending badge); **`docs/DATA_CONTRACT.md`** leadership RPCs + **`updated_by`** + RLS note; **`docs/ROADMAP.md`** Phase 4 **[x]**; **`types.ts`**. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Ambassador Phase 3 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870200000_ambassador_task_feed_rpcs.sql`** (internal **`_ambassador_can_access_chapter`** / **`_building_in_ambassador_chapter_scope`**, five **`GRANT EXECUTE`** RPCs); **`src/features/embassy/api/taskFeed.ts`**; **`Embassy.tsx`** task sections + TanStack Query + leaders **Chapter tasks** / **Applications** tabs; **`docs/DATA_CONTRACT.md`** Embassy task feed RPCs + migration note; **`docs/Roadmap.md`** Phase 3 **[x]**; **`types.ts`** RPC entries. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Ambassador Phase 2 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870100000_ambassador_applications.sql`** (table, partial unique pending index, RLS, notification **`type`** values, **`submit_ambassador_application`** / **`review_ambassador_application`**); **`BecomeAmbassador.tsx`**, **`Embassy.tsx`** + **`AmbassadorGuard.tsx`**, **`AmbassadorApplications.tsx`**; **`app/routes.ts`** (`/become-ambassador`, `/embassy`, `/admin/ambassadors/applications` before **`/:chapterId`**); **`AmbassadorChapters`** link to applications; **`Notifications.tsx`** ambassador types; **`ambassadorApplicationSubmitSchema`**; **`docs/DATA_CONTRACT.md`** applications + RPCs; **`docs/ROADMAP.md`** Phase 2 **[x]**; **`types.ts`** **`ambassador_applications`**, **`is_ambassador`**, RPC args. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Ambassador Phase 1 (EXECUTOR_PROMPT / ROADMAP, 2026-04-29):** Migration **`20270870000000_ambassador_foundation.sql`** (tables, triggers, **`get_user_ambassador_membership`**, **`is_ambassador`**, **`is_chapter_leader`**, **`is_chapter_president`**, **`get_ambassador_badge_for_profile`**, RLS, grants); admin **`AmbassadorChapters`** / **`AmbassadorChapterDetail`** + **`app/routes.ts`** + **`AdminSidebar`**; profile **`Shield`** badge via RPC; Zod **`src/lib/validations/ambassador.ts`**; **`docs/DATA_CONTRACT.md`** ambassador section; **`docs/ROADMAP.md`** Phase 1 marked **[x]**; **`types.ts`** tables + RPC. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Admin dashboard metrics overhaul (2026-04-29):** Migration **`20270869000000_admin_dashboard_metrics_overhaul.sql`** — **`get_admin_pulse`** (full pulse + **`SET search_path`**), **`get_admin_retention`** (activity proxy), **`get_admin_content_stats`** / **`get_admin_trends`** / **`get_admin_leaderboards`** / **`get_admin_notifications`** aligned; **`is_admin()`** includes **`app_admin`**; RLS **`Admins can view all user_buildings for moderation`**. Client: **`fetchAdminDashboardStats`** RPC-only pulse + **`normalizeActivityTrends`** + errors; **`PulseZone`**, **`ActivityTrendsZone`** (line DAU, copy), **`UserLeaderboardZone`**, **`NotificationIntelligenceZone`** (tokens), **`Dashboard`** error UI; **`docs/DATA_CONTRACT.md`**. **`npm run typecheck`**, **`lint`**, **`build`** green.

- **Building detail — no hero placeholder (2026-04-29):** **`BuildingDetails.tsx`** — when **`heroImageUrl`** is null, omit the full-bleed hero wrapper entirely so the header/title row sits higher (no gray band). **`npm run typecheck`**, **`lint`**, **`build`** green.
- **PWA SW update — no Sonner toast (2026-04-28):** **`usePwaInstall.tsx`** — removed **`pwa-sw-update`** “new version” toast; with **`registerType: "prompt"`**, call **`updateServiceWorker()`** when **`needRefresh`** after a full browser reload (**`navigation.type === 'reload'`**) or a client-side **`useLocation()`** pathname change (workbox then reloads once). **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Meta Sharing Debugger / link previews — `robots.txt` (2026-04-28):** **`public/robots.txt`** — explicit **`Allow: /`** for **`facebookexternalhit`**, **`Facebot`**, **`FacebookBot`**, **`Twitterbot`**, **`LinkedInBot`**, **`Slackbot-LinkExpanding`** before **`User-agent: *`** so Meta’s scraper is not treated as blocked (debugger **403** / bad **`og:title`** fallback). **`tests/unit/seo-sitemap-qa113.test.ts`** — stricter disallow assertions for **`/person`** / **`/company`** (allows **`/*/edit`** only); sitemap rewrite expects **`/api/sitemap-proxy`**; asserts Meta allowlist present. **`npm run build`**, **`typecheck`**, **`lint`**, **`vitest`** `seo-sitemap-qa113` green.
- **PWA install prompt cooldown (2026-04-28):** Auto **`PwaPrompt`** records **`pwa_install_prompt_last_shown_at`** when the toast or iOS drawer is shown; **`shouldShowPrompt`** returns false for **7 days** (replaces 14-day dismiss + visit gating). **`markInstallPromptShown`** on context; removed **`dismissPrompt`**. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **PWA / chunk reload UX (2026-04-28):** **`vite.config.ts`** — `registerType: "prompt"` (replaces **`autoUpdate`**, which reloaded all clients when **`registration.update()`** found a new SW — often right after switching back to the tab). **`usePwaInstall.tsx`** — Sonner toast **`pwa-sw-update`** when **`needRefresh`**, user chooses **Refresh now**; dismiss when update clears. **`lazyWithRetry.tsx`** — skip automatic **`location.reload()`** in **`import.meta.env.DEV`** only (keeps post-deploy recovery in production + Vitest **`MODE === "test"`**). **`npm run typecheck`**, **`lint`**, **`lazyWithRetry`**, **`build`** green.
- **Explore swipe / iPad portrait (2026-04-28):** **`DiscoveryCard`** — removed Framer **`drag`** / **`dragDirectionLock`** (axis mis-read vs vertical snap on iPad). Custom **pointer** pipeline: ratio-based **horizontal vs vertical** intent (`clientX`/`clientY` only), **`setPointerCapture`** + **`touch-none`** + non-passive **`touchmove`** **`preventDefault`** only after horizontal arms; vertical intent yields to parent **`touch-pan-y`** scroller. Velocity + offset commit, spring reset, **`blockImageTapRef`** after horizontal drag; merged ref for **`useIntersectionObserver`** callback ref + **`cardRootRef`**. **`npm run build`**, **`typecheck`**, **`lint`**, **`DiscoveryCard.test`** green.
- **Explore tiered location filter (2026-04-28):** Migration **`20270867000000_discovery_feed_tiered_location.sql`** — **`resolve_locality_for_explore`**, **`get_discovery_feed`** tiers: **`locality_id`**, viewport envelope, text. **`exploreLocationFilter.ts`**, **`Explore.tsx`**, **`useDiscoveryFeed`**, **`DiscoverySearchInput`** async **`onPlaceDetails`**, **`exploreLocationFilter.test.ts`**. **`docs/DATA_CONTRACT.md`**; **`types.ts`**.
- **Explore location filter (2026-04-28):** Migration **`20270866000000_discovery_feed_location_filter_fix.sql`** — **`get_discovery_feed`** adds **`p_country_code_filter`**, matches **`buildings.country_code`** (with name fallback when code null on row), substring **`country`** when only name sent, case-insensitive region in **`address`** or **`city`**. **`Explore.tsx`** / **`useDiscoveryFeed`** pass **`countryCode`** from **`extractLocationDetails`**. **`docs/DATA_CONTRACT.md`** RPC row; **`types.ts`** Args. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Auth page layout (2026-04-28):** **`Auth.tsx`** — root **`w-full flex-1`** so the form is not a shrink-wrapped flex item under **`SidebarProvider`** (fixes card stuck left); **`max-w-sm`** column wraps logo + card with **`items-center`**; sign-in **Forgot password?** and sign-up terms share one **`space-y-2`** slot, **`text-sm`**, left-aligned secondary row (**`items-start`**, **`gap-3`** for checkbox + label); terms link **`rel="noreferrer"`**. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Logged-out waitlist CTA (2026-04-28):** Migration **`20270865000000_waitlist_signups.sql`** — **`waitlist_signups`** + anon/auth **`INSERT`** + admin **`SELECT`**; **`WaitlistSignupProvider`** / **`WaitingListDialog`** + **`insertWaitlistSignup`**; **`AppTopNav`** / **`MobileTopBar`** hide **Log a visit** + bell when signed out, show **Join the waiting list** (mobile **Join list** + **Log in**); **`Index`** **`Landing`** adds top-right waitlist button. **`docs/DATA_CONTRACT.md`** waitlist section; **`types.ts`** **`waitlist_signups`**. Apply migration in SQL Editor before production inserts. **`npm run typecheck`**, **`lint`**, **`build`**, **`Index.test`** green.
- **Trip logistics markers — Google primary type icons (2026-04-28):** Migration **`20270864000000_collection_markers_google_primary_type.sql`** adds **`google_primary_type`**; **`src/features/collections/markerPlaceDisplay.ts`** (**`pickGooglePrimaryTypeForStorage`**, **`mapGoogleTypesToCollectionCategory`**, **`getCollectionMarkerLucideIcon`**); Places insert stores primary type; map pins (**`MapMarkers`** / **`marker_google_primary_type`**), **`CollectionMarkerCard`**, and add-dialog list use distinct icons (e.g. bakery **Croissant** vs restaurant **Utensils**). **`docs/DATA_CONTRACT.md`**; **`types.ts`** / **`plano-tables.types.ts`**; **`tests/unit/markerPlaceDisplay.test.ts`**. **`npm run typecheck`**, **`lint`**, **`build`** green. Apply migration in Supabase before production deploy.
- **Collection map share preview / WhatsApp OG (2026-04-28):** **`CollectionMapPage.loader.ts`** SSR loader + **`meta()`** on **`/:username/map/:slug`** — **`og:image`** from first **`collection_items`** building (**`hero_image_url`** then **`community_preview_url`** via **`getBuildingImageUrl`**), else **`https://plano.app/cover.jpg`**; canonical + Twitter large card; **`noindex`** when not public. **`collectionStructuredData`** optional **`listUrl`** for real map URL. **`og-tags`** Edge Function branch for same path (public only). **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Collection itinerary segment popovers — destination photo (2026-04-28):** **`ItineraryList`** **`ItinerarySegment`** popovers (between stops) show the **next** stop’s image: buildings via **`getBuildingImageUrl(hero_image_url || community_preview_url)`** from the itinerary store; markers via **`useGooglePlacePhotos`** map passed as **`markerPlacePhotos`** from **`CollectionMapPage`**. Read-only hover opens the popover when the destination has a photo even if duration/instructions are empty. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Feed PYMK rail layout (2026-04-28):** **`UserRow`** — optional **`layout="stacked"`** (identity row then actions); **`PeopleYouMayKnow`** — **`userRowLayout`** prop + stacked loading skeleton; **`FeedRightRail`** passes **`userRowLayout="stacked"`**. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Add to Collection dialog (2026-04-28):** **`AddBuildingsToCollectionDialog`** — **Other markers** tab: **`ScrollArea`** on loaded content so the tab scrolls inside **`80vh`**; place search (**`PlacesAutocompleteFields`**) above **Places on this map**; **`PlacesAutocompleteFields`** no longer uses **`flex-1`** so content height drives scroll. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Collection map Trip Logistics notes (2026-04-28):** **`CollectionMarkerCard`** — same **Add note** / textarea pattern as **`CollectionBuildingCard`** when **`onUpdateNote`** + edit rights; **`CollectionMapPage`** Trip Logistics accordion passes **`handleUpdateMarkerNote`**; **`ItineraryList`** / **`SortableItineraryItem`** pass **`onUpdateMarkerNote`** for itinerary marker stops + optimistic **`markerDetails`**. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Explore UX (2026-04-28):** **`useDiscoveryFeed`** — always pass **`p_typology_ids`** / **`p_attribute_ids`** / **`p_architect_ids`** / **`p_credit_roles`** / **`p_contact_user_ids`** / **`p_building_statuses`** (empty **`[]`** when unset) so **`get_discovery_feed`** is not ambiguous vs a legacy 4-arg overload. Migration **`20270863000000_drop_legacy_get_discovery_feed_overload.sql`** drops **`get_discovery_feed(uuid,int,int,text)`**. **`Explore.tsx`** — **`Escape`** navigates home (**`/`**) when the location sheet is closed. **`DiscoveryCard`** — right-swipe wash uses **`bg-feedback-success`**; save stamp is white bookmark on **`rounded-full bg-feedback-success`** + **`shadow-card-elevated`**. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Explore discovery feed filters (2026-04-28):** Migration **`supabase/migrations/20270862000000_discovery_feed_city_contact_media.sql`** — **`get_discovery_feed`**: viewer **`NOT EXISTS`** only without contact IDs; city filter via **`strpos`** on **`city`**/**`address`**; relaxed media **`HAVING`** when **`p_contact_user_ids`** set. Prior **`20270861000000_…`** subsumed by 620. **`Explore.tsx`** — **`extractLocationDetails`** for geocoder picks. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Explore discovery feed (2026-04-28):** **`useDiscoveryFeed`** — normalize RPC **`data`** to an array (avoid **`null`/non-array** → **`buildings.length`** runtime throw and TanStack **`error`** state); drop **`profiles.first_name`/`last_name`** from review-image and interaction embeds (columns removed from **`profiles`** per gen-types). **`useBuildingImages`** — same profile select trim. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Map markers vs map controls (2026-04-28):** **`MapMarkers`** — cap **`Marker`** stack z-index (**38** base, **39** hover) so pins stay below **`CollectionMapGL`** / **`PlanoMap`** chrome (**`z-40`** / **`z-60`**); tier ordering preserved via **`Math.min(pinStyle.zIndex, 38)`**. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Collection map — spurious refresh (2026-04-28):** **`CollectionMapPage`** — `initializeItinerary` only runs when **`itinerarySourceFingerprint`** (collection id + itinerary JSON + item/markers fields incl. coords) changes; avoids resetting **`useItineraryStore`** on TanStack Query refetches with new array references. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Collection detail map vs header (2026-04-28):** **`CollectionMapPage`** — **`md:h-[100dvh]`** replaced with **`md:h-full flex-1 min-h-0`** so the split layout fits **`SidebarInset`** below **`AppTopNav`** (no extra viewport overflow / scroll under chrome); **`AppLayout`** **`isFullScreen`** adds **`min-h-0`**. **`CollectionMapGL`** — Satellite / expand / **Add in view** stack at **`z-40`** (below site header **`z-50`**). Mobile **Itinerary** chip **`z-40`**. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Home feed load performance (2026-04-28):** **`Index.tsx`** — gate initial shell on **`showFeedLoader`** (social **`isLoading`**, or empty social + attendance pending); defer collections/suggested queries until **`!socialFeed.isLoading`**; memoized **`buildActiveFeedNodes`**. **`useFeed`** / **`useSuggestedFeed`** — **`useCallback`** like handlers; attendance query **`staleTime: 60s`**. **`Index.test.tsx`** — assert **`feed-card-r1`**. **`npm run build`** + **`typecheck`** + **`lint`** green.
- **Mobile responsiveness roadmap Phase 3 Polish (2026-04-28):** **`src/index.css`** — utility **`overflow-x-scroll-touch`** (`-webkit-overflow-scrolling: touch` + hidden scrollbars) for horizontal strip scrollers. **Typography (mobile cap ~`text-3xl`):** **`Profile`**, **`ArchitectureHub`**, **`Connect`**, **`LandingHero`**, **`ExploreTutorial`**, **`DiscoveryCard`** rating numerals, **`GuidesPage`**, locality/country/person/company/event heroes; **Spacing:** **`FeedHero`** **`px-4 md:px-6`**, **`CollectionsGrid`** empty state **`px-4`**, **`Explore`** filter sheet **`px-4 md:px-6`**. **Scrollers:** **`BuildingDetails`**, **`Profile`** tabs/highlights/kanban/list, **`ManageFavoritesDialog`**, **`EventDetail`** related buildings, **`ExploreTeaserBlock`** — migrated to **`overflow-x-scroll-touch`**. **`docs/ROADMAP.md`** Phase 3 **[x]**. **`npm run build`** + **`npm run typecheck`** + **`npm run lint`** green.
- **Mobile responsiveness roadmap Phase 2.16 Auth & Onboarding (2026-04-28):** **`Auth.tsx`** / **`Onboarding.tsx`** — **`min-h-dvh`**, **`safe-area-pt`** / **`safe-area-pb`**, **`justify-start md:justify-center`**, **`overflow-y-auto`** for short phones; tighter mobile padding (**`p-6`**, **`gap-4`**, **`mb-4`** logo); headings **`text-2xl md:text-3xl`**; primary CTAs **`h-11`**; password visibility **`min-h-11 min-w-11`** + **`aria-label`**; onboarding avatar **`h-20 md:h-24`**, step dots narrower on mobile, **`Change Photo`** **`min-h-11`**. Confirmed **`index.css`** **`input`/`textarea`/`select`** **`font-size: 16px !important`**. **`docs/ROADMAP.md`** §2.16 **[x]**.
- **Mobile responsiveness roadmap Phase 2.15 Collection Map (2026-04-28):** **`CollectionMapGL`** — **`marginBottom: env(safe-area-inset-bottom)`** on **`GeolocateControl`** + **`NavigationControl`** so zoom/location controls clear mobile overlay chrome; **`CollectionMapPage`** — **Add in view** uses **`bottom-[calc(5.5rem+env(safe-area-inset-bottom,0px))]`** (aligned with Explore spacing above **`BottomNav`**). **`CollectionSettingsDialog`** Map tab — scroll area **`pb-[calc(2rem+env(safe-area-inset-bottom,0px))]`**; **Saved Places** rating/list **`ToggleGroup`** items **`min-h-[44px]`** on small screens. **`docs/ROADMAP.md`** §2.15 **[x]**. **`npm run build`** + **`npm run typecheck`** + **`npm run lint`** green.
- **Mobile responsiveness roadmap Phase 2.13 Guides (2026-04-28):** **`GuidesPage.tsx`** — wrapped in **`AppLayout`** so mobile gets **`pb-20`** clearance + **`BottomNav`** + site footer (aligned with Architecture Hub); continent filter tabs **`inline-flex min-h-[44px] items-center justify-center px-3`**. **`docs/ROADMAP.md`** §2.13 **[x]**. **`npm run build`** + **`npm run typecheck`** + **`npm run lint`** green.
- **Mobile responsiveness roadmap Phase 2.12 Locality Page (2026-04-28):** **`LocalityPage.tsx`** — **`FilterChip`** **`min-h-[44px]`** + **`px-3 py-2`** + **`inline-flex`** centering; **`QuickActions`** links **`px-3`** on mobile (was **`px-1`**); **`CollectionPreviewMosaic`** — full-width primary image below **`md`**, **`md+`** keeps **`2fr / 1fr`** mosaic. **`docs/ROADMAP.md`** §2.12 **[x]**. **`npm run build`** + **`npm run typecheck`** + **`npm run lint`** green.
- **Collection map — add visible saved places (2026-04-28):** With **Show Saved Places** on and edit permission, **`CollectionMapPage`** shows **Add in view (n)** — bulk **`collection_items`** insert for saved-place candidates matching dot + visited/pending filters **and** the current map viewport (**`isLngLatInBounds`** on **`Bounds`** from **`CollectionMapGL`** **`onViewportBoundsChange`**). **`CollectionMapGL`** reports bounds on load, move end, and after programmatic fit.
- **Mobile responsiveness roadmap Phase 2.11 Architecture Hub (2026-04-28):** **`ArchitectureHub.tsx`** — hero **`py-12 md:py-24`**; stats strip **`gap-2 sm:gap-4 md:gap-8`** + stat numbers **`text-3xl md:text-4xl`**; **`HydrateFallback`** skeleton aligned. **`docs/ROADMAP.md`** §2.11 **[x]**. **`npm run build`** + **`npm run typecheck`** + **`npm run lint`** green.
- **Collection Settings — Saved Places notice (2026-04-28):** **`CollectionSettingsDialog`** Map View — when **Show Saved Places** is on, informational **`Alert`** reminds users that Saved Places are map suggestions only until added to the collection. **`CollectionMapPage`** — removed duplicate **`matchesSavedPlacesStatusFilter`** definition (restores **`tsc`**).
- **Mobile responsiveness roadmap Phase 2.9 Company Details (2026-04-28):** **`CompanyDetails.tsx`** — **Additional credits** **`CollapsibleTrigger`** **`min-h-[44px]`**; steward **Remove** **`min-h-[44px] min-w-[44px]`**; role filter **`SelectTrigger`** **`h-11`**. **`docs/ROADMAP.md`** §2.9 **[x]**. **`npm run build`** + **`npm run typecheck`** green; **`npm run lint`** fails on pre-existing **`no-console`** elsewhere.
- **Mobile responsiveness roadmap Phase 2.8 Person Details (2026-04-28):** **`PersonDetails.tsx`** — **Additional credits** **`CollapsibleTrigger`** **`min-h-[44px]`**; **Claim this profile** uses **`size="default"`** (replacing **`sm`**) for a taller tap target. **`docs/ROADMAP.md`** §2.8 **[x]**. **`npm run build`** + **`npm run typecheck`** green; **`npm run lint`** per repo baseline (pre-existing **`no-console`** if unchanged).
- **Mobile responsiveness roadmap Phase 2.7 Event Detail (2026-04-28):** **`EventDetail.tsx`** — article **`pb-20 md:pb-12`**; hero cover **`h-[240px]`** on small screens, **`md:h-[clamp(260px,48vh,500px)]`** above breakpoint (skeleton aligned); RSVP / Log in / Recommend / Read more / external link **`min-h-[44px]`** where needed + **`active:`** color parity with **`hover:`**; building chips **`active:bg-surface-muted`**; organiser/submitter **`Link`** **`active:underline`**. **`docs/ROADMAP.md`** §2.7 **[x]**. **`npm run build`** + **`npm run typecheck`** green; **`eslint`** on **`EventDetail.tsx`** green; full-repo **`npm run lint`** still fails on pre-existing **`no-console`**.
- **Mobile responsiveness roadmap Phase 2.6 Events list (2026-04-28):** **`EventCard`** — full-card link already provides a large target; added **`min-h-11`**, **`touch-manipulation`**, and **`active:bg-surface-muted`** for 44px floor + touch feedback. Bottom padding for **`Events`** page comes from **`AppLayout`** (`pb-20 md:pb-0`). **`docs/ROADMAP.md`** §2.6 **[x]**. **`npm run typecheck`** + **`npm run build`** green; **`EventCard.tsx`** eslint clean; full-repo **`npm run lint`** still fails on pre-existing **`no-console`**.
- **Mobile responsiveness roadmap Phase 2.5 Search (2026-04-28):** **`SearchPage.tsx`** — mobile floating search bar **`left-4 right-4`** (was **`left-14`**) for wider field at 320px; List/Map toggle **`h-11`** (44px); toggle wrapper **`bottom-[calc(2rem+env(safe-area-inset-bottom,0px))]`** above home indicator. **`docs/ROADMAP.md`** §2.5 **[x]**. **`npm run typecheck`** + **`npm run build`** green; **`npm run lint`** fails on pre-existing **`no-console`** outside touched files.
- **Mobile responsiveness roadmap Phase 2.4 Profile (2026-04-28):** **`Profile.tsx`** — visited/saved toolbar: **`flex-wrap`** layout, full-width search row on small screens, **`min-h-11`** search + map control + toggle targets; **`FollowButton`** / followers-dialog follows; header **Sign out** tap area; **`EditorialBuildingCard`** touch-safe hover parity (`[@media(hover:none)]`); squad links **`active:`**. **`docs/ROADMAP.md`** §2.4 **[x]**. **`npm run typecheck`** + **`npm run build`** green; **`eslint`** on **`Profile.tsx`** green; repo **`npm run lint`** still fails on pre-existing **`ConsoleErrorInterceptor`** **`no-console`**.
- **Mobile responsiveness roadmap Phase 2.2 Explore (2026-04-28):** **`Explore.tsx`** — location pill **`min-h-11`**, clearer label truncation at **`max-w-[120px]`** on narrow widths; clear control **`min-h-11 min-w-11`** + **`active:`** feedback; scroll-driven filter bar uses **hysteresis** (**show ≤28px** / **hide ≥80px**) so snap/elastic scroll does not flicker the overlay; **`DiscoveryCard`** building title **`text-3xl sm:text-4xl md:text-5xl`** + link **`active:`** state for touch. **`docs/ROADMAP.md`** §2.2 **[x]**. Scoped eslint on touched files green; full-repo **`npm run lint`** still fails on pre-existing **`no-console`** elsewhere.
- **Mobile responsiveness roadmap Phase 2.1 Feed (2026-04-28):** **`Index.tsx`** — collections strip **`grid-cols-2 sm:grid-cols-3`**; horizontal padding **`px-4 md:px-6 lg:px-16`** (cold-start + active feed); load-more retry buttons **`text-xs`** instead of **`text-[11px]`**. **`docs/ROADMAP.md`** **`### [x] 2.1 Feed`** + subsection **`[ ]`** markers for **2.2–2.16**; Phase 2 heading tracks progress via subsections. **`npm run build`** + **`npm run typecheck`** green; **`npm run lint`** fails on pre-existing **`no-console`** outside touched files.
- **Mobile responsiveness roadmap Phase 1 (2026-04-28):** `docs/ROADMAP.md` Phase 1 **[x]** — `AppLayout` already had `pb-20 md:pb-0`; removed duplicate bottom padding from **Settings**; **Button** `size="sm"` / `"icon-sm"` use ≥44px min tap height on viewports below `md`; **BuildingDetails** editorial stream gradients + **Profile** masonry overlays use `[@media(hover:none)]` / focus-within so photo affordances work on touch; **Profile** edit control + clear-search + map shortcut + follower counts tuned for touch; **MasonryPhotoGrid** `columns-2 md:columns-3` + token `text-2xs-plus` caption. `npm run build` + `npm run typecheck` green; full-repo `npm run lint` still fails on pre-existing **`no-console`** (unchanged files).
- **Search map pins — discovery vs library tiers (2026-04-28):** **`getPinStyle`** (`pinStyling.ts`) — Discovery: **`Top 1%` → S**, **`Top 5%` → A**, **`Top 20%`** + legacy **`Top 10%` → B**, **`Standard` / `Top 25%` / other → C**. My Library: personal rating **3 / 2 / 1 / Rest** unchanged (maps to S / A / B / C). **`calculateTierRank`** in **`useMapData`** aligned (library: distinct **2** vs **1** star; discover: **5%** middle band vs **10%/20%** lower). Vitest **`pinStyling.test`** updated.
- **Explore — tutorial vs immersive top chrome (2026-04-28):** **`ExploreShellContext`** + **`MainLayout`** — on **`/explore`**, **`MobileTopBar`** + **`AppTopNav`** render only while the intro tutorial is active; after dismiss (or if **`explore-tutorial-seen`**), horizontal top chrome hides for a full-bleed feed above **`BottomNav`**. **`Explore.tsx`** syncs chrome via **`useLayoutEffect`**; **`showTutorial`** is **`null`** until client reads **`localStorage`** (safe default: keep top chrome). **`ExploreTutorial`** uses **`belowAppTopChrome`** (positioned under shell header, above bottom tab bar). **`npm run typecheck`** + **`npm run build`** green; scoped **`eslint`** on touched files green; full repo **`npm run lint`** still fails on pre-existing **`no-console`** elsewhere.
- **Add to collection modal — preview fallback (2026-04-28):** **`AddBuildingsToCollectionDialog`** — `user_buildings` → `buildings` select includes **`community_preview_url`**; list thumbnails + **`BuildingDetailPanel`** use **`hero_image_url || community_preview_url`** (via **`main_image_url`** / panel **`hero_image_url`** chain). **`searchBuildingsRpc`** (`supabaseFallback`) selects **`community_preview_url`** and sets **`main_image_url`** to **`hero_image_url ?? community_preview_url`**. **`DiscoveryBuilding`** documents optional **`community_preview_url`**. **`npm run typecheck`** + **`npm run build`** green; repo **`npm run lint`** still fails on pre-existing **`no-console`** in other files.
- **Collection map — saved places dot filter (2026-04-28):** **`CollectionSettingsDialog`** — when **Show Saved Places** is on, **Show by rating** toggle group (**All** vs 1/2/3 dots); **`CollectionMapPage`** loads **`user_buildings.rating`**, maps to **`personal_rating`**, filters candidates client-side; preference persisted per user+collection next to the existing show toggle.
- **Collection map — saved places status filter (2026-04-28):** **`SavedPlacesStatusFilter`** (**All** / **Visited** / **Bucket list** ↔ **`user_buildings.status`** **`visited`** / **`pending`**); **`CollectionSettingsDialog`** **Show by list** toggle group + **`localStorage`** per user+collection; **`CollectionMapPage`** applies **`matchesSavedPlacesStatusFilter`** together with the dot filter; candidate rows set **`personal_status`**. **`npm run typecheck`** + **`npm run build`** green; **`eslint`** on touched collection files green.
- **Collection map — Show Saved Places + settings save (2026-04-28):** **`CollectionMapPage`** — removed **`window.location.reload()`** from settings **`onUpdate`** (replaced with TanStack **`invalidateQueries`** for collection, items, members, stats); persisted **Show Saved Places** per user+collection in **`localStorage`** with **`useLayoutEffect`** hydration so toggles survive save and full reloads.
- **Feed right rail PYMK (2026-04-28):** Single section label **People you may know** in **`FeedRightRail`**; **`PeopleYouMayKnow`** uses shared **`UserRow`** in a vertical list (`maxSuggestions={3}` in rail, default 5 inline), optional **`showHeading`** for inline placements. **`npm run typecheck`** clean; repo **`npm run lint`** still fails on pre-existing **`no-console`** in other files.
- **PYMK / user rows — flex truncation (2026-04-28):** **`UserRow`** — middle column **`flex-1 min-w-0 overflow-hidden`** so display names and mutual copy keep usable width beside dismiss + **`FollowButton`**; outer row **`gap-3`**; dismiss control **`gap-3`** from Follow, larger tap target + **`focus-visible`** ring; **`MutualFacepile`** — root **`min-w-0 w-full`**, avatar stack **`shrink-0`**, mutual text **`title`** for hover full string. **`npm run typecheck`**, **`npm run build`**, **`npm run lint`** green.
- **SubmitEvent edit — DOM crash guard (2026-04-16):** **`SubmitEvent.tsx`** — edit mode hydrates form + address + building chips in **`useLayoutEffect`** with **`editHydratedKey`** (`slug:id`); blocks the main form (spinner) until hydration matches so **`LocationInput`** / cmdk never mount on a frame where **`event` is loaded but local state is still empty** (mitigates **`removeChild` / NotFoundError** + root **`ErrorBoundary`** “Something went wrong”). **`LocationInput`** gets a stable **`key`** per create vs edit slug. **`eslint`** clean on touched file; repo **`npm run typecheck`** still fails on pre-existing errors (**`eventsApi` `locality_id`**, **`contributors.ts`**, etc.).
- **Building detail + edit layout (2026-04-14):** **`BuildingDetails`** — removed inner **`max-w-4xl`** wrappers (identity + stream sections) and **`HydrateFallback`** max width; **`EditBuilding`** — form column **`w-full min-w-0`** with existing padding (no **`max-w-2xl`**). **`npm run lint`** + **`npm run build`** green; **`npm run typecheck`** fails on pre-existing **`contributors.ts`** (**`building_id`** / **`approved`** vs generated types) — unchanged by this task.
- **Events additions Phase 4 Task 4.97 — Autonomous build & integrity (2026-04-14):** **`npm run typecheck`**, **`npm run lint`**, **`npm run build`** — all exit **0**. **`grep`** on Phase 4 migrations **`20270845000000_recommendations_add_event_id.sql`** / **`20270846000000_notifications_add_metadata.sql`** — no **`auth.uid()`** (RLS unchanged on those files). Feed slice: no **`console.log`**; no raw default Tailwind palette classes in **`FeedEventAttendanceRow`**. **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task **4.97** **[x]**.
- **Events additions Phase 4 Task 4.6 — Attendance feed cards (2026-04-14):** **`FeedEventAttendance`** / **`FeedHomeEntry`** in **`src/types/feed.ts`**; **`loadFeedEventAttendance`** in **`useFeed`** (follows → **`event_attendances`** + **`events!inner`**, profiles batch, cluster by **`eventId`**, **`staleTime: 0`**); **`mergeAggregatedFeedWithEventAttendance`** + **`aggregatedItemTime`** in **`feed-aggregation.ts`**; **`FeedEventAttendanceRow`**; **`ReviewCardFeed`** / **`Index`** merge + cold-start when no reviews **and** no attendance; **`Index.test`** mock extended. **`docs/ROADMAP.md`** Task **4.6** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`**, **`Index.test`** green.
- **Events additions Phase 4 Task 4.5 — Notification copy for event recommendations (2026-04-14):** **`Notifications.tsx`** — **`metadata.event_slug` / `event_title`** on **`recommendation`** rows: **`@{actor}`** + linked title + **to you**; **`handleNotificationClick`** → **`/events/:slug`** when slug present; building **`visit_with`** + building recommend branches unchanged. **`NotificationSettingsDialog`** — label **Recommendations & invites**. **`FeedResolvedEntry`** — scoped **`TODO`** for event-backed feed cards. **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task **4.5** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events additions Phase 4 Task 4.4 — RecommendDialog event mode (2026-04-14):** **`RecommendDialog`** — `mode` **`building`** (default) vs **`event`**, **`intent`** **`recommend`** / **`visit_with`** (replaces overloaded visit flag); event preview + **Mark me as going too** checkbox; inserts **`recommendations`** with **`event_id`** + **`notifications.metadata`** `{ event_slug, event_title }`; optional **`event_attendances`** update/insert (errors swallowed, no toast); invalidates **`eventKeys.detail`** + **`eventKeys.profile(…, attending)`**. **`EventDetail`** — **Recommend** control (signed-in opens dialog; signed-out → **`/login?redirect=`**), **`eventDtoToCardDto`**. **`docs/ROADMAP.md`** Task **4.4** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events additions Phase 4 Task 4.3 — Profile events UI (2026-04-14):** **`EventProfileCard`** + skeletons; **`ProfileEventsSection`** (**`SegmentedControl`** Organising / Attending / Interested with counts; own-profile **Past events** **`Accordion`** + **`usePastAttendanceCount`** / **`usePastAttendanceEvents`**); **`Profile.tsx`** renders the section at the foot of the main profile content when any segment has rows; **`profileEventsApi.fetchProfilePastAttendanceCount`**; **`eventKeys.profilePast`** / **`profilePastCount`**; **`SegmentedControl`** option labels accept **`ReactNode`**. **`docs/ROADMAP.md`** Task **4.3** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events additions Phase 4 Task 4.2 — `useProfileEvents` data layer (2026-04-14):** **`useOrganisingEvents`** / **`useAttendingEvents`** / **`useInterestedEvents`**, **`useProfileEventsCountBadges`**; **`eventKeys.profile(userId, segment)`**; **`profileEventsApi`** + **`hydrateEventRowsToCards`** in **`eventsApi`**. Organising uses **`useClaimedPersonForNav`** / **`useStewardCompaniesForNav`** when **`userId`** is the signed-in user, else **`getClaimedPersonSummaryForProfile(userId)`**; steward company **`IN`** filter only for self (RLS cannot list another user’s stewardships). **`docs/ROADMAP.md`** Task **4.2** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events additions Phase 4 Task 4.1b — `notifications.metadata` migration (2026-04-14):** Added **`supabase/migrations/20270846000000_notifications_add_metadata.sql`** (`metadata jsonb`, nullable, **`IF NOT EXISTS`** for idempotency). **`docs/ROADMAP.md`** Task **4.1b** **[x]**. **Manual:** run the migration in the Supabase SQL Editor, then **`npm run gen-types`** so hosted types stay aligned.
- **Events additions Phase 4 Task 4.1 — `recommendations.event_id` migration (2026-04-14):** Added **`supabase/migrations/20270845000000_recommendations_add_event_id.sql`** (`event_id` FK to **`events`**, **`recommendations_single_target_check`**, **`idx_recommendations_event_id`**, **`building_id`** nullable). Synced **`docs/DATA_CONTRACT.md`** + **`src/integrations/supabase/types.ts`** (`recommendations` row/insert/update + **`events`** relationship). **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task **4.1** **[x]**. **Manual:** run the migration in Supabase (SQL Editor) before relying on the new column in production.
- **Events additions Phase 4 Task 4.0 — tech-debt & stability (2026-04-14):** Re-read **KNOWN_ISSUES**; no code. **`npx tsc --noEmit`** (root **`tsconfig.json`**) — pre-existing **TS6305** (composite / emit artifact; unchanged from Task 1.0). **`npm run typecheck`** (**`tsc --noEmit -p tsconfig.app.json`**) — **clean**. **`npm run gen-types`** — Supabase CLI returned **access-control / insufficient privileges** for project **`lnqxtomyucnnrgeapnzt`** from this environment; **`src/integrations/supabase/types.ts`** not modified — rerun **`gen-types`** from a logged-in / permitted environment before schema-heavy work if needed. **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task **4.0** **[x]**.
- **Events — Event detail SSR sidebar + building card images (2026-04-14):** **`app/routes.ts`** — moved **`/events`** routes inside **`MainLayout`** (was standalone, no **`AppSidebar`** on SSR); **`eventsApi.getEventBySlug`** — nested **`buildings`** select includes **`community_preview_url`**, **`mapBuildings`** uses **`hero_image_url ?? community_preview_url`** for **`getBuildingImageUrl`** (parity with collection cards). **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events — SubmitEvent address suggestions + cover upload (2026-04-14):** **`useAutocompleteSuggestions`** maps **`(addresses)`** to venue/street primary types; **`SubmitEvent`** uses it for **Where**; cover **Choose image** → **`event-covers`** S3 prefix via **`generate-upload-url`**, public URL in form; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events roadmap Task 1.97 — Phase 1 build & integrity (2026-04-14):** Ran **`npm run typecheck`** (**`tsc --noEmit -p tsconfig.app.json`**) — clean; **`npm run lint`** — clean; **`npm run build`** — clean; client manifest includes events chunks (**`SubmitEvent`**, **`EventDetail`**, **`Events-`**). **`src/features/events/**`** — no **`console.log`**; no raw default Tailwind palette utility classes. **`20270844000000_add_events.sql`** — only **`(SELECT auth.uid())`** form for auth. Root **`tsc --noEmit`** (default **`tsconfig.json`**) still **TS6305** composite / emit artifact issue — app gate remains **`tsconfig.app.json`** (unchanged from Task 1.0). **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task **1.97** **[x]**.
- **Events roadmap Task 1.6 — Navigation + edit route (2026-04-14):** **`AppSidebar`** / **`BottomNav`** — **Events** + **`CalendarDays`**, between Feed and Explore; active state for **`/events`** prefix. **`App.tsx`** — **`/events/:slug/edit`** → **`SubmitEvent`** before **`/events/:slug`**. **`app/routes.ts`** — same order + **`{ id: 'events-submit-new' | 'events-submit-edit' }`** (fixes duplicate module route id). **`SubmitEvent`** — edit mode (`pathname` **`/edit`** + **`slug`**), **`getEventBySlug`** + hydrate, submitter-only redirect, **`useUpdateEvent`** (preserves org/person organiser when not self-hosted; clears self-hosted → unclaimed), login **`redirect`** for edit path. **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task **1.6** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events roadmap Task 1.5 — Event detail `/events/:slug` (2026-04-14):** **`useEvent.ts`** — `useQuery` + **`getEventBySlug`**, **`staleTime: 0`**, **`eventKeys.detail`**. **`EventDetail.tsx`** — full-page skeleton, not-found copy aligned with person detail 404, hero **`h-[260px]`** + placeholder, **`font-display text-3xl font-black`** title, **`enGB`** date range, address + **`MapPin`**, organiser / unclaimed submitter + no-op **Claim this event →** **`span`**, building chips → **`getBuildingUrl`**, description **Read more** >300 chars, external CTA, **Edit** for submitter → **`/events/:slug/edit`**, **`MetaHead`** + canonical. Routes: **`app/routes.ts`** + **`App.tsx`** after **`/events`** ( **`lazyWithRetry`**). **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task **1.5** **[x]**; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Events roadmap Task 1.4 — Events listing `/events` (2026-04-14):** `Events.tsx` — `useInfiniteQuery` + `getUpcomingEvents` + `useIntersectionObserver` (200px root margin), empty state + 3× `EventCardSkeleton`, **Share an event** → `/events/new`, `MetaHead` **`documentTitle`** `Events · Plano` + canonical `/events`. `EventCard.tsx` — cover/placeholder **`h-[180px]`** **`w-[120px]`**, title **`text-base font-black`** `line-clamp-2`, **`enGB`** date line, organiser copy, **Claim** badge when unclaimed, whole-card **`Link`**. **`MetaHead`** optional **`documentTitle`** (full title without `| Plano` suffix). **`UPCOMING_EVENTS_PAGE_SIZE`** exported from **`eventsApi`**. Routes: **`app/routes.ts`** + **`App.tsx`** `/events`. **`docs/ROADMAP.md`** Task **1.4** **[x]**.
- **Events roadmap Task 1.3 — Submit event form (2026-04-14):** `SubmitEvent.tsx` + `useSubmitEvent.ts` + `schemas.ts` (`SubmitEventSchema`) + `utils/eventSlug.ts` (kebab title + random suffix, no `nanoid` dep). Inserts `events` / `event_buildings`, `getUser()` for submitter/organiser, `invalidateQueries(eventKeys.lists())`, navigate `/events/:slug`. UI: `AppLayout`, `Calendar` + time inputs, `LocationInput` + optional lat/lng, `search_buildings` building chips (max 20), Switch self-hosted, `Loader2` submit, inline errors. Routes: **`app/routes.ts`** + **`App.tsx`** — `/events/new`, **`/login`** → `Auth` (matches roadmap redirect). **`docs/ROADMAP.md`** Task **1.3** **[x]**.
- **Events roadmap Task 1.2 — Types, query keys, events API (2026-04-14):** Added `src/features/events/types.ts`, `queryKeys.ts` (`eventKeys`), `api/eventsApi.ts` (`getUpcomingEvents`, `getEventBySlug`, `getEventsByBuilding` with stable `{ code, message }` errors). `tests/unit/events-task12-types.test.ts`. `Database` extended with `events` / `event_buildings` + enums in `src/integrations/supabase/types.ts` — `npm run gen-types` failed (Supabase project access); types merged manually to match `20270844000000_add_events.sql`; re-run `gen-types` when access is restored. `docs/ROADMAP.md` + `docs/Roadmap.md` Task 1.2 checked.
- **Events roadmap Task 1.1 — `events` / `event_buildings` migration (2026-04-14):** Added **`supabase/migrations/20270844000000_add_events.sql`** — enums **`event_claim_status`**, **`event_attendance_status`**; tables, indexes (incl. GiST on **`location`**), RLS per roadmap (**`(SELECT auth.uid())`**, admin via **`profiles.role IN ('admin','app_admin')`**); **`submitted_by_user_id`** FK to **`auth.users`** without invalid **`ON DELETE SET NULL`** on a NOT NULL column (file header notes roadmap wording). **`docs/ROADMAP.md`** Task **1.1** **[x]** (**`docs/Roadmap.md`** already **[x]**). **Manual:** paste migration into Supabase SQL Editor, then **`npm run gen-types`** before Task **1.2**.
- **Events roadmap Task 1.0 — Tech-debt & stability check (2026-04-14):** Re-read **`KNOWN_ISSUES`**; root **`tsc --noEmit`** (default **`tsconfig.json`**) fails with **TS6305** (“output file … has not been built”) — app gate is **`npm run typecheck`** (**`tsc --noEmit -p tsconfig.app.json`**), **clean pass** on 2026-04-14. **`src/integrations/supabase/types.ts`** last modified **2026-04-11** ( **`gen-types`** targets hosted **`lnqxtomyucnnrgeapnzt`** ); run **`npm run gen-types`** again after Task **1.1** migration is applied. No code changes. **`docs/Roadmap.md`** Task **1.0** **[x]**.
- **App sidebar — account links in user menu (2026-04-14):** **`AppSidebar`** — **Notifications**, **Profile**, and **Settings** removed from the main sidebar list; **`UserMenu`** dropdown (footer username) now includes **Notifications** + **Profile** (with **Settings** retained); portfolio / company steward nav unchanged; **`AppSidebar.test`** still green; build, typecheck, lint green.
- **Building detail — Info summary links (2026-04-13):** **`BuildingDetails`** compact **Info** row — **View info →** (`#building-info-extended` when that section renders), **Edit info →** to **`/edit`** for editors (replaces pencil-only affordance there); typecheck + lint green.
- **Building detail — map height + no Location heading (2026-04-13):** **`BuildingDetails`** — map column **`h-48`** (was **`h-36`**); removed **Location** **`h2`**; typecheck + lint green.
- **Building detail — info layout + extended Info (2026-04-13):** **`BuildingDetails`** — compact **Info** above **Credits** in the left column; map column titled **Location**; bottom **`#building-info-extended`** section for catalogue detail + editor empty state; **`formatCatalogLabel`** for enum display; build, typecheck, lint green.
- **Building detail — hide hero without image (2026-04-13):** **`BuildingHero`** returns **`null`** when there is no loadable image (missing **`src`** or **`onError`**); **`BuildingDetails`** mounts **`BuildingHero`** only when **`heroImageUrl`** is truthy so identity block sits higher with no gray bar; build, typecheck, lint green.
- **Roadmap Card System Task 4.5 — `DetailSectionHeader` count (2026-04-13):** **`BuildingDetails`** — **`detailSectionContributionCount`** = entries where **`resolveCardType(buildingEntryToFeedReview(…)) !== 'activity'`** (reviews + photo/video rows; excludes pure visited/pending activity); **`DetailSectionHeader`** uses this instead of **`streamBlocks.length`** (orphan tiles no longer inflate the count); **`docs/ROADMAP.md`** Task 4.5 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 4.4 — `PointsBadge` placement (2026-04-13):** Audited feed A/B (below sub-label; C omits); **`DetailCardWithMedia`** / **`DetailCardNoMedia`** now render **`PointsBadge`** below hairline and above **`DetailCardTextTreatmentBlock`** when **`rating`** is set and **> 0**; **`rating` 0/null** suppressed (matches feed + **`PointsBadge`**); **`docs/ROADMAP.md`** Task 4.4 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 4.3 — detail timestamps month/year (2026-04-13):** **`DetailCardWithMedia`** / **`DetailCardNoMedia`** already used **`format(..., 'MMMM yyyy')`**; **`DetailCard`** has no date formatting; **`DetailCardA`**/**`B`**/**`C`** (playground) now use the same **`format`** instead of **`formatDistanceToNow`**; **`docs/ROADMAP.md`** Task 4.3 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 4.2 — responsive / mobile pass (2026-04-13):** **`DetailCardWithMedia`** — `grid-cols-1 md:grid-cols-2`, stacked hero + **`px-0 py-6`** text block on small screens, **`matchMedia`** row height on **`md+`**, **`truncate`** @username with **`md`** reset; overflow strip **`max-w-full`** + **`overflow-x-hidden`**; **`DetailCardNoMedia`** / single-media hero **`py-6`** + truncated name; **`FeedCardB`** mobile text column **`py-6`**; **`ActivityLead`** **`line-clamp-1`**; **`FeedActivityRow`** **`gap-2 md:gap-4`**; **`docs/Roadmap.md`** Task 4.2 **[x]**; test, build, typecheck, lint green.
- **Roadmap Card System Task 4.1 — deprecated cleanup verify (2026-04-13):** Confirmed **`ReviewCardFeed`** thin dispatcher (no **`spec`** / **`prominenceOverride`** / **`variant`** / **`imagePosition`**); deprecated files already absent; renamed **`deriveLegacyFeedCardLayout`** → **`deriveLegacyFeedUi`** + **`deriveLegacyFeedUi.ts`** so **`grep '…CardLayout…' src`** is clean; **`docs/Roadmap.md`** Task 4.1 **[x]**; test, build, typecheck, lint green.
- **Roadmap Card System Task 3.4 — overflow photo grid (2026-04-13):** **`partitionDetailOverflowImages`** + Vitest; **`DetailCardWithMedia`** overflow grid, **`failedImages`** on overflow + full-width hero, single-media **`none`** full-width layout; **`BuildingDetails.gallery.test`** asserts **`Reviews & photography`**; **`docs/Roadmap.md`** Task 3.4 **[x]**; test, build, typecheck, lint green.
- **Roadmap Card System Task 3.3 — detail text treatments + footnote (2026-04-13):** **`DetailCardTextTreatmentBlock`** (quote with serif 56px mark, split **24px**/3 vs **32px**/4, body + **Read full review →**, **`none`** + **`formatDetailMediaMetadataLine`**), **`DetailCardFootnote`**, **`DetailCardNoMedia`** full layout, **`formatDetailMediaMetadataLine`** + Vitest; **`docs/ROADMAP.md`** Task 3.3 **[x]**; test (resolveCardType), build, typecheck, lint green.
- **Roadmap Card System Task 3.2 — `DetailCardWithMedia` first row + byline (2026-04-13):** Two-column grid (**`variant.photoColHeight`**), **`CardImage`** **`firstMediaOnly`**, text column padding **32px** top/bottom, **44px** left, §4.3 byline (no follow), hairline, stub slot for 3.3/3.4; **`docs/ROADMAP.md`** Task 3.2 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 3.1 — `DetailCard` scaffold (2026-04-13):** **`DetailCard`** + **`DetailCardWithMedia`** / **`DetailCardNoMedia`** stubs, **`DetailSectionHeader`**, **`BuildingDetails`** wires header (**`streamBlocks.length`**) + replaces A/B/C with **`DetailCard`**; **`docs/ROADMAP.md`** Task 3.1 **[x]**; build, typecheck, lint green.
- **SEO — `/connect` indexable (2026-04-13):** Removed **`robots: noindex`** from **`Connect.tsx`** (nav-linked public page); added canonical, description, OG/Twitter like home feed; **`scripts/seo-check.sh`** asserts **`/connect`** has no noindex. Build, typecheck, lint green.
- **Roadmap Card System Task 2.5 — `FeedActivityRow` (2026-04-13):** **`activityStatus`** / **`displayText`** props; **`@username`** via **`ActivityLead`** **`usernameWithAt`**; building title **`1.3125rem` (21px)** + **`city`** meta line; **`ReviewCardFeed`** + **`ProfileReviewCard`** pass **`activityStatus`**; **`FeedActivityCard`** / **`FeedCompactCard`** already absent from **`src/`**; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 2.5 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 2.4 — `FeedCardC` (2026-04-13):** Type C — **`CardImage`** **`firstMediaOnly`** + **`CARD_C_IMAGE_HEIGHT` 185**; text stack **`mt-4`** (16px below media, incl. when **`hideUser`**); **`BuildingHeadline`** **`md`** (28px / **`1.75rem`**, one line); removed **`feedC`** from **`BuildingHeadline`**; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 2.4 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 2.3 — `FeedCardB` (2026-04-13):** **`CardImage`** **`firstMediaOnly`** — Type B shows only the first media tile (no carousel / multi-tile); **`FeedCardB`** — fixed **`md:h-[320px]`** / **`max-h`**, text column **`md:py-[28px] md:pl-[40px]`**, alternating columns unchanged; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 2.3 **[x]**; build, typecheck, lint green.
- **Roadmap Card System Task 2.2 — `FeedCardA` §3.2 alignment (2026-04-13):** **`BuildingHeadline`** **`size="xl"`** (52px / `3.25rem`,2-line clamp) for Type A; removed unused **`hero`** size from **`BuildingHeadline`**; **`FeedCardA`** uses shared **`countWords`** from **`resolveCardType`**; layout/formatting unchanged (**`max-w-xl`**, 3-line clamp + **Read more →**, **`CardFooter`**); **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 2.2 **[x]**; build, typecheck, lint, test green.
- **Roadmap Card System Task 2.1 — `ReviewCardFeed` dispatcher (2026-04-13):** New **`ReviewCardFeed`** — **`resolveCardType`** switch, **`SuggestedContentBlock`** wraps A/B/C only, **`typeBAlternateIndex`**, **`showCommunityImages`**; **`FeedCardA`/`B`/`C`** strip inner suggested wrapper; **`useReviewCardData`** optional **`showCommunityImages`** (video poster + community fallback); **`FeedResolvedEntry`** → **`ReviewCardFeed`** (+ legacy **`index`** alias); **`ProfileReviewCard`** wraps A–C with **`SuggestedContentBlock`**; **`Index`** grid uses **`ReviewCardFeed`**; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 2.1 **[x]**; build, typecheck, lint, test green.
- **Roadmap Card System Task 1.2 — `resolveDetailVariant` + types (2026-04-13):** **`src/types/cards.ts`** — **`FeedCardType`**, **`DetailCardTextTreatment`**, **`DetailCardVariant`**; **`resolveCardType.ts`** — exported **`countWords`**, **`detailTextTreatmentFromWordCount`**, **`resolveDetailVariant`** (photo column **260/300/400** from **none/quote/body**; **`mediaCount`** = usable images + video); Vitest extended; **`docs/Roadmap.md`** Task 1.2 **[x]**; **`npm run`** test (resolveCardType), typecheck, lint, build green.
- **Roadmap Phase 5 Task 14 — simplify `useReviewCardData` (2026-04-12):** Hook is **`useReviewCardData(entry)`**; video **`poster`** = first user image or building hero only (no **`community_preview_url`**); removed unused **`posterUrl`** from **`ReviewCardData`**; **`FeedCard*`** / **`DetailCard*`** / **`FeedResolvedEntry`** dropped **`showCommunityImages`**; **`FeedActivityRow`** + **`ProfileReviewCard`** / kanban still take the toggle for row thumb + list tiles; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 14 **[x]**; **`npm run`** build, typecheck, lint, test (**643**) green.
- **Roadmap Phase 5 Task 13 — delete old feed card components (2026-04-12):** Removed `ReviewCardFeed.tsx` shim and barrel export; bookmark/title tests → `FeedResolvedEntry.test.tsx`; hero/compact/activity/detail/`resolveCardSpec`/`CardSpec` cleanup already done in prior card-system tasks; `docs/Roadmap.md` Task 13 **[x]**; `npm run` build, typecheck, lint, test green.
- **Roadmap Phase 4 Task 12 — building detail detail cards (2026-04-12):** **`BuildingDetails`** — all non-orphan stream blocks use **`buildingEntryToFeedReview`** + **`resolveCardType`** → **`DetailCardA`**/**`B`**/**`C`**; video-only reviews included in stream; **`entryImageIds`** excludes duplicate video orphans; architect score boost; **`docs/Roadmap.md`** Task 12 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap feed cards Task 11 — feed renderer wiring (2026-04-12):** `FeedResolvedEntry` + `ReviewCardFeed` re-export; `ActivityStreamGroup` → `FeedActivityRow` list; `ColdStartFeed` / `EmptyFeed` / `CardPlayground` / `ProfileReviewCard` activity path updated; `FeedActivityRow` optional `className`; removed legacy `ActivityStreamRow` module; `docs/Roadmap.md` Task 11 **[x]**; `npm run` typecheck, lint, test, build green.
- **Roadmap feed cards Task 10 — `DetailCardC` (2026-04-12):** Verified `src/features/feed/components/detail/DetailCardC.tsx` vs roadmap; attribution row uses `h-7` avatar, `min-w-0 flex-1 truncate` username + `text-2xs` verb/timestamp, `CardFooter` without bookmark; `docs/Roadmap.md` Task 10 **[x]** + shipped note; `npm run` build, typecheck, lint green.
- **Roadmap feed cards Task 9 — `DetailCardB` (2026-04-12):** Confirmed `src/features/feed/components/detail/DetailCardB.tsx` vs roadmap; restored full `DetailByline` hairline (removed `[&_hr]:hidden`); `docs/ROADMAP.md` Task 9 **[x]** + shipped note; `npm run` build, typecheck, lint green.
- **Roadmap feed cards Task 8 — `DetailCardA` (2026-04-12):** Verified `src/features/feed/components/detail/DetailCardA.tsx` vs roadmap (byline, border, points, clamp-5 + read-more, `CardFooter` without `buildingId`); `docs/Roadmap.md` + `docs/ROADMAP.md` Task 8 **[x]**; `npm run` build, typecheck, lint green.
- **Roadmap feed cards Task 7 — `DetailByline` (2026-04-12):** Confirmed `src/features/feed/components/detail/DetailByline.tsx` (44px avatar, badges, meta, `FollowButton`, hairline `hr`); meta tracking aligned with feed micro labels; `docs/Roadmap.md` + `docs/ROADMAP.md` Task 7 **[x]**; `npm run` build, typecheck, lint green.
- **Roadmap feed cards Task 6 — `FeedActivityRow` (2026-04-12):** Added `src/features/feed/components/FeedActivityRow.tsx` (48×48 thumb, `ActivityLead` + visited / wants to visit, building title, `CardBookmark` `hoverGroup="activity-row"`, row click → building); `docs/Roadmap.md` + `docs/ROADMAP.md` Task 6 **[x]**; `npm run` build, typecheck, lint green.
- **Feed cards roadmap v2 reconciliation (2026-04-12):** **`FeedCardA`/`B`/`C`** (replaced **`CardType*`**), **`card-primitives`** + **`CardBookmark`** / **`CardFooter`**, **`detail/DetailCard*`**; removed **`ReviewCardDetail`**; **`feed/index.ts`** exports **`DetailCard*`**; **`BuildingDetails`** — **`buildingEntryToFeedReview`** + **`resolveCardType`** + **`DetailCard*`** for **`image-review`/`image-only`/`text-only`** blocks (**`featured`/`mosaic`/orphans** legacy); **Also visited** via **`ActivityStreamGroup`** for rows with no text and no images; **`useReviewCardData`** — dropped **`variant`/`compact`** and **`carouselImages`**; optional **`showCommunityImages`**; **`CardImage`** / **`FeedPhotoCarousel`** wired via per-image **`likes_count`/`is_liked`** on **`ReviewCardMediaItem`**; **`ProfileReviewCard`**, **`CardPlayground`**, **`Index.test`** (`feed-card-a-r1`); **`ActivityStream`** JSDoc avoids **`@deprecated`** substring (QA 11.2). **`npm run`** typecheck, lint, test (**643**), build green.
- **Roadmap Card System Task 12 — remove deprecated card components (2026-04-12):** Removed **`FeedHeroCard`**, **`FeedCompactCard`**, **`FeedActivityCard`** and their Vitest files; **`ReviewCardFeed`** delegates via **`resolveCardType`** to **`CardTypeA`**/**`B`**/**`C`** or **`ActivityStreamGroup`**; **`ColdStartFeed`**, **`EmptyFeed`**, **`CardPlayground`** wired to the thin feed card; **`deriveLegacyFeedCardLayout`** comments scoped to fixture/playground regression; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 12 **[x]**; **`npm run`** build, typecheck, lint, test green.
- **Roadmap Card System Task 11 — remove resolveCardSpec (2026-04-12):** Deleted **`resolveCardSpec.ts`**; added **`deriveLegacyFeedCardLayout.ts`** for legacy **`ReviewCardFeed`** / **`FeedHeroCard`**; **`aggregateFeed`** attaches **`cardType?: CardType`** via **`resolveCardType`**; **`cardFixtures`** use **`expectedLayout`**; **`CardPlayground`** — legacy layout + **`resolveCardType`** panels, feed **card type override** (A/B/C/activity); **`src/test/setup.ts`** **`ResizeObserver`** stub; **`Index.test`** / **`ProfileKanbanView.test`** fixes; **`docs/ROADMAP.md`** Task 11 **[x]**; build, typecheck, lint, full Vitest green.
- **Roadmap Card System Task 10 — responsive & polish (2026-04-12):** **`CardImage`** **`FadeInImg`** + **`FeedPhotoCarousel`** — **`hover:scale-105`** + transform transition; **`CardTypeA`** — hide **Read more →** on viewports **≤767px** when copy is under **120** words; **`ActivityStreamRow`** — **40×40** thumb + **`text-lg`** title on mobile (**48×48** + **21px** at **`md+`**); **`CardTypeB`** / **`CardFooter`** bookmark pattern already **`opacity-100`** mobile / hover **`md+`**; **`docs/ROADMAP.md`** Task 10 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 9 (2026-04-12):** **`CardTypeA`**/**`B`**/**`C`** — **`hideUser`**, **`hideBuildingInfo`**, **`showCommunityImages`**; **`ProfileReviewCard`** + **`DraggableReviewCard`**/**`ProfileKanbanView`**/**kanban overlay**; **`ReviewCardDetail`** delegates to **`resolveCardType`** + **`ActivityStreamGroup`** + typed cards (byline + stream **`hideUser`** parity); profile Vitest mocks → **`ProfileReviewCard`**; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 9 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 8 (2026-04-12):** **`Index.tsx`** — social feed uses **`resolveCardType`** + **`CardTypeA`**/**`B`**/**`C`** + buffered **`ActivityStreamGroup`** for consecutive activity entries; shared **`cardBIndex`** for Type B alternation; **`onLike`**/**`onImageLike`** from social/discovery feeds; clusters/collection injects/discovery injects flush activity + use same spacing (**`border-b`** **`pb-10`** **`mb-10`**); **`collapseIntoRows`** no longer pairs two activity items; **`ActivityStream`** list uses **`divide-y`** to avoid double borders; **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 8 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 7 (2026-04-12):** **`ActivityStream.tsx`** — **`ActivityStreamRow`** (48×48 thumb, **`ActivityLead`** meta, 21px truncated building title, **`user_buildings`** bookmark parity, building navigation) + **`ActivityStreamGroup`** (optional **Activity** label when **≥2** entries); **`docs/Roadmap.md`** + **`docs/ROADMAP.md`** Task 7 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 6 (2026-04-12):** **`CardTypeC.tsx`** — full-width **`CardImage`** at **`CARD_C_IMAGE_HEIGHT`**; text block **`pt-4`** with **`ActivityLead`** → **`BuildingHeadline`** md → **`BuildingSubtitle`** → **`CardFooter`** (no **`PointsBadge`**, body, or read-more); architect border, **`SuggestedContentBlock`**, navigation + save parity with **`CardTypeA`**/**`B`**; optional **`onImageLike`**; **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task 6 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 5 (2026-04-12):** **`CardTypeB.tsx`** — responsive **`md`**: **`md:h-80`** two-column grid, **`CardImage`** height synced via **`matchMedia`** to **`CARD_B_HEIGHT`** / **`CARD_C_IMAGE_HEIGHT`**; alternating or **`imagePosition`** column order; **`BuildingHeadline`** lg, body **`line-clamp-4`** + **Read more →**; text column **`md:h-full`** + **`CardFooter`** **`mt-auto`**; mobile single column stack + **`py-5 px-0`** text; **`SuggestedContentBlock`**, architect border, save/like/comment parity; **`docs/ROADMAP.md`** + **`docs/Roadmap.md`** Task 5 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 4 (2026-04-12):** **`CardTypeA.tsx`** — full-width **`group/card`** column, **`ActivityLead`** → **`BuildingHeadline`** xl → **`BuildingSubtitle`** → **`PointsBadge`** → clamped body + overflow-measured **Read more →** + **`CardFooter`**; architect left rule; **`SuggestedContentBlock`**; navigation + save/bookmark parity with **`ReviewCardFeed`**; **`docs/Roadmap.md`** Task 4 **[x]**; build + lint + typecheck green.
- **Roadmap Card System overhaul Task 3 (2026-04-12):** **`card-parts/CardImage.tsx`** — fixed pixel **`height`** shell, empty **`surface-muted`** placeholder, **`FadeInImg`** load fade + error → placeholder, single **`VideoPlayer`** / image, **`gap-[2px]`** pair grid, **`FeedPhotoCarousel`** for 3+ stills, stacked video + pair / carousel for mixed 3+ item rows; barrel export; **`docs/Roadmap.md`** Task 3 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 2 (2026-04-12):** **`src/features/feed/components/card-parts/`** — **`ActivityLead`**, **`BuildingHeadline`** (xl/lg/md fixed steps), **`BuildingSubtitle`**, **`CardFooter`**, **`PointsBadge`** + **`index.ts`** barrel; **`FeedHeroCard`**, **`ReviewCardFeed`**, **`FeedCompactCard`**, **`ReviewCardDetail`** import **`PointsBadge`** from barrel; **`docs/Roadmap.md`** Task 2 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System overhaul Task 1 (2026-04-12):** **`src/types/cards.ts`** — **`CardType`** only; **`resolveCardType`** + **`CARD_B_HEIGHT`** / **`CARD_C_IMAGE_HEIGHT`** in **`src/features/feed/utils/resolveCardType.ts`** + **`resolveCardType.test.ts`**; legacy **`CardSpec`** types moved into **`resolveCardSpec.ts`**; imports updated (**`FeedHeroCard`**, **`ReviewCardFeed`**, **`CardPlayground`**, **`cardFixtures`**, **`feed-aggregation`**); **`docs/Roadmap.md`** Task 1 **[x]**; **`npm run`** build, typecheck, lint, **`vitest run resolveCardType.test`** green.
- **Feed cards: attribution line replaces category labels (2026-04-11):** Removed **Review** / **Building** / **Visited** / **Saved** / **Collection** / **Activity** chrome on **`ReviewCardFeed`**, **`FeedHeroCard`**, **`FeedCompactCard`**, **`FeedActivityCard`**, **`FeedCollectionCard`**, **`FeedClusterCard`**; lead line is **`{username} visited`** or **`{username} wants to visit`** (or owner · building count for collections). **`DiscoveryBuildingCard`** — no **Visited**/**Saved** text badge; points-only chip with **`aria-label`** when rated. **`DiscoveryBuildingCard.overflow.test`** updated. Build, typecheck, lint, targeted tests green.
- **Feed cards: bookmark save control (2026-04-11):** **`ReviewCardFeed`** / **`FeedHeroCard`** / **`FeedCompactCard`** / **`FeedActivityCard`** — **Save** text → **`Bookmark`**; **`md+`** hidden until **`group/card`** hover or focus-visible; saved = filled bookmark (**`useUserBuildingStatuses`** + query invalidate on **`ReviewCardFeed`** save). **`DiscoveryCard`** swipe stamp = bookmark; rating overlay save column **`sr-only`** label. **`ReviewCardFeed.test`** — **`QueryClientProvider`**, **`cleanup`** between tests, bookmark **`aria-label`**. Typecheck + lint green.
- **Building detail stream spacing (2026-04-11):** **`BuildingDetails`** — **`space-y-10`** on the stream list; removed **`mb-0.5`** from featured / image-review / mosaic / image-only wrappers. Build, typecheck, lint green.
- **Card titles use Inter (2026-04-11):** **`ReviewCardFeed`** **`BuildingHeadline`** and **`FeedHeroCard`** **`h2`** — **`font-sans`** instead of **`font-display`** (Barlow). Typecheck + lint green.
- **Building detail stream author treatment (2026-04-11):** **`BuildingDetails`** editorial stream — **`StreamAuthorAttribution`** (avatar, `text-base`/`md:text-lg` name link, architect/verified/rating); featured blocks show author before pull quote. **`useReviewCardData`** + **`ReviewCardDetail`** — `avatarUrl` + matching columnist byline for detail/playground parity. Typecheck, lint, **`BuildingDetails.gallery.test`** green.
- **Feed review like label (2026-04-11):** **`ReviewCardFeed`** / **`ReviewCardDetail`** — footer count for **`likes_count`** reads **like / likes** (was note / notes). Typecheck + lint green.
- **Feed activity bucket-list lead line (2026-04-11):** **`FeedActivityCard`** — pending (`bucket list`) entries show **`{username} wants to visit`** in the former category-label slot; **Bucket list** label removed; username + action duplicate removed from byline (Save only). Visited layout unchanged.
- **Larger card title typography (2026-04-11):** **`ReviewCardFeed`** / **`FeedHeroCard`** / **`FeedCompactCard`** / **`FeedActivityCard`** / **`FeedCollectionCard`** / **`FeedClusterCard`** / **`DiscoveryBuildingCard`** / **`ReviewCardDetail`** / **`ColdStartFeed`** featured tile — building (or collection) headlines stepped up one tier on the type scale (`text-7xl` cap for elevated hero). Typecheck + lint green.
- **A24 feed follow-up (2026-04-11):** **`FeedPhotoCarousel`** — inner photo area uses **`h-full`** when **`className`** includes **`h-full`** (hero / **`FeedHeroCard`** fill), else **`aspect-[4/5]`**; **`SuggestedContentBlock`** — chromeless **Suggested** label (no padded brand box); **`FeedHeroCard`** **`FeedHeroSingleImage`** wrapper **`rounded-none`**; **`ReviewCardFeed`** — removed dead **`useHeroGrid`** (TS6133). Build, typecheck, lint green.
- **A24 editorial typography & card chrome (2026-04-11):** **`src/root.tsx`** — Google Fonts URL adds Barlow + Inter300 + Space Mono italics; **`tailwind.config.ts`** — **`fontFamily.display`** (Barlow); **`ReviewCardFeed`** **`BuildingHeadline`** — **`font-display`**, standard prominence **`text-3xl md:text-4xl`**; **`FeedHeroCard`** **`h2`** — **`font-display font-black leading-none`**; **`CollectionCard`** — no border/radius/shadow chrome, favourite **`border-b-2 border-text-primary`**, no neon name hover; **`DiscoveryBuildingCard`** — **`font-black text-xl`** title + **`group-hover:opacity-75`**. **`npm run`** build, typecheck, lint green (`turbo` not in PATH; used npm scripts).
- **Editorial feed design (2026-04-11):** **`ReviewCardFeed`** monochromatic points + architect treatments + **`shadow-card-elevated`** when **`prominence === 'elevated'`**; **`src/index.css`** **`.hairline`**; **`FeedHeroCard`** / **`FeedCompactCard`** / **`FeedActivityCard`** per editorial patterns; **`useReviewCardData`** trimmed (**no** **`avatarUrl`** / **`userInitial`** / **`action`**). Build, typecheck, lint, full Vitest green.
- **Building details action row (2026-04-11):** **`BuildingDetails`** — **Add note** / **Add to collection** / **Plan a visit** moved to the same row as **Visited** / **Save** / **Hide** + rating, right-aligned (replacing **Write review →** when signed in and status is visited or saved); **Write review →** unchanged for other states. Build, typecheck, lint green.
- **Building details UX (2026-04-11):** **`useBuildingInteractions`** — **`lastSuccessfulLoadBuildingId`** ref + **`buildingRef`** so **`fetchUserSpecificData`** does not call **`setLoading(true)`** on background refetches; stable **`userId`** / **`fetchTopLinks`** deps vs whole **`user`** object. Build, typecheck, lint, **`BuildingDetails.test`** green.
- **Roadmap Card System Task 5.3 (2026-04-11):** **`cardFixtures`** — frozen **`expectedSpec`** per fixture; **`CardPlayground`** “Show all” grid — resolved **`CardSpec`** badge + mismatch alert vs expectation; **`cardFixtures.resolve.test.ts`** regression test; **`docs/Roadmap.md`** Task 5.3 **[x]**; test, typecheck, lint, build green.
- **Roadmap Card System Task 5.2 (2026-04-11):** **`docs/COMPONENT_SPEC.md`** — new §14 **Card System** (`CardSpec`, resolver, Mermaid + ASCII flow, five archetypes, main feed vs **`ColdStartFeed`** components, playground); former Content Detail section renumbered to §15; **`docs/Roadmap.md`** Task 5.2 **[x]**; **`npm run`** build, typecheck, lint green.
- **Roadmap Card System Task 5.1 (2026-04-11):** Removed legacy **`ReviewCard.tsx`**; **`deprecated-artifacts-qa112`** no longer allowlists that file for **`@deprecated`**; **`FeedReview`** comments in **`feed.ts`** de-staled; **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 5.1 **[x]**; typecheck, lint, test, build green.
- **Roadmap Card System Task 4.3 (2026-04-11):** **`Index.tsx`** — **`renderSocialCard`** / **`renderRowCell`** pass **`item.spec`** / **`cell.spec`** into **`ReviewCardFeed`** (hero, compact, row compacts); discovery inject uses **`resolveCardSpec(post)`**; **`FeedHeroCard`** optional **`spec`**, **`resolveCardSpec`** fallback, **`imageWeight`** pair grid vs carousel, **`textWeight`** clamps + essay **Read more**, **`prominence`** **`shadow-card-elevated`**; **`FeedHeroCard*.test`**, **`Index.test`**, **`docs/ROADMAP.md`** Task 4.3 **[x]**; typecheck, lint, test, build green.
- **Roadmap Card System Task 4.2 (2026-04-11):** **`ReviewCardFeed`** — optional **`spec`** prop (defaults to **`resolveCardSpec(entry)`**); **`imageWeight`** drives carousel (gallery,3+ images, no video) / two-up grid (pair) / single; **`textWeight`** → **`line-clamp-card-*`** + essay **Read more →** expand; **`layout === 'compact-stack'`** → vertical chrome (header → media → body); **`prominence`** + override unchanged; **`useReviewCardData`** no longer returns **`useCarousel`**. **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 4.2 **[x]**; typecheck, lint, test, build green.
- **Roadmap Card System Task 4.1 (2026-04-11):** **`aggregateFeed`** (`src/lib/feed-aggregation.ts`) attaches **`spec: resolveCardSpec(entry)`** to **`hero`**, **`compact`**, and **`activity`** items; **`RowCell`** + **`collapseIntoRows`** forward **`spec`** into paired **`row`** cells when present; UI unchanged. **`docs/ROADMAP.md`** Task 4.1 **[x]**; **`feed-aggregation.test`** expectations updated; typecheck, lint, build green.
- **Roadmap Card System Task 3.4 (2026-04-11):** **`CardPlayground.tsx`** — **Page background** presets (feed light/dark canvas, building detail panel, profile), **viewport** widths 375 / 768 / 1280 with padded preview frame; **Prominence diff** (two **`ReviewCardFeed`** columns with forced standard vs elevated); **Copy fixture JSON** (single + show-all); **`ReviewCardFeed`** optional **`prominenceOverride`**; **`ReviewCardFeed.test`** elevated shadow case; **`docs/ROADMAP.md`** Task 3.4 **[x]**; typecheck, lint, build green.
- **Roadmap Card System Task 3.3 (2026-04-11):** **`CardPlayground.tsx`** — collapsible **Fixture controls** (sliders: image 0–8, words 0–500, likes 0–200, followers 0–2000; switches: verified / architect-of-building / broken image URLs / video); local **`editedEntry`** + reset to selected fixture; **`docs/ROADMAP.md`** Task 3.3 **[x]**; typecheck, lint, build green.
- **Roadmap Card System Task 3.2 (2026-04-11):** **`/superadmin/cards`** in **`app/routes.ts`** + **`App.tsx`** (lazy); **`CardPlayground.tsx`** — grouped fixture sidebar, **`ReviewCardFeed`** + **`ReviewCardDetail`** at **`max-w-md`**, **`resolveCardSpec`** debug panels, **Show all** grid; **`SuperadminGuard`** + **`isSuperadminAccess`** (`admin` / `app_admin` or **`VITE_SUPERADMIN_EMAILS`**); **`CardFixture.group`** on fixtures; **`vite-env.d.ts`** env typing; **`docs/ROADMAP.md`** Task 3.2 **[x]**; typecheck, lint, build green.
- **Roadmap Card System Task 3.1 (2026-04-11):** **`src/features/superadmin/fixtures/cardFixtures.ts`** — **`CardFixture`** type, **`cardFixtures`** (16 **`FeedReview`** mocks per ROADMAP archetypes), **`CARD_FIXTURE_COUNT`** guard; **`docs/ROADMAP.md`** Task 3.1 **[x]**; **`npm run`** typecheck, lint, build green.
- **Roadmap Card System Task 2.4 (2026-04-11):** Migrated **`EmptyFeed`**, **`DraggableReviewCard`**, **`Profile`** drag overlay to **`ReviewCardFeed`**; **`ReviewCardFeed`** supports **`variant`** / **`hideUser`** / **`hideBuildingInfo`** / **`imagePosition`** / **`showCommunityImages`** (compact + spec default parity with legacy non-detail); **`ReviewCard`** is thin **`@deprecated`** delegate to **`ReviewCardFeed`** / **`ReviewCardDetail`**; **`tests/unit/deprecated-artifacts-qa112.test.ts`** allowlist for that file; profile + draggable Vitest mocks target **`ReviewCardFeed`**; **`BuildingDetails`** had no **`ReviewCard`** import (no change); **`FeedHeroCard`** unchanged. **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 2.4 **[x]**; **`npm run`** typecheck, lint, build, full **`vitest`** green.
- **Roadmap Card System Task 2.3 (2026-04-11):** **`ReviewCardDetail.tsx`** — detail layout (`entry`, **`hideUser`** / **`hideBuildingInfo`**, **`onLike`** / **`onComment`**, hook options **`variant`** / **`showCommunityImages`**); **`useReviewCardData`**; **`ReviewCard`** delegates **`isDetailView`** to **`ReviewCardDetail`** via hook-free wrapper + **`ReviewCardNonDetail`** inner component (single hook path); **`src/features/feed/index.ts`** export; **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 2.3 **[x]**; **`npm run`** typecheck, lint, build + **`BuildingDetails*.test`** green.
- **Roadmap Card System Task 2.2 (2026-04-11):** **`ReviewCardFeed.tsx`** — feed-only card (`entry`, **`onLike`** / **`onImageLike`** / **`onComment`**); **`useReviewCardData`** + **`resolveCardSpec`**; card aspect tokens (**`aspect-card-*`**) from **`CardSpec.layout`**; **`shadow-card-elevated`** + **`font-bold`** building name when **`prominence === 'elevated'`**; **`ReviewCardFeed.test.tsx`** smoke test; **`src/features/feed/index.ts`** export; **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 2.2 **[x]**; **`npm run`** typecheck, lint, **`vitest run ReviewCardFeed.test`**, build green.
- **Roadmap Card System Task 2.1 (2026-04-11):** **`useReviewCardData`** in **`src/features/feed/hooks/useReviewCardData.ts`** — **`ReviewCardData`**, **`ReviewCardMediaItem`**, **`ReviewCardCarouselImage`**; media/subTitle/city/user/action/carousel flags; **`failedImages`** state in hook; **`ReviewCard.tsx`** consumes hook (no UI change). **`docs/ROADMAP.md`** Task 2.1 **[x]**; **`npm run`** typecheck, lint, build + **`EmptyFeed.test`** green.
- **Roadmap Card System Task 1.3 (2026-04-11):** **`src/index.css`** — `:root` card CSS variables (image ratios, line-clamp counts, elevated shadow); **`tailwind.config.ts`** — `aspect-card-*`, `line-clamp-card-*`, `shadow-card-elevated`; **`docs/DESIGN_TOKENS.md`** — §6b Card Tokens + §7 paste block sync; **`docs/ROADMAP.md`** / **`docs/Roadmap.md`** Task 1.3 **[x]**; no component changes. **`npm run`** typecheck, lint, build green.
- **Roadmap Card System Task 1.2 (2026-04-11):** **`src/features/feed/utils/resolveCardSpec.ts`** — pure **`resolveCardSpec(FeedReview)`** (word-count text tiers, usable image URLs → image weight, layout matrix, prominence from likes / followers / verified / architect-of-building); **`resolveCardSpec.test.ts`** (29 cases); **`src/features/feed/index.ts`** re-exports **`resolveCardSpec`**; **`docs/ROADMAP.md`** Task 1.2 **[x]**; **`vitest run resolveCardSpec`**, **`npm run`** typecheck, lint, build green.
- **Roadmap Card System Task 1.1 (2026-04-11):** **`src/types/cards.ts`** — **`CardSpec`**, **`CardLayout`**, **`CardImageWeight`**, **`CardTextWeight`**, **`CardProminence`** + JSDoc; **`AggregatedFeedItem`** optional **`spec?: CardSpec`**; **`ReviewUser.followers_count`** + **`RawFeedUserData`**; **`useFeed`** / **`useSuggestedFeed`** mapping; migration **`20270843000000_feed_user_data_followers_count.sql`**; **`docs/DATA_CONTRACT.md`** feed RPC notes; **`docs/ROADMAP.md`** Task 1.1 **[x]**; tests + **`npm run`** typecheck, lint, build, targeted Vitest green.
- **Building detail — pencil → edit route (2026-04-11):** Removed inline official-data editing on **`BuildingDetails`**; **`canEditOfficialData`** pencil is a **`Link`** to **`getBuildingUrl(…) + "/edit"`**; dropped **`isOfficialEditing`** / **`draftOfficialData`** / **`handleSaveOfficialData`** from **`useBuildingInteractions`**. **`npm run`** typecheck, lint, targeted **`BuildingDetails`** Vitest, build green.
- **Building detail — collections & visit under note block (2026-04-11):** **`BuildingDetails`** — **Add to collection** / **Plan a visit** (and expanded **`CollectionSelector`** / **`UserPicker`**) moved from below Resources into the signed-in **Visited** / **Save** note card, directly under **Save note**, with a divider; removed duplicate lower section. **`npm run`** typecheck, lint, **`BuildingDetails.test`**, build green.
- **Building detail — community stat: rating points sum (2026-04-11):** Replaced average rating (stars + “X.X avg”) with **sum of non-null `rating` values** per building (`totalRatingPoints` in **`useBuildingInteractions`**); **`BuildingDetails`** shows count + singular/plural **rating point(s)** and a **`title`** explaining the sum. **`npm run`** typecheck, lint, **`BuildingDetails.test`**, build green.
- **Building details — note on Visited / Save (2026-04-11):** **`BuildingDetails`** optional note **`Textarea`** when signed in and **`userStatus`** is **`visited`** or **`pending`**; **`setNote`** on **`useBuildingInteractions`** return; type fixes (**`accessSynthesis`**, orphan stream **`user`**). **`npm run`** typecheck, lint, **`BuildingDetails.test`**, build green.
- **BuildingDetails SSR crash — `useParams is not defined` (2026-04-11):** `BuildingDetails.tsx` on `main` was an incomplete refactor stub (no imports; placeholders). Restored the full page from commit `d75663a3` (pre-`b01e7ac3`). `useBuildingInteractions.ts` — `accessSynthesis` typed as `| null`, returned `verifiedClaims` / `hasVerifiedArchitect`, removed obsolete `eslint-disable-next-line react-hooks/exhaustive-deps`. `npm run` build, typecheck, lint; `BuildingDetails*.test.tsx` green.
- **Review photo upload + map pins guard (2026-04-10):** **`generate-upload-url`** Edge — presigned PUT via **`aws4fetch`** SigV4 query signing (no **`@aws-sdk/client-s3`** / no **`x-amz-checksum-*`**); path-style **`s3.{region}.amazonaws.com/{bucket}/{key}`**; **`X-Amz-Expires`** 3600, **`X-Amz-Content-Sha256=UNSIGNED-PAYLOAD`**. **`useBuildingSearch`** — **`get_map_clusters`** only with finite bounds. **Manual:** redeploy **`generate-upload-url`**; if CORS persists, S3 bucket CORS for **`https://www.plano.app`** / **`https://plano.app`**. `npm run` typecheck, lint, build green.
- **Write review: optional rating (2026-04-10):** **`reviewSubmitSchema`** allows **0–3**; **`WriteReview`** upserts **`rating: null`** when **0** (DB check allows **null** or **1–3** only). **`DATA_CONTRACT`** Zod example **`min(0)`**. `npm run` typecheck, lint, build green.
- **Invited-by onboarding follow as checkbox (2026-04-10):** `Onboarding.tsx` — default-checked **Follow {username}** checkbox replaces Follow button; follow row inserted in `completeOnboarding` when checked (Save and Continue / Skip for now); existing `suggest_follow` fallback if follow not created. `npm run` typecheck, lint, build green.
- **PWA version + cadence notifications (2026-04-09):** `tsconfig.node.json` `resolveJsonModule`; `vite.config.ts` `define.__APP_VERSION__`, `registerType: "autoUpdate"`; `src/vite-env.d.ts` global; `src/hooks/useVersionNotification.ts`; `usePwaInstall` drops `needRefresh` / `updateServiceWorker`; `AppShell` in `src/root.tsx` wires Sonner reload toast. `npm run` typecheck, lint, build green.
- **Building detail first-paint polish + SEO alignment (2026-04-09):** **`BuildingDetails.tsx`** — removed **`interactiveUiReady`** preview shell and full-page **`loading`** gate during **`fetchUserSpecificData`**; editorial layout from loader data renders on first paint (SSR HTML matches hydrated chrome); **`HydrateFallback`** uses main-column **`px`/`py`**; Vitest **`BuildingDetails*.test.tsx`** green; **`npm run`** typecheck, lint, build green.
- **Profile website + firm persistence (2026-04-09):** Migration **`20270842000000_profiles_firm_website.sql`** adds **`profiles.firm`** / **`profiles.website`**; **`Profile.tsx`** selects both, hydrates drafts, **`handleSaveHeader`** persists **`website`** (and **`firm`** when **`verified_architect_id`** is set) with **`profileHeaderUpdateSchema`**; **`Profile.loader`** + **`DATA_CONTRACT`** §1 + **`types.ts`** **`profiles`** row/insert/update aligned; Vitest **`profile-header-update-schema.test.ts`**. **`EmptyFeed.test.tsx`** assertions + hook mock reset aligned with current **`EmptyFeed`** copy (CI green).
- **Search SERP list copy + thumbnail inset (2026-04-09):** **`BuildingSidebar`** — buildings tab meta is architect credits + locality (**`city`**) only; removed country and year from the list line; **`pr-3`** on result rows so the image is not flush to the column border.
- **`get_building_reviews` restore + map guard (2026-04-09):** Migration **`20270841000000_get_building_reviews_restore.sql`** recreates RPC aligned with **`get_feed`** user flags + grants; **`BuildingLocationMap`** skips MapLibre when **`lat`/`lng`** are non-finite (fallback **Location unavailable**); Vitest **`BuildingLocationMap`** invalid-coords case.
- **Places autocomplete migration (2026-04-09):** Removed **`use-places-autocomplete`** (legacy **`AutocompleteService`**); added **`useAutocompleteSuggestions`** + **`googleMapsGeocoding`** — **`DiscoverySearchInput`**, **`LocationInput`**, **`OmniSearchBar`**, **`AddBuildingsToCollectionDialog`**; **`getGeocode`/`getLatLng`** imports from **`@/lib/googleMapsGeocoding`** elsewhere.
- **`get_feed` 400 / PL/pgSQL 42702 (2026-04-09):** Migration **`20270840000000_get_feed_ambiguous_id_plpgsql.sql`** — qualify **`people.id`** subquery as **`pe_sub.id`** (avoids clash with **`RETURNS TABLE (id …)`**); same one-line fix in **`20270839000000`** + **`20270837000000`** sources for clean full replays.
- **SSR auth hydration + upload auth check (2026-04-09):** **`getSessionForClientHydration`** in **`supabase.server.ts`** — **`getUser()`** then token fields from **`getSession()`** with verified **`user`** (avoids server **`insecureUserWarningProxy`** on **`Response.json({ session })`**); **`src/root.tsx`** + **`app/root.tsx`** loaders; **`upload.ts`** uses **`getUser()`** for signed-in guard.
- **Feed page errors + PWA workbox (2026-04-09):** Migration **`20270839000000_feed_main_image_and_steward_rls.sql`** — **`get_feed`** / **`get_suggested_posts`** use **`main_image_url(buildings)`**; **`plano_auth_is_company_steward`** + **`company_stewards`** SELECT policy; **`20270837000000`** bodies aligned. **`vite.config.ts`** **`workbox.navigateFallback: null`**. **`ColdStartFeed`** — **`creditedEntities`**, **`getBuildingUrl`** args; **`DATA_CONTRACT`** §9b RLS note.
- **Follows UI + contacts RLS (2026-04-09):** **`FollowButton`** syncs **`initialIsFollowing`** when Profile finishes loading; **`Profile`** resets follow state when **`targetUserId`** changes. Migration **`20270838000000_user_buildings_contacts_followers_select.sql`** aligns **`user_buildings`** SELECT for **`contacts`** with **`DATA_CONTRACT`** ( **`EXISTS` on `follows`** ) vs **`is_mutual_contact`**.
- **Roadmap QA 11.6 — Documentation audit (2026-04-09):** Verified **`DATA_CONTRACT.md`** / **`SECURITY.md`** for credits-era tables + token lifecycle; **`ROADMAP.md`** explicit **Status: Shipped** + QA 11.6 **[x]**; **`PRD.md`** §8.5–§9.5 + RPC table de-staled from removed **`architects`** catalog; QA 11.5 checklist line → **`DisconnectLegacyClaimDialog`**.
- **Roadmap QA 11.5 — Profile / nav regression gate (2026-04-09):** **`AppSidebar.test.tsx`** (claimed-person **My portfolio** visibility) + **`tests/unit/profile-nav-qa115.test.ts`**; **`docs/ROADMAP.md`** QA 11.5 **[x]** (multi-user browser UAT manual).
- **Roadmap QA 11.4 — Building detail regression gate (2026-04-09):** **`tests/unit/building-detail-pages-qa114.test.ts`** + targeted Vitest on **`BuildingDetails`** / **`BuildingHeader`** / **`BuildingAttributes`** / **`BuildingForm`** suites; **`docs/ROADMAP.md`** QA 11.4 **[x]** (live building flows + flag/hide manual).
- **Roadmap QA 11.3 — SEO / sitemap / meta (2026-04-09):** **`tests/unit/seo-sitemap-qa113.test.ts`**; **`docs/ROADMAP.md`** QA 11.3 **[x]** (live sitemap + Rich Results + prod **`<head>`** manual).
- **Roadmap QA 11.2 — Deprecated artifacts gate (2026-04-09):** **`tests/unit/deprecated-artifacts-qa112.test.ts`**; source tweaks (**`Notifications`**, **`architectIdRedirect.loader`**, **`ArchitectDashboardRedirect`**, **`OmniSearchBar`**, QA 10.1 / **`BuildingHeader`** / loader tests); **`docs/ROADMAP.md`** QA 11.2 **[x]** (migration log + **`tsc --noEmit`** + page smoke manual).
- **Roadmap QA 11.1 — Canonical routes in app/routes (2026-04-09):** **`tests/unit/routes-canonical-qa111.test.ts`**; **`docs/ROADMAP.md`** QA 11.1 **[x]** (browser navigation manual).
- **Roadmap QA 10.2 — Map credit filters (2026-04-09):** **`useURLMapState.qa102.test.tsx`**, **`useMapData.test.tsx`** + **`FilterDrawer.test.tsx`** QA block; **`docs/ROADMAP.md`** QA 10.2 **[x]** (live map RPC + migration manual).
- **Roadmap QA 10.1 — Global search + deprecated architect artifacts (2026-04-09):** **`no-architect-select-files-qa101.test.ts`**, **`BuildingSidebar.qa101.test.tsx`**, **`DiscoverySearchInput.qa101.test.tsx`**, **`ArchitectSearchNudge.qa101.test.tsx`**; **`docs/ROADMAP.md`** QA 10.1 **[x]** (browser Network UAT manual).
- **Roadmap QA 9.2 — Company steward dashboard (2026-04-09):** **`CompanyDashboard.qa92.test.tsx`**, **`AppSidebar.qa92.test.tsx`**; **`docs/ROADMAP.md`** QA 9.2 **[x]** (live owner queue + RPC manual).
- **Roadmap QA 9.1 — Person portfolio dashboard + profile portfolio tab (2026-04-09):** **`PersonDashboard.qa91.test.tsx`**, **`AppSidebar.qa91.test.tsx`**, **`Profile.qa91.test.tsx`**; **`docs/ROADMAP.md`** QA 9.1 **[x]** (live nav + DB stats cross-check manual).
- **Roadmap QA 8.3 — Audit log coverage (2026-04-09):** **`entity-audit-log-insert-qa83.test.ts`**, **`admin-audit-logs-schema-qa83.test.ts`**, **`BuildingAudit.qa83.test.tsx`**; **`docs/ROADMAP.md`** QA 8.3 **[x]** (live DB audit rows manual).
- **Roadmap QA 8.2 — Admin people/companies + entity claims (2026-04-09):** **`AdminPeople.test.tsx`**, **`AdminCompanies.test.tsx`**, **`EntityClaims.test.tsx`**, **`admin-no-architect-claims-route.test.ts`**, **`AdminSidebar.qa82.test.tsx`**; **`docs/ROADMAP.md`** QA 8.2 **[x]** (merge RPC + public badge + dispute DB manual).
- **Roadmap QA 8.1 — Flagged credits queue (2026-04-09):** **`FlaggedCredits.test.tsx`**; **`docs/ROADMAP.md`** QA 8.1 **[x]** (live admin + building visibility + email manual).
- **Roadmap QA 7.4 — Company claim dispute (2026-04-09):** **`CompanyDetails.qa74.test.tsx`**, **`CompanyClaimDispute.loader.test.ts`**, **`CompanyClaimDispute.test.tsx`**; **`docs/ROADMAP.md`** QA 7.4 **[x]** (DB + admin email manual).
- **Roadmap QA 7.3 — Steward access request (2026-04-09):** **`CompanyDetails.qa73.test.tsx`**, **`ApproveStewardRequest.loader.test.ts`**; **`docs/ROADMAP.md`** QA 7.3 **[x]** (email + DB idempotence manual).
- **Roadmap QA 7.2 — Company first claim (2026-04-09):** **`CompanyDetails.qa72.test.tsx`**, **`VerifyCompanyClaim.loader.test.ts`**, **`VerifyCompanyClaim.test.tsx`**; **`docs/ROADMAP.md`** QA 7.2 **[x]** (edge logs + DB smoke manual).
- **Roadmap QA 7.1 — Claim person profile (2026-04-09):** **`PersonDetails.qa71.test.tsx`** + **`no-claim-profile-dialog.test.ts`**; **`docs/ROADMAP.md`** QA 7.1 **[x]** (`notify-entity-claimed` logs manual).
- **Roadmap QA 6.4 — Remove credit by token (2026-04-09):** **`RemoveCredit.loader.test.ts`** + **`RemoveCredit.test.tsx`**; **`docs/ROADMAP.md`** QA 6.4 **[x]** (DB + building visibility + logged-out smoke remain manual).
- **Roadmap QA 6.3 — Notify step & email flow (2026-04-09):** **`AddCreditForm`** Send disabled when **`parsedNotify.invalid.length > 0`**; **`AddCreditForm.test.tsx`** QA 6.3 block; **`docs/ROADMAP.md`** QA 6.3 **[x]** (email/DB log checks remain manual UAT).
- **Roadmap QA 6.2 — AddCreditForm validation & batch (2026-04-09):** Vitest **`AddCreditForm.test.tsx`** QA 6.2 block + Radix pointer stubs in **`AddCreditForm`** suite; **`docs/ROADMAP.md`** QA 6.2 **[x]**.
- **Roadmap QA 6.1 — CreditEntityPicker (2026-04-09):** Vitest coverage in **`CreditEntityPicker.test.tsx`** (merged hits, create options, similarity accept, novel create, list scroll on narrow width); **`docs/ROADMAP.md`** QA 6.1 **[x]**.
- **Roadmap QA 5.3 — Flag a credit (2026-04-09):** **`BuildingCredits`** reason required before submit; **`BuildingCredits.test.tsx`** QA 5.3 + **`useIsMobile`** mock; **`credits-api-qa.test.ts`** **`flaggedByUserId`** row; Roadmap QA 5.3 **[x]**.
- **Roadmap QA 5.2 — Building credits section (2026-04-09):** **`BuildingCredits`** **`isAdmin`** + flagged filter/badge; **`BuildingDetails`** wiring; **`BuildingCredits.test.tsx`** QA 5.2 block; Roadmap QA 5.2 **[x]**.
- **Roadmap QA 5.1 — Primary credits header + search RPC (2026-04-09):** **`tests/unit/building-credit-display-qa.test.ts`**, **`tests/unit/search-buildings-rpc-qa.test.ts`**, **`BuildingHeader.test.tsx`**, **`ArchitectStatement.test.tsx`**; Roadmap QA 5.1 **[x]**.
- **Roadmap QA 4.2 — Company detail claimed + stewards (2026-04-09):** **`CompanyDetails.test.tsx`** (QA 4.2 block), **`EditCompanyForm.test.tsx`**; Roadmap QA 4.2 **[x]**.
- **Roadmap QA 4.1 — Company detail unclaimed (2026-04-09):** **`CompanyDetails.loader.test.ts`**, **`CompanyDetails.test.tsx`** (happy-dom **Radix Select** pointer-capture stubs); Roadmap QA 4.1 **[x]**.
- **Roadmap QA 3.3 — Legacy `/architect/:id` redirects (2026-04-09):** **`architectIdRedirect.loader.test.ts`** (301 → `/person/` / `/company/`, 404 invalid/unknown UUID, `?edit=1`, person query error propagation); **`docs/LAUNCH_HOSTING.md`** — 404/edit/single-hop prose + **Vercel / vercel.json** (no `/architect` rewrite; SSR loader); Roadmap QA 3.3 **[x]**.
- **Phase 11 Task 11.3 — Contract, PRD, security doc, roadmap shipped marker (2026-04-08):** See Current Phase bullet; `npm run typecheck`, `lint`, `build` green.
- **Phase 11 — Post-migration type alignment (2026-04-08):** After **`20270837000000`** on hosted DB + **`gen-types`**, updated **`buildings`** selects to **`hero_image_url`**, **`profiles`** rated-by query (**`username`** display), **`MergeComparison`** review images via **`user_buildings`** → **`review_images.review_id`**, **`merge_buildings`** arg names, **`MergeBuildings`** RPC args, admin counts, **`get_map_pins`** / **`get_discovery_feed`** null-safe args, **`flagCredit`** / Zod slug RPCs, **`BuildingFormData.slug`**, enum casts on building insert/update. **`npm run typecheck`**, **`lint`**, **`test`**, **`build`** green.
- **Phase 11 Task 11.1 — Remove `features/architect` + settings legacy claim UI (2026-04-08):** Deleted **`ArchitectDashboard.tsx`** / barrel; **`ArchitectDashboardRedirect`** in **`features/credits/pages`**; **`App.tsx`** + **`app/routes.ts`**; **`DisconnectArchitectDialog`** → **`DisconnectLegacyClaimDialog`** (copy + props **`legacyEntityId`**); **`Settings`** strings; **`FilterDrawer.test`** **`person-filter-select`** mock id; **`src/`** has no matches for **`architects`**, **`building_architects`**, **`ArchitectSelect`**, or **`ClaimProfileDialog`** (Roadmap 11.1 verify grep).
- **Phase 11 Task 11.1 — DATA_CONTRACT §9 + map Zod + RPC/edge registry (2026-04-08):** Replaced stale **Architect Domain** DDL/RLS/`ArchitectDTO` with post-drop **`architect_claims`** + routes/edge notes; §9d intro and **`building_credits`** DELETE/migration copy updated; **`MapFilterSchema`**: **`people`**, **`creditCompany`**, **`creditRoles`**; RPC table + **`sitemap`** registry row; **`20270837000000`** adds **`is_verified_architect_for_building`** rewrite using **`building_credits`** + **`people.claimed_by_user_id`** / **`company_stewards`** before legacy table drops.
- **Phase 11 partial — credits migration wiring (2026-04-08):** Search/map **`people`** URL + sidebar **`credit_names`**; library branch queries **`building_credits`**; building add/edit/admin edit use **`designCreditEntities`** and **`replacePrimaryDesignCredits`**; shared **`primaryBuildingCreditsToSummaries`**; discovery feed + collection map/add-dialog/itinerary/export migrated off **`building_architects`** where touched in this pass; **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Roadmap Task 10.2 — Credits filter on map discovery (2026-04-08):** Filter drawer Credits section; URL + RPC **`credit_company_id`** / **`credit_roles`**; migration + **`building_matches_credit_filters`**; **`CompanyMapFilterSelect`**; **`useMapData`** / sidebar / **`useBuildingSearch`** wiring; **`DATA_CONTRACT`**; tests; Roadmap 10.2 checked.
- **Roadmap Task 9.2 — Company steward portfolio dashboard (2026-04-08):** `CompanyDashboard`, `/company-portfolio`, steward nav hook + `AppSidebar` / user menu, owner approve/reject RPC migration + `companies.ts`, `CompanyDetails` `?edit=1`, `DATA_CONTRACT`, `plano-tables` RPC entries, tests; Roadmap 9.2 checked.
- **Roadmap Task 9.1 — Person portfolio dashboard + profile tab (2026-04-08):** `PersonDashboard`, `/portfolio`, sidebar/menu links, `useClaimedPersonForNav`, Profile portfolio tab + ArchitectDashboard redirect; `App.tsx`, `app/routes.ts`, `Profile.regression` expectation tweak; Roadmap 9.1 checked.
- **Roadmap Task 8.3 — Entity + credit audit logs (2026-04-08):** Migration RLS + RPC hooks; **`entity-audit-log.ts`**; **`credits.ts`** / **`companies.ts`**; **`BuildingAudit`** building filter + merged timeline; **`DATA_CONTRACT`** §11; Roadmap 8.3 checked.
- **Roadmap Task 8.2 — Admin: people/companies + unified entity claims (2026-04-08):** Migration `admin_merge_*` RPCs; `entity-management.ts`; `AdminPeople`, `AdminCompanies`, `EntityClaims`; sidebar Credits items; removed `ArchitectClaims.tsx`; `DATA_CONTRACT` §11; `plano-tables.types`; routes; Roadmap 8.2 checked.
- **Roadmap Task 8.1 — Admin: flagged credits moderation queue (2026-04-08):** Migration + RPC update; `notify-credit-outcome` + `CreditOutcomeEmail`; `FlaggedCredits` + sidebar Credits group; `credits.ts` admin helpers; moderation lib + unit tests; `DATA_CONTRACT`; `App.tsx` + `app/routes.ts`; Roadmap 8.1 checked.
- **Roadmap Task 7.4 — Dispute a company claim (2026-04-08):** `company_claim_disputes` migration + RLS; `notify-admin-dispute`; `CompanyDetails` dispute CTA + submitter-only notice; `CompanyClaimDispute` page; `companies.ts` helpers + Zod tests; `DATA_CONTRACT` §9b 3d; Roadmap 7.4 checked.
- **Roadmap Task 7.3 — Request steward invite for an already-claimed company (2026-04-08):** Migration + RPC + two Edge Functions + `/approve-steward-request/:token` + CompanyDetails CTA/dialog + `DATA_CONTRACT` §9b 3c; Roadmap 7.3 checked.
- **Roadmap Task 7.2 — Claim a company first claimant (2026-04-08):** `ClaimCompanyDialog`, `verify-company-claim`, `company_claim_verification_tokens` + `redeem_company_claim_token`, `/verify-company-claim/:token`, `/company/:slug/dispute` stub, `CompanyDetails` CTA + `claimVerified` flow, `DATA_CONTRACT` §9b; Roadmap 7.2 checked.
- **Roadmap Task 7.1 — Claim a person profile (2026-04-08):** `ClaimPersonDialog`, `PersonDetails` CTA + post-claim edit sheet, `claimPerson` / `notify-entity-claimed`, RPC migration, Edge Function + email template, `DATA_CONTRACT` §9a; Roadmap 7.1 checked.
- **Roadmap Task 6.4 — One-click credit removal via token (2026-04-08):** `/remove-credit/:token` route, loader + minimal page, RPC migration for building payload, `removeCreditByTokenWithClient`, tests + contract; Roadmap 6.4 checked.
- **Roadmap Task 6.3 — Post-submission notification email flow (2026-04-08):** `AddCreditForm` notify step + `parseCreditNotifyEmails`; `notifyCreditedEntities` + Edge Function `notify-credited-entities` + `CreditNotificationEmail.tsx`; `credit_notification_log` inserts; `config.toml` `verify_jwt = false`; tests; Roadmap 6.3 checked; DATA_CONTRACT §9d/§9e.
- **Roadmap Task 6.2 — AddCreditForm + batch submit (2026-04-08):** Multi-row credit sheet, sequential `addBuildingCredit`, per-row status, notify step stub; Zod/API notes cap 500 + `other` requires `roleCustom`; `AddCreditForm.test.tsx`; Roadmap 6.2 checked; DATA_CONTRACT §9d.
- **Roadmap Task 6.1 — CreditEntityPicker (2026-04-08):** Combined entity combobox + create flow; `creditCount` on company search; trigram similarity gate; tests; Roadmap Task 6.1 checked; contract sync.
- **Roadmap Task 5.3 — Flag a credit UI (2026-04-08):** `BuildingCredits` report flow; `creditFlagSession.ts`; `flag_building_credit` migration + `flagCredit` RPC client; `DATA_CONTRACT` §9d Component 4; `plano-tables.types.ts`; Roadmap Task 5.3 checked.
- **Roadmap Task 5.2 — Full credits section on building detail (2026-04-08):** `BuildingCredits.tsx`, wired in `BuildingDetails` after About; `BuildingCredits.test.tsx`; Roadmap Task 5.2 checked.
- **Roadmap Task 5.1 — Building header + search RPC from credits (2026-04-08):** `BuildingHeader` + `BuildingDetails` primary credits UI; `BuildingDetails.loader` + `useQuery`; `supabaseFallback` `searchBuildingsRpc` / `getBuildingsByIds` / `fetchBuildingDetails`; `structuredData` + tests; Roadmap checkbox.
- **Roadmap Task 4.2 — Company claimed + stewards (2026-04-08):** Migration invites + redeem RPC + DELETE policy; Edge Function `invite-company-steward`; `EditCompanyForm`, `CompanyDetails` steward UI, `AcceptCompanySteward`, `Auth` redirect param; `companies.ts` API; `DATA_CONTRACT` §9b; `plano-tables.types.ts`; Roadmap checkbox.
- **Roadmap Task 4.1 — Company detail page unclaimed (2026-04-08):** `CompanyDetails`, loader, `CompanyCreditCard`, `getCompanyWithClient`, `companyPageStructuredData`, `companyQueryKey`, routes + `App.tsx`, Roadmap checkbox.
- **Roadmap Task 3.3 — Retire `/architect/:id` + neutralise ArchitectDetails loader redirect (2026-04-08):** `ArchitectIdRedirect.tsx`, `app/routes.ts` + `App.tsx`, loader cleanup, `LAUNCH_HOSTING.md`, Roadmap checkbox.
- **Roadmap Task 3.2 — Person claimed + edit + profile link (2026-04-08):** `EditPersonForm`, `PersonDetails` owner UI + query cache, `Profile` professional card, `people.ts` helpers, `DATA_CONTRACT` §9a, Roadmap checkbox.
- **Roadmap Task 3.1 — Person detail page unclaimed (2026-04-08):** `/person/:slug`, loader, PersonCreditCard, tier grouping + ancillary collapsible, SSR meta + JSON-LD, `getPersonWithClient`, `short_id` on credit building summaries, `app/routes.ts` + `App.tsx`, Roadmap checkbox.
- **Roadmap Task 2.4 — API functions for `building_credits` (2026-04-08):** `src/features/credits/api/credits.ts`, migration `redeem_credit_removal_token`, DATA_CONTRACT §9d/§9e updates, `plano-tables.types.ts`, `credits-removal-token` unit tests, Roadmap checkbox.
- **Roadmap Task 2.3 — API functions for `companies` (2026-04-08):** `src/features/credits/api/companies.ts`, company portfolio DTOs in `types.ts`, DATA_CONTRACT §9b Component 4, Roadmap checkbox, `credits-companies-slug` unit tests.
- **Roadmap Task 2.2 — API functions for `people` (2026-04-08):** `src/features/credits/api/people.ts`, types + DATA_CONTRACT §9a Component 3, Roadmap checkbox, slug unit tests.
- **Favicon + PWA icons (2026-04-08):** Copied user-provided PNGs into `public/`; wired `rel="icon"` (16 + 32) and `apple-touch-icon` in `src/root.tsx` and `index.html`; extended `vite.config.ts` `includeAssets` with favicon PNGs; manifest `theme_color` / `background_color` set to `#000000` to match the icon. (Unused asset: `favicon-bafdcf23…` is JPEG data with a `.png` name — not committed.)
- **Roadmap Task 2.1 — credits TypeScript DTOs (2026-04-08):** `src/features/credits/types.ts`; deprecated JSDoc on `Architect` / `ArchitectBuilding`; `docs/ROADMAP.md` Task 2.1 checked.
- **Roadmap Task 1.5 — credit notification log + removal tokens (2026-04-08):** Migration adds `credit_notification_log` (recipient + token SHA-256 `bytea`), `credit_removal_tokens` (unique `token_hash`, `expires_at` default +30d, `used_at`), `generate_credit_removal_token(credit_id)` returns 64-char hex, inserts token row; pgcrypto; RLS no policies; execute grant service_role only; `DATA_CONTRACT` §9e; `plano-tables.types.ts`. **Manual:** apply `20270823000000_credit_notification_removal_tokens.sql` after 1.4.
- **Roadmap Task 1.4 — `building_credits` (2026-04-08):** Migration adds enums `credit_status_enum`, `credit_flag_reason_enum`; table with FKs to `buildings`, `people`, `companies`, `profiles`; backfill from `building_architects` (`design_architect`, `primary`, `is_lead`, `active`, `display_order` per building); RLS as above; `DATA_CONTRACT` §9d; `plano-tables.types.ts`. **Manual:** run `20270822000000_building_credits.sql` on Supabase after 1.1+1.2.
- **Roadmap Task 1.3 — `person_company_affiliations` (2026-04-08):** Migration creates table (unique `person_id`+`company_id`, year checks), copies `architect_affiliations` → `person_id`/`company_id` with preserved `created_at`; RLS (public select, authenticated insert, update/delete for person claim owner, company steward, or admin); `DATA_CONTRACT` §9c; `plano-tables.types.ts`. **Manual:** run `20270821000000_person_company_affiliations.sql` on Supabase after 1.1+1.2.
- **Roadmap Task 1.2 — `companies` + `company_stewards` (2026-04-08):** Migration creates tables, backfill studios (UUID preserve, slug suffixes, `country`/`website` from headquarters/url), owner rows + `claimed` from `profiles.verified_architect_id`, RLS (steward bootstrap insert for first owner); `DATA_CONTRACT` §9b; `plano-tables.types.ts`. **Manual:** run `20270820000000_companies_company_stewards.sql` on Supabase.
- **Roadmap Task 1.1 — `people` + credit enums (2026-04-08):** Migration creates `people`, enums, backfill from `architects` (`individual` only, preserve UUIDs, slug suffixes, claims from `profiles.verified_architect_id` + `architect_claims` verified), RLS; `DATA_CONTRACT` §9a; `plano-tables.types.ts` `people`; Roadmap checkbox. Applied on hosted Supabase.
- **PostGIS `spatial_ref_sys` security (2026-04-08):** Replaced RLS migration with `REVOKE ALL ... FROM anon, authenticated, PUBLIC` on `public.spatial_ref_sys` (hosted Supabase cannot `ALTER TABLE` as non-owner). Updated `20270807` + `20270818` migrations; see `.ai-status` KNOWN_ISSUES for SQL Editor role / support.
- **Feed collection cards — navigation URL (2026-04-08):** Replaced wrong `/:username/list/:slug` with `/:username/map/:slug` to match `CollectionCard` and `Route path="/:username/map/:slug"` (`CollectionMapPage`).
- **Feed collection cards — preview images (2026-04-08):** `FeedCollectionCard` passed storage paths as raw `img` src; now uses `getBuildingImageUrl` with `community_preview_url` fallback; `get_collections_feed` migration adds `community_preview_url` to preview JSON; `useCollectionsFeed` + `DATA_CONTRACT` DTOs updated.
- **Profile Visited tab — building thumbnails (2026-04-07):** `EditorialBuildingCard` used raw `main_image_url` (storage path) as `<img src>`; now resolves via `getBuildingImageUrl` with `community_preview_url` fallback; profile `user_buildings` query selects `community_preview_url`; `ProfileListView` thumbnails use the same fallback chain.
- **Sidebar Profile → own profile (2026-04-07):** Fixed "User not found" on `/profile` by dropping non-existent `firm`/`website` from `profiles` select (aligned with `types.ts` / DATA_CONTRACT). Deferred profile fetch until auth finishes; avoid not-found flash for logged-out `/profile`; sidebar + user menu link to `/profile/:username` when `useUserProfile` has username. Lint/type hygiene: removed dead `handleLike` / `handleSave`, Explore menu `onClick`, `EditorialBuildingCard` architect typing, legacy `section=log` handling.
- **Explore location filter — right Sheet + Places (2026-04-07):** Replaced bottom `Drawer` with right `Sheet` (`SheetContent` side right, `w-search-serp` on `sm+`); `DiscoverySearchInput` unchanged — Google Places `(regions)` suggestions (localities, admin areas, countries) + geocode for `handlePlaceDetails`.
- **Building detail hero — landscape + min width (2026-04-07):** `review_images.width_px` / `height_px` (migration `20270816100000_review_images_dimensions.sql`); client uploads store output dimensions via `resizeImageWithDimensions`; `buildingLoader` uses designated `hero_image_id` only when eligible (≥1000px wide, width > height) or when dimensions are unknown (legacy); else first image in popularity order (`likes_count`, `created_at`) that qualifies; then `community_preview_url`. Helpers in `src/lib/building-hero-image.ts`. Removed unused `Button` import in `Profile.tsx` (typecheck).
- **Map lime markers opaque fill (2026-04-07):** MapLibre `Marker` content is portaled onto the map canvas; Tailwind `bg-*` using `var(--…)` often did not paint pin faces. `getPinStyle` now sets `backgroundColor` from `MAP_MARKER_FILL` (mirrors `index.css`); `MapPin` applies it only when set; Tier S pulse, optional dot, and candidate mini-dots use the same inline lime. Tier 3 clusters use solid `#F7FFE0` instead of `/90` wash.
- **Search SERP column design tokens (2026-04-07):** Added `search-serp` (25rem) and `max-w-search-serp-alt` in `tailwind.config.ts` + `docs/DESIGN_TOKENS.md`. `SearchPage` desktop column uses `w-search-serp`, `left-0`, map area `md:ml-search-serp` (no inline px). `BuildingSidebar` SERP rows: `min-h-28`, `max-w-search-serp-alt`, `text-2xs` for micro labels.
- **Search map lime accent (2026-04-07):** Replaced legacy yellow / chartreuse map UI with design tokens: Tier S pins and pulse use `bg-brand-primary` + `text-brand-primary-foreground`; Tier 3 clusters use `bg-brand-secondary/90` + `border-brand-primary`; candidate dots use `bg-brand-primary`; “SUGGESTED” popup chip uses `bg-brand-primary` / `text-brand-primary-foreground`. Updated `pinStyling`, `MapPin`, `MapMarkers`, `BuildingPopupContent`, and map tests.
- **Search SERP sidebar full width (2026-04-07):** `BuildingSidebar` used `w-80` inside Search’s `w-[400px]` column (~80px dead strip); root is now `w-full min-w-0`. Scroll content uses `pl-4 pt-4 pb-4` (no `pr-4`) so building thumbnails align to the column edge beside the scrollbar.
- **Plano wordmark SVG (2026-04-07):** `PlanoLogo` replaced plain “PLANO” text with inline paths from `svgviewer-output.svg` (filled letter gaps); `fill="currentColor"` so `text-white` / `text-text-primary` call sites unchanged; `h-[1em] w-auto` scales with existing `text-*` classes; `role="img"` + `aria-label="Plano"`.
- **Explore / Search / Connect layout gap (~210px) (2026-04-07):** Offcanvas `Sidebar` spacer was still `w-[--sidebar-width]` when the menu was open (`data-collapsible=offcanvas` only when collapsed); spacer is now `w-0` whenever `collapsible === "offcanvas"`. Removed redundant desktop offsets: Explore `md:ml-[calc(var(--sidebar-width)-var(--sidebar-width-icon))]`, Connect dropped extra `md:pl-*`, Search `SIDEBAR_EXPANDED_OFFSET` on the 400px panel and map `marginLeft`. **Follow-up:** `MainLayout` `MainLayoutInset` applies `md:pl-[var(--sidebar-width)]` when the menu is open and `md:pl-16` when closed so content clears the overlay and floating trigger (no content under the menu).
- **Primary sans: Inter (2026-04-07):** Replaced Space Grotesk with Inter in `src/root.tsx` (Google Fonts), `tailwind.config.ts` `fontFamily.sans`, `src/index.css` `html` stack; `docs/DESIGN_TOKENS.md` typography table, font-loading examples, Tailwind paste block, application matrix copy, designer notes; Space Mono unchanged for `font-mono`.
- **Remove Playwright / e2e harness (2026-04-07):** Dropped `@playwright/test` and `playwright` from `package.json`, deleted `playwright.config.ts`, `tests/e2e/`, and all Playwright `*.spec.ts` files under `tests/` and `src/`; kept Vitest `tests/unit/` and `tests/repro/`; refreshed lockfile via `npm install`; updated `README.md`, `CONTRIBUTING.md`, `repomix.config.json`, `vitest.config.ts` excludes.
- **Sidebar SSR: no flash on refresh when menu closed (2026-04-07):** Root `loader` parses `sidebar:state` from `Cookie` (`parseSidebarOpenFromRequest` in `src/lib/sidebar-cookie.ts`); JSON adds `sidebarOpen`. `SidebarProvider` accepts `initialOpen` for first paint; `Root` → `AppShell` and `MainLayout` (`useRouteLoaderData("root")`) pass it. Client cookie read/write moved to `sidebar-cookie.ts`. `useLayoutEffect` still syncs document cookie for edge cases.
- **Google Fonts in document head (2026-04-07):** `src/root.tsx` `Layout` adds `preconnect` to `fonts.googleapis.com` / `fonts.gstatic.com` and the Inter + Space Mono stylesheet (`display=swap`) so `font-sans` / `font-mono` resolve instead of falling back to system UI.
- **Sidebar trigger: hamburger only, no card chrome (2026-04-07):** `SidebarTrigger` uses Lucide `Menu` (three strokes) instead of `PanelLeft`; `aria-expanded` + dynamic `aria-label`. `MainLayout` floating trigger drops border/bg/shadow; transparent hit area `min-h/w-11`, larger SVG. `Header` no longer pins `aria-label` to “Open menu”.
- **App sidebar logo size (2026-04-07):** `AppSidebar` header `PlanoLogo` uses `text-[2.5em] leading-none` (was `text-2xl`).
- **Sidebar default open + persist (2026-04-07):** `MainLayout` `SidebarProvider defaultOpen={true}`; `sidebar.tsx` restores `sidebar:state` cookie on load (desktop + mobile), persists mobile toggles, initial `openMobile` matches controlled `open`; nav links no longer call `setOpen`/`setOpenMobile` on route change.
- **Remove global top header (2026-04-07):** `AppLayout` defaults `showHeader` to false (no `Header` bar / `pt-16`). `MainLayout` adds a fixed minimal `SidebarTrigger` (hamburger only, no card chrome). Logged-out landing `Log in` is top-right; map search uses a mobile floating bar on `SearchPage`; Explore duplicate trigger removed; `NotFound` no longer mounts `Header`. `AppSidebar.test` uses controlled provider + scoped close query.
- **App sidebar black background (2026-04-07):** `Sidebar` inner panel used `bg-sidebar` (muted gray), covering `AppSidebar`’s outer `bg-black`. Added `--sidebar-background` in `src/index.css`, switched panel fills to `bg-[hsl(var(--sidebar-background))]`, forwarded `className`/`style` to mobile `SheetContent`, `AppSidebar` sets `0 0% 0%` + `text-white`; documented in `docs/DESIGN_TOKENS.md`.
- **Pre-deploy gate (2026-04-06):** `npm run build`, `typecheck`, `lint` — initial typecheck failed on unused lucide imports in `BucketListWidget.tsx` and `FeedActivityCard.tsx`; removed; all three green.
- **Roadmap Task 6.7:** CI SEO smoke-test QA (2026-04-06) — ran `scripts/seo-check.sh` vs `BASE_URL=https://plano.app`; production failed only on `/terms` canonical (live lag vs repo `Terms.tsx`); verified `fail()` stderr (URL / Expected / Actual) and simulated public-building `noindex` + missing-canonical detection; `.github/workflows/ci.yml` `seo-smoke` runs same script. Optional: disposable PR to prove CI red/green on preview.
- **Roadmap Task 6.6:** Sitemap QA (2026-04-06) — `curl -sSL https://plano.app/sitemap.xml | xmllint --noout` exit 0; sections static (4) + buildings (1000) + architects (999) + profiles (11); zero slug-less `/building/:id` URLs; 5 architect URLs return 200 without profile redirect; 7 distinct building `<lastmod>` dates; live partial-DB failure not executed — behavior covered by isolated queries in `supabase/functions/sitemap/index.ts` (Tasks 4.1–4.2). `docs/Roadmap.md` Task 6.6 marked complete; note bare `curl` without `-L` sees apex redirect HTML, not XML.
- **Roadmap Task 6.5:** `og-tags` QA documented in Roadmap (checkbox complete). Live `og-tags` endpoint may still 404 until `supabase functions deploy og-tags` — see KNOWN_ISSUES.
- **Roadmap Task 6.4:** noIndex QA — production `curl` spot-checks (private vs public); documented admin SSR `robots` gap on live vs local dev; added `NotFound.tsx` `loader` returning `data(null, { status: 404 })` for real HTTP 404 on unknown routes. `docs/Roadmap.md` Task 6.4 marked complete.
- **Roadmap Task 6.3:** SSR meta QA — production `curl` (Googlebot UA) on `www.plano.app`: `/`, sample building, architect, profile, review; canonical/description/og:title in raw HTML; absolute `og:image` where emitted; apex canonical host noted. No folder URLs in sitemap at check time (logged under KNOWN_ISSUES). `docs/Roadmap.md` Task 6.3 marked complete.
- **Roadmap Task 6.2:** `MetaHead` — codebase audit: no route mounts `MetaHead` (SSR `meta` elsewhere). Added `src/components/common/MetaHead.test.tsx` for canonical updates, single canonical link, `noIndex` / robots, JSON-LD add/remove, unmount cleanup. `docs/Roadmap.md` Task 6.2 marked complete.
- **SEO Roadmap Task 5.2:** `docs/LAUNCH_HOSTING.md` — Google Search Console operator steps (sitemap submit URL, URL Inspection expectations), Vercel `/sitemap.xml` rewrite table, GSC issue-category checklist vs. shipped SEO work, dated Googlebot UA spot-check table (`www.plano.app`, 2026-04-06). **Owner still performs** live Search Console submit/inspection per that doc. RR7 Phase 4.4 known issue removed after production curl spot-check.
- **SEO Roadmap Task 5.1:** `scripts/seo-check.sh` — `BASE_URL` (default `https://plano.app`), apex→origin normalization, public vs private assertions, slug-less building `301`; defaults for building/architect/profile smoke entities overridable via `SEO_TEST_*` env; PR job `seo-smoke` in `.github/workflows/ci.yml` using `VERCEL_PREVIEW_URL` secret. **SSR gaps closed for checks:** `Terms.tsx` exports full `meta` (canonical, description, OG/Twitter); `AdminDashboard.tsx` adds `noindex, nofollow` so `/admin` HTML matches smoke test (layout-only meta was not present in document head for that route).
- **SEO Roadmap Task 4.2:** Sitemap Edge Function — per-query `error` handling + try/catch for buildings, architects, and profiles; verified-architect exclusion query isolated (empty set on failure); partial XML when some sections fail; assembly errors return **500** with `no-store` (no fake minimal 200 sitemap); `Content-Length` + `console.log` of UTF-8 byte size; `docs/LAUNCH_HOSTING.md` notes Vercel rewrite passes through 5xx.
- **SEO Roadmap Task 4.1:** Sitemap edge function — `buildings` uses `updated_at` for `<lastmod>` and ordering; architects linked via `profiles.verified_architect_id` omitted from `/architect/:id` (profiles list still carries `/profile/:username`); comment re `banned_users`. Migration `20270816000000_buildings_updated_at.sql` adds `buildings.updated_at` + trigger; apply in Supabase before relying on sitemap lastmod.
- **SEO Roadmap Task 3.4:** Admin `meta` — `AdminLayout` sets `noindex, nofollow` for guarded admin subtree; per-page titles on dashboard/buildings/users/moderation/images/photos/audit/claims/storage-jobs/system; full `meta` on `Unauthorized`, `MergeBuildings`, `MergeComparison`; `NotFound` SSR title `Page Not Found | Plano` + `noindex`, removed `MetaHead`.
- **SEO Roadmap Task 3.1:** `EditBuilding.tsx` and `EditArchitect.tsx` export `meta: MetaFunction` with page title and `robots: noindex, nofollow` for SSR head on edit URLs.
- **SEO Roadmap Task 2.4:** `Profile.loader.ts` — `/profile` returns `noIndex: true` + `Cache-Control: private, no-store`; UUID segment with resolved `username` → `redirect(..., 301)` + `Cache-Control: no-store`. `Profile.tsx` `meta()` adds `robots: noindex, nofollow` when `data.noIndex`.
- **SEO Roadmap Task 2.2:** `FolderView.loader.ts` — server profile + `user_folders` fetch (RLS-aligned); `FolderView.tsx` exports `loader` + `meta` (title `"{name} by {username} | Plano"`, description, canonical `https://plano.app/{username}/folders/{slug}`, OG/Twitter with `cover.jpg`, `noindex` when `!is_public`); removed client `MetaHead`.
- **SEO Roadmap Task 2.1:** `ReviewDetails.loader.ts` — server `user_buildings` fetch with `profiles`, `buildings` (id, name, short_id, slug, main_image_url), `review_images`; `ReviewDetails.tsx` exports `loader` + `meta` (canonical `https://plano.app/review/:id`, OG/Twitter); removed client `MetaHead`.
- **SEO Roadmap Task 1.3:** `supabase/functions/og-tags/index.ts` — removed `http-equiv` refresh; added `<meta name="robots" content="noindex, nofollow">` and `<link rel="canonical">`; body copy no longer implies redirect. Deploy function to Supabase for production.
- **SEO Roadmap Task 1.2:** `MetaHead.tsx` — removed void stubs; single `useEffect` (deps: all props) upserts `description`, `robots`, full OG/Twitter set, absolutised `og:image`/`twitter:image` via `https://plano.app`, `og:url` from canonical or `window.location.href`, canonical link (removed when cleared), JSON-LD `script[data-meta="ld"]`; cleanup removes all `data-plano-metahead` nodes on unmount/update.
- **Build hygiene (same session):** `BuildingDetails` — correct `CollectionSelector` / `UserPicker` props; fix `handleLinkLike` no-unused-expressions; wire pending-photo strip + save, link list + “Save links & collections” to `handleSaveNote` / `removePendingImage` / `handleRemoveLink`. `Profile` / `Profile.loader` — replace `any` with narrow types; drop dead handlers. `npm run typecheck`, `lint`, `build` green.
- **SEO Roadmap Task 2.3:** `Index.tsx` exports static `meta` (title, description, OG/Twitter, canonical `https://plano.app/`); removed client-only `<MetaHead title="Home" />`. No `loader` on index route in repo (Roadmap text assumes one for auth — unchanged).
- **SEO Roadmap Task 1.1:** `buildingLoader` issues a 301 redirect to `/building/:id/:slug` when the building has a non-empty `slug` and the route segment is missing or wrong; `docs/Roadmap.md` Task 1.1 checked off.
- **Feed activity cards — community photo:** `FeedActivityCard` resolves image via `getBuildingImageUrl(main_image_url)` with fallback to `community_preview_url`; `aggregateFeed` Rule 0 treats either field as sufficient; `get_feed` / `get_suggested_posts` migration adds `community_preview_url` to `building_data` (apply `20270815000000_feed_building_data_community_preview_url.sql` in Supabase).
- **FIX-2 (Roadmap):** `FeedClusterCard` shows at most 3 bulleted building lines; when more exist, a trailing `text-text-secondary text-sm` line reads `and {n} more` (no bullet).
- **Phase 7 QA-3:** Verified `Index.tsx` — `FeedActivityCard`, `FeedCollectionCard`, `SectionDivider`, `ColdStartFeed` imported; legacy feed imports absent; `renderSocialCard` exhaustive for `hero` | `compact` | `activity` | `cluster` | `row`; `useSuggestedFeed`/`useCollectionsFeed` + `collectionItems` useMemo; `ColdStartFeed` when `socialReviews.length === 0`.
- **Phase 7 P7-99:** Spec sync — DATA_CONTRACT (`get_collections_feed`, feed DTOs, `mainImageUrl`), PRD feed note, Roadmap Phase 7 Summary, DESIGN_TOKENS + tailwind feed tokens, C7 component class cleanup for verify grep.
- **Vercel build — Guides + locality slug alignment (2026-04-16):** `GuidesPage` / `useGuides` / `guidesApi` import paths and `MetaHead`; `guidesApi` + locality queries use `localities.slug` (generated types) with DTO `city_slug` mapping; `EVENT_CARD_LIST_COLUMNS` matches `events` row; `contributors` credit filter `active`/`verified`; `TrendingBuildings` / `BucketListWidget` / loaders use `localities(slug)`; duplicate `react-router` imports in `EventDetail` / `Events`; `LocalityPage` loader extensions typed; `/// <reference types="vitest/import-meta" />` in `vite-env.d.ts`. **`npm run typecheck`**, **`lint`**, **`build`** green.
- **Phase 7 P7-97:** Quality gate — typecheck, lint, full Vitest, `react-router build` green; Vitest `~` alias + `useLoaderData` test harness fixes.
- **Phase 7 P7-3:** `Index.tsx` — `useCollectionsFeed` + `useSuggestedFeed({ enabled: !!user })`, interleaved collection cards, discovery heroes, `ColdStartFeed`, three-way infinite-scroll priority.
- **Phase 7 P7-2:** SQL `get_collections_feed`; `useCollectionsFeed`; barrel export; typed RPC.
- **Phase 7 P7-1B:** `AggregatedFeedItem` + `RowCell`, Rule 0, `collapseIntoRows`, tests.
- **Phase 7 P7-1A:** Feed types + `useFeed` `main_image_url`; `FeedCollectionCard` DTO alignment.
- **Phase 7 P7-0:** Pre-phase stability gate.
- **Search fix — map filter URL sync (2026-04-29):** `useURLMapState.setMapURL` now serializes `updates.filters` back into flat URL params (`q`, rating/contact/category/access/credit keys, etc.), restoring `MapContext.setFilter("query", ...)` behavior on `/search`; added regression test in `useURLMapState.qa102.test.tsx` to assert `setMapURL({ filters: { query } })` updates both URL and parsed filters.
- **Building posts migration unblock (2026-05-02):** `20270872000000_building_posts.sql` now drops/recreates `"Users can view user_buildings"` before removing `user_buildings.visibility`, and maps SELECT visibility checks to `building_posts.visibility` (including `contacts` follower semantics). Also expanded `"building_posts_select"` to preserve `contacts` reads after the migration.
- *(Earlier phases P0–P6 and launch tasks — see git history / prior `.ai-status` entries for detail.)*
