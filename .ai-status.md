## Current Phase
Phase 0 — Repo hygiene

## CURRENT_ARCHITECTURE_SNAPSHOT
Vite + React 18 SPA at repo root. Junk/debug files removed from root and loose SQL removed from `supabase/` (non-migrations). QA and verification markdown moved to `docs/archive/`.

## KNOWN_ISSUES
- `npm run typecheck` fails with many TS errors (Supabase `Database` types vs app usage: `buildings`, RPC names, etc.) — pre-existing; not introduced by Task 0.1.
- `npm run lint` reports hundreds of issues — pre-existing; not introduced by Task 0.1.

## Completed Tasks
- Task 0.1: Deleted verified junk files from repo root and `supabase/` root; moved 14 QA/historical markdown files to `docs/archive/`. `npm run build` succeeds.
