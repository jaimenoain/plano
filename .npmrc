# TypeScript 6 satisfies only an (optional) peer range of `typescript@^5` declared by
# some tooling (e.g. @react-router/dev). The dep tree is functionally correct — typecheck
# and build pass — so we relax strict peer resolution to keep `npm ci` working in CI.
legacy-peer-deps=true
